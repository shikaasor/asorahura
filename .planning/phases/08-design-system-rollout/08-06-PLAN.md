---
phase: 08-design-system-rollout
plan: 06
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/services/services.module.css
  - src/app/work/work.module.css
  - src/app/engage/engage.module.css
  - src/components/Navigation.module.css
  - src/components/shared/TestimonialCard.module.css
  - src/components/shared/TrustSignals.module.css
autonomous: true
requirements: [STYLE-02, STYLE-03, STYLE-05]

must_haves:
  truths:
    - "No hardcoded hex colors remain in services.module.css, work.module.css, engage.module.css, Navigation.module.css, TestimonialCard.module.css, or TrustSignals.module.css"
    - "The legacy var(--gold, #C9A060), var(--gold-light, #E0B878), and var(--bg-base, #04080F) fallback patterns are gone from every file in this plan, replaced by var(--accent) / var(--accent-hover) / var(--surface-1)"
  artifacts:
    - path: "src/components/Navigation.module.css"
      provides: "Token-only color/spacing/type declarations (site-wide nav, highest-impact single component)"
    - path: "src/app/services/services.module.css"
      provides: "Token-only color/spacing/type declarations"
  key_links:
    - from: "src/components/Navigation.module.css"
      to: "src/app/globals.css"
      via: "var(--surface-*), var(--ink-*), var(--accent*) custom property references"
      pattern: "var\\(--(surface|ink|accent|border|fontSize|fontWeight|spacing)-?"
---

<objective>
Convert the services, work, and engage page stylesheets plus the site-wide Navigation component and two shared components (TestimonialCard, TrustSignals) from hardcoded hex colors and legacy `var(--gold*, #hex)` / `var(--bg-base, #hex)` fallback syntax to Phase 6 tokens.

Purpose: Navigation.module.css renders on every page site-wide, making it the single highest-impact conversion target for visual consistency. services.module.css (31 hex) and engage.module.css (20 hex, plus a `var(--bg-base, #04080F)` hero) are the two heaviest remaining files outside the assessment/checkout/home domains already covered.
Output: 6 files converted to 100% token-driven color/spacing/type values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-design-system-rollout/08-RESEARCH.md
@.planning/phases/08-design-system-rollout/08-PATTERNS.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert services.module.css and work.module.css to tokens</name>
  <files>src/app/services/services.module.css, src/app/work/work.module.css</files>
  <read_first>
    src/app/services/services.module.css (full file — 31 hex occurrences, not yet read in detail, read fully before editing)
    src/app/work/work.module.css (full file — contains `var(--bg-base, #04080F)` line 3, `var(--gold, #C9A060)` lines 70/126, `var(--gold-light, #E0B878)` line 140)
    src/app/globals.css (token definitions, lines 3-58)
  </read_first>
  <action>
    For `services.module.css`: read the full file, classify every hex color's CSS property, and apply the property-context rule table from PATTERNS.md (`background`/`background-color` → `var(--surface-N)` or `var(--accent)`; `color:` → `var(--ink-N)` or semantic `var(--success/error/warn)`; `border-color:` → `var(--border-1)`/`var(--border-2)`; `box-shadow:` → `var(--shadow-N)`). If a hero/header section uses `#0a0a0a` or `#04080F` as background while the rest of the page is light, flip it to `var(--surface-1)`/`var(--ink-1)` (same theme-unification principle applied in Plans 08-02 and 08-05).

    For `work.module.css`: apply the exact legacy-fallback replacement: `var(--bg-base, #04080F)` → `var(--surface-1)` (background) or `var(--ink-1)` (text — check property context); `var(--gold, #C9A060)` → `var(--accent)`; `var(--gold-light, #E0B878)` → `var(--accent-hover)`. Then handle any remaining plain hex colors with the standard property-context rule table.

    Apply STYLE-05 spacing/type-scale normalization to both files: hardcoded `font-size` → nearest `--fontSize-N`; `font-weight` outside 400/700 → `var(--fontWeight-bold)`; `padding`/`margin`/`gap` → nearest `--spacing-N`. Leave one-off layout constants untouched.
  </action>
  <verify>
    <automated>grep -cE "var\(--(gold|bg-base)\b|#[0-9a-fA-F]{3,8}" src/app/services/services.module.css src/app/work/work.module.css</automated>
  </verify>
  <done>Both files return 0 from the grep above; `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Convert engage.module.css, Navigation.module.css, TestimonialCard.module.css, and TrustSignals.module.css to tokens</name>
  <files>src/app/engage/engage.module.css, src/components/Navigation.module.css, src/components/shared/TestimonialCard.module.css, src/components/shared/TrustSignals.module.css</files>
  <read_first>
    src/app/engage/engage.module.css (full file — contains `var(--bg-base, #04080F)` line 5, `var(--gold, #C9A060)` lines 57/66/140/199, `var(--gold-light, #E0B878)` line 213, `border-color: var(--gold, #C9A060)` line 172)
    src/components/Navigation.module.css (full file — contains `color: #C9A060 !important` line 72, `var(--gold, #C9A060)` lines 76/146, `var(--gold-light, #E0B878)` line 89)
    src/components/shared/TestimonialCard.module.css (full file — contains `var(--gold, #C9A060)` lines 4/49)
    src/components/shared/TrustSignals.module.css (full file — contains `var(--gold, #C9A060)` lines 17/30)
    src/app/globals.css (token definitions, lines 3-58)
  </read_first>
  <action>
    Apply this exact deterministic replacement to all four files:

    - `var(--bg-base, #04080F)` (engage.module.css line 5) → `var(--surface-1)` if `background`, `var(--ink-1)` if `color` (check property context).
    - `var(--gold, #C9A060)` → `var(--accent)` on every occurrence in all four files.
    - `var(--gold-light, #E0B878)` → `var(--accent-hover)` on every occurrence.
    - `color: #C9A060 !important` (Navigation.module.css line 72, a bare hex with no `var()` wrapper) → `color: var(--accent) !important` (preserve the `!important` — it is overriding a more specific selector's inherited color, not a code smell to remove in this plan).
    - `border-color: var(--gold, #C9A060)` (engage.module.css line 172) → `border-color: var(--accent)`.

    After the legacy-fallback pass, check all four files for any remaining plain hex colors and apply the property-context rule table from PATTERNS.md if found.

    Apply STYLE-05 spacing/type-scale normalization to all four files identically to prior tasks.
  </action>
  <verify>
    <automated>grep -cE "var\(--(gold|bg-base)\b|#[0-9a-fA-F]{3,8}" src/app/engage/engage.module.css src/components/Navigation.module.css src/components/shared/TestimonialCard.module.css src/components/shared/TrustSignals.module.css</automated>
  </verify>
  <done>All four files return 0 from the grep above; `npm run build` succeeds; site-wide navigation renders using `var(--accent)` for its gold interactive states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Pure CSS token substitution; Navigation.module.css is rendered on every page but this is a styling-only change, no new user input or data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Denial of Service (visual — nav becomes unusable) | Navigation.module.css color/contrast regression site-wide | mitigate | `npm run verify:contrast` (Plan 07) gates on every ink/surface/accent pairing before phase completion; Navigation renders on every route so Plan 07's manual route checks (`/articles`, `/privacy`, `/terms`, `/checkout`, `/assessment`) transitively cover it. |
</threat_model>

<verification>
Run `npm run build` after both tasks — must exit 0. Run `grep -cE "var\(--(gold|bg-base)\b|#[0-9a-fA-F]{3,8}" src/app/services/services.module.css src/app/work/work.module.css src/app/engage/engage.module.css src/components/Navigation.module.css src/components/shared/TestimonialCard.module.css src/components/shared/TrustSignals.module.css` — all six files must return 0.
</verification>

<success_criteria>
- Zero hardcoded hex colors and zero legacy `--gold`/`--gold-light`/`--bg-base` fallback references remain in the six CSS module files
- `npm run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/08-design-system-rollout/08-06-SUMMARY.md` when done
</output>
