---
phase: 08-design-system-rollout
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/checkout/checkout.module.css
  - src/components/checkout/PaddleCheckout.module.css
  - src/app/assessment/assessment.module.css
  - src/components/assessment/AssessmentShell.module.css
  - src/components/assessment/EmailGate.module.css
  - src/components/assessment/ProgressBar.module.css
  - src/app/checkout/success/page.tsx
  - src/components/checkout/OrderSummary.tsx
  - src/components/checkout/TierSelector.tsx
  - src/components/checkout/TrustBadges.tsx
autonomous: true
requirements: [STYLE-02, STYLE-03, STYLE-04, STYLE-05]

must_haves:
  truths:
    - "The /checkout hero renders on the light surface-1 background with ink-1 text, matching the rest of the page (no more #0a0a0a dark hero)"
    - "The /assessment hero renders on the light surface-1 background with ink-1 text, matching the rest of the page (no more #0a0a0a dark hero)"
    - "No hardcoded hex colors remain in checkout.module.css, PaddleCheckout.module.css, assessment.module.css, AssessmentShell.module.css, EmailGate.module.css, or ProgressBar.module.css"
    - "Tailwind gray/slate utility classes in checkout success + shared checkout components use token-backed arbitrary values instead"
  artifacts:
    - path: "src/app/checkout/checkout.module.css"
      provides: ".hero using var(--surface-1)/var(--ink-1); .tierCard.selected using var(--accent)"
    - path: "src/app/assessment/assessment.module.css"
      provides: ".hero using var(--surface-1)/var(--ink-1)"
  key_links:
    - from: "src/app/checkout/checkout.module.css"
      to: "src/app/globals.css"
      via: "var(--surface-1), var(--ink-1), var(--accent) custom property references"
      pattern: "var\\(--(surface|ink|accent|border|fontSize|fontWeight|spacing)-?"
    - from: "src/app/assessment/assessment.module.css"
      to: "src/app/globals.css"
      via: "var(--surface-1), var(--ink-1) custom property references"
      pattern: "var\\(--(surface|ink|border|fontSize|fontWeight|spacing)-?"
---

<objective>
Convert the checkout and core assessment-shell CSS modules from hardcoded hex colors to Phase 6 tokens, and unify the `/checkout` and `/assessment` hero sections from dark (`#0a0a0a`) to the light-first theme (STYLE-04's two explicitly named theme-collision routes). Also convert the Tailwind gray utility classes in the checkout success page and shared checkout components to token-backed arbitrary values.

Purpose: These are the two routes explicitly named in ROADMAP success criterion 4 as flipping light/dark — fixing them is the most visible, highest-priority theme-unification work in the phase.
Output: 10 files converted to 100% token-driven color/spacing/type values; `/checkout` and `/assessment` heroes render light.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-design-system-rollout/08-RESEARCH.md
@.planning/phases/08-design-system-rollout/08-PATTERNS.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert checkout.module.css and PaddleCheckout.module.css to tokens; unify hero to light theme</name>
  <files>src/app/checkout/checkout.module.css, src/components/checkout/PaddleCheckout.module.css</files>
  <read_first>
    src/app/checkout/checkout.module.css (full file — already read during planning; see mapping table below)
    src/components/checkout/PaddleCheckout.module.css
    src/app/globals.css (token definitions, lines 3-58)
  </read_first>
  <action>
    Apply this exact hex-to-token mapping to `checkout.module.css` (property context already resolved per PATTERNS.md's context-aware rule table):

    - `.page { background: #f9fafb }` → `background: var(--surface-2)`
    - `.hero { background: #0a0a0a; color: #fff }` → `background: var(--surface-1); color: var(--ink-1)` (THIS is the STYLE-04 fix — hero flips from dark to light)
    - `.heroLabel { color: #6b7280 }` → `color: var(--ink-2)`
    - `.heroTitle { color: #fff; font-weight: 800 }` → `color: var(--ink-1); font-weight: var(--fontWeight-bold)`
    - `.heroSub { color: #9ca3af }` → `color: var(--ink-3)`
    - `.step { color: #6b7280 }` → `color: var(--ink-2)`
    - `.stepNum { border: 1px solid #374151; color: #9ca3af }` → `border: 1px solid var(--border-2); color: var(--ink-3)`
    - `.stepActive .stepNum { background: #fff; border-color: #fff; color: #0a0a0a }` → `background: var(--surface-1); border-color: var(--surface-1); color: var(--ink-1)`
    - `.stepActive { color: #e5e7eb }` → `color: var(--ink-2)`
    - `.stepArrow { color: #374151 }` → `color: var(--ink-2)`
    - `.sectionLabel { color: #9ca3af }` → `color: var(--ink-3)`
    - `.tierCard { border: 1.5px solid #e5e7eb; background: #fff }` → `border: 1.5px solid var(--border-1); background: var(--surface-1)`
    - `.tierCard:hover { border-color: #6b7280 }` → `border-color: var(--border-2)`
    - `.tierCard.selected { border-color: #0a0a0a; background: #0a0a0a }` → `border-color: var(--accent); background: var(--accent)` (selected state now uses the accent token as the affordance, not a dark fill)
    - `.tierName { color: #0a0a0a }` → `color: var(--ink-1)`
    - `.tierCard.selected .tierName { color: #fff }` → `color: var(--ink-1)` (ink-1 on accent fill is the verified-contrast pairing per RESEARCH.md's contrast script rule)
    - `.tierPrice { color: #0a0a0a }` → `color: var(--ink-1)`
    - `.tierCard.selected .tierPrice { color: #d1d5db }` → `color: var(--ink-1)`
    - `.tierDesc { color: #6b7280 }` → `color: var(--ink-2)`
    - `.tierCard.selected .tierDesc { color: #9ca3af }` → `color: var(--ink-1)`
    - `.panel { background: #fff; border: 1.5px solid #e5e7eb }` → `background: var(--surface-1); border: 1.5px solid var(--border-1)`
    - `.summaryHeader { border-bottom: 1px solid #e5e7eb }` → `border-bottom: 1px solid var(--border-1)`
    - `.summaryTier { color: #0a0a0a; font-weight: 800 }` → `color: var(--ink-1); font-weight: var(--fontWeight-bold)`
    - `.summaryPrice { color: #0a0a0a; font-weight: 900 }` → `color: var(--ink-1); font-weight: var(--fontWeight-bold)` (900 is not a valid token weight — collapse to bold/700)
    - `.summaryPriceDetail { color: #9ca3af }` → `color: var(--ink-3)`
    - `.deliverable { color: #374151 }` → `color: var(--ink-2)`
    - `.deliverableCheck { color: #0a0a0a }` → `color: var(--ink-1)`
    - `.meta { border-top: 1px solid #e5e7eb }` → `border-top: 1px solid var(--border-1)`
    - `.metaRow { color: #6b7280 }` → `color: var(--ink-2)`
    - `.metaLabel { color: #374151 }` → `color: var(--ink-2)`
    - `.paymentPanel { border-top: 1px solid #e5e7eb }` → `border-top: 1px solid var(--border-1)`
    - `.paymentLabel { color: #9ca3af }` → `color: var(--ink-3)`
    - `.enterpriseCta { border-top: 1px solid #e5e7eb }`, `.enterpriseCtaText { color: #6b7280 }`, `.enterpriseBtn { background: #0a0a0a; color: #fff }`, `.enterpriseBtn:hover { background: #1f1f1f }` — this entire `.enterpriseCta`/`.enterpriseCtaText`/`.enterpriseBtn` rule block is now dead CSS (Plan 08-01 removed the only JSX that referenced these classes). Delete these three rule blocks entirely rather than converting them.
    - `.trustItem { color: #6b7280 }` → `color: var(--ink-2)`
    - `.trustIcon { border: 1px solid #e5e7eb }` → `border: 1px solid var(--border-1)`
    - `.trustDot { background: #0a0a0a }` → `background: var(--ink-1)`

    Also apply STYLE-05 spacing/type scale pass: replace `font-size: 0.75rem`→`var(--fontSize-1)`, `0.8rem`/`0.85rem`→`var(--fontSize-1)` (nearest, 12px), `0.875rem`/`0.9rem`/`1rem`→`var(--fontSize-2)` (16px), `1.5rem`→`var(--fontSize-5)` (24px); replace the `clamp(1.75rem, 4vw, 2.5rem)` heroTitle font-size with `var(--fontSize-6)` (32px) at desktop and reduce to `var(--fontSize-5)` inside the existing `@media (max-width: 768px)` block. Replace `font-weight: 600`→`var(--fontWeight-bold)`. Leave one-off layout constants alone (`max-width`, `gap` values that are already 4px multiples like `0.5rem`/`2rem` map to `var(--spacing-2)`/`var(--spacing-4)`; `1.25rem`→`var(--spacing-4)` nearest; `1.75rem`→`var(--spacing-5)` nearest).

    For `PaddleCheckout.module.css`: read the file, identify each hex color's CSS property (background/color/border/box-shadow), and apply the same property-context rule table (background→surface or accent, color→ink or semantic, border→border-1/2, box-shadow→shadow-1/2/3) per PATTERNS.md "Context-Aware Color Replacement" section.
  </action>
  <verify>
    <automated>grep -cE "#[0-9a-fA-F]{3,8}" src/app/checkout/checkout.module.css src/components/checkout/PaddleCheckout.module.css</automated>
  </verify>
  <done>Both files return 0 from the grep above; `.hero` in checkout.module.css uses `var(--surface-1)`/`var(--ink-1)`; `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Convert assessment.module.css, AssessmentShell.module.css, EmailGate.module.css, ProgressBar.module.css to tokens; unify hero to light theme</name>
  <files>src/app/assessment/assessment.module.css, src/components/assessment/AssessmentShell.module.css, src/components/assessment/EmailGate.module.css, src/components/assessment/ProgressBar.module.css</files>
  <read_first>
    src/app/assessment/assessment.module.css (full file — already read during planning; see mapping table below)
    src/components/assessment/AssessmentShell.module.css
    src/components/assessment/EmailGate.module.css
    src/components/assessment/ProgressBar.module.css
  </read_first>
  <action>
    Apply this exact hex-to-token mapping to `assessment.module.css`:

    - `.page { background: #fff }` → `background: var(--surface-1)`
    - `.hero { background: #0a0a0a; color: #fff }` → `background: var(--surface-1); color: var(--ink-1)` (THIS is the STYLE-04 fix)
    - `.heroTitle { color: #fff; font-weight: 800 }` → `color: var(--ink-1); font-weight: var(--fontWeight-bold)`
    - `.heroSub { color: #9ca3af }` → `color: var(--ink-3)`
    - `.pill { border: 1px solid #374151; color: #9ca3af }` → `border: 1px solid var(--border-2); color: var(--ink-3)`

    Also apply STYLE-05: `font-size: clamp(1.75rem, 4vw, 2.75rem)` on `.heroTitle` → `var(--fontSize-6)` (32px) desktop, reduce to `var(--fontSize-5)` in the existing `@media (max-width: 640px)` block; `font-size: 1.1rem` on `.heroSub` → `var(--fontSize-3)` (18px); spacing values `5rem 2rem 4rem` on `.hero` → `var(--spacing-6) var(--spacing-4) var(--spacing-5)`; `4rem 2rem 6rem` on `.shell` → `var(--spacing-6) var(--spacing-4) var(--spacing-6)`.

    For `AssessmentShell.module.css`, `EmailGate.module.css`, `ProgressBar.module.css`: read each file, identify every hex color's CSS property context, and apply the property-context rule table from PATTERNS.md (background→`--surface-N` or `--accent`, text color→`--ink-N` or semantic, border-color→`--border-1`/`--border-2`, box-shadow→`--shadow-N`). Any `#0a0a0a` or `#04080F` found → `var(--surface-1)` if background, `var(--ink-1)` if text. Any legacy `var(--gold, #C9A060)` or `var(--gold-light, #E0B878)` found → `var(--accent)` / `var(--accent-hover)` respectively. Any `var(--foreground)`/`var(--muted)`/`var(--border)` legacy alias found → `var(--ink-1)`/`var(--ink-2)`/`var(--border-1)` respectively (these Phase 6 legacy tokens no longer exist in globals.css, so any surviving reference is currently rendering nothing/invalid — fixing this restores intended styling, not just tidiness).
  </action>
  <verify>
    <automated>grep -cE "#[0-9a-fA-F]{3,8}" src/app/assessment/assessment.module.css src/components/assessment/AssessmentShell.module.css src/components/assessment/EmailGate.module.css src/components/assessment/ProgressBar.module.css</automated>
  </verify>
  <done>All four files return 0 from the grep above; `.hero` in assessment.module.css uses `var(--surface-1)`/`var(--ink-1)`; `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Convert Tailwind gray/slate utilities to token-backed arbitrary values in checkout success page + shared checkout components</name>
  <files>src/app/checkout/success/page.tsx, src/components/checkout/OrderSummary.tsx, src/components/checkout/TierSelector.tsx, src/components/checkout/TrustBadges.tsx</files>
  <read_first>
    src/app/checkout/success/page.tsx
    src/components/checkout/OrderSummary.tsx
    src/components/checkout/TierSelector.tsx
    src/components/checkout/TrustBadges.tsx
  </read_first>
  <action>
    This project uses Tailwind CSS v4 (`@import "tailwindcss"` in globals.css), which supports arbitrary-value utilities referencing CSS custom properties via `text-[var(--token)]` syntax. Replace every raw Tailwind gray/slate utility class with the token-backed equivalent, using this exact mapping (STYLE-03's "raw Tailwind grays → text scale" requirement):

    - `text-gray-900`, `text-gray-800`, `text-gray-700` → `text-[var(--ink-1)]`
    - `text-gray-600`, `text-gray-500` → `text-[var(--ink-2)]`
    - `text-gray-400`, `text-gray-300` → `text-[var(--ink-3)]`
    - `bg-gray-50` → `bg-[var(--surface-2)]`
    - `border-gray-200` → `border-[var(--border-1)]`
    - `hover:border-gray-400` → `hover:border-[var(--border-2)]`

    Specific per-file instances to convert (all four files, confirmed during planning):
    - `src/app/checkout/success/page.tsx` line 14: `text-3xl font-bold text-gray-900` → `text-[var(--fontSize-6)] font-bold text-[var(--ink-1)]`; line 15: `text-gray-600` → `text-[var(--ink-2)]`; line 19: `text-gray-500` → `text-[var(--ink-2)]`; line 21: `text-gray-700` → `text-[var(--ink-1)]`; line 28: `border-gray-200` → `border-[var(--border-1)]`; line 41: `text-gray-400 ... hover:text-gray-600` → `text-[var(--ink-3)] ... hover:text-[var(--ink-2)]`
    - `src/components/checkout/OrderSummary.tsx` line 6: `bg-gray-50` → `bg-[var(--surface-2)]`; line 8: `text-gray-500` → `text-[var(--ink-2)]`; line 9: `text-gray-900` → `text-[var(--ink-1)]`; line 10: `text-gray-900` → `text-[var(--ink-1)]`; line 11: `text-gray-500` → `text-[var(--ink-2)]`; line 14: `text-gray-700` → `text-[var(--ink-1)]`; lines 17,25,28: `text-gray-600` → `text-[var(--ink-2)]`
    - `src/components/checkout/TierSelector.tsx` line 13: `text-gray-700` → `text-[var(--ink-1)]`; line 21 (selected state, dark fill): `border-gray-900 bg-gray-900 text-white` → `border-[var(--ink-1)] bg-[var(--ink-1)] text-[var(--surface-1)]`; line 22 (unselected state): `border-gray-200 hover:border-gray-400 text-gray-800` → `border-[var(--border-1)] hover:border-[var(--border-2)] text-[var(--ink-1)]`; line 27: `text-gray-300` (selected, text-on-dark-fill) → `text-[var(--surface-3)]`; `text-gray-500` (unselected) → `text-[var(--ink-2)]`
    - `src/components/checkout/TrustBadges.tsx` line 13: `border-gray-200 ... text-gray-600` → `border-[var(--border-1)] ... text-[var(--ink-2)]`

    Do not change any non-color Tailwind utilities (layout, spacing, flex, etc.) — only the color-bearing gray/slate/white classes listed above.
  </action>
  <verify>
    <automated>grep -rEc "text-gray-|bg-gray-|border-gray-|text-slate-|bg-slate-|border-slate-" src/app/checkout/success/page.tsx src/components/checkout/OrderSummary.tsx src/components/checkout/TierSelector.tsx src/components/checkout/TrustBadges.tsx</automated>
  </verify>
  <done>All four files return 0 from the grep above; `npm run build` succeeds with no TypeScript/JSX errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Pure CSS/className token substitution; no new user input, no new external dependency, no change to data flow or auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure (visual regression) | checkout.module.css / assessment.module.css color flips | mitigate | `npm run verify:contrast` run at end of Wave 2 (Plan 07) validates every ink/surface pairing still meets WCAG floors after the flip. |
| T-08-04 | Tampering | Deletion of dead `.enterpriseCta`/`.enterpriseBtn` CSS rules | accept | Confirmed dead in Plan 08-01 (JSX branch removed); no live selector references these classes. |
</threat_model>

<verification>
Run `npm run build` after all three tasks — must exit 0. Run `grep -cE "#[0-9a-fA-F]{3,8}" src/app/checkout/checkout.module.css src/components/checkout/PaddleCheckout.module.css src/app/assessment/assessment.module.css src/components/assessment/AssessmentShell.module.css src/components/assessment/EmailGate.module.css src/components/assessment/ProgressBar.module.css` — every file must return 0.
</verification>

<success_criteria>
- `/checkout` and `/assessment` heroes render on `var(--surface-1)` with `var(--ink-1)` text (light theme, matching rest of page)
- Zero hardcoded hex colors remain in the six CSS module files
- Zero raw Tailwind gray/slate utility classes remain in the four checkout TSX files
- `npm run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/08-design-system-rollout/08-02-SUMMARY.md` when done
</output>
