---
phase: 08-design-system-rollout
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/home/HeroSection.module.css
  - src/components/home/Footer.module.css
  - src/components/home/AboutSection.module.css
  - src/components/home/LeadMagnetStrip.module.css
  - src/components/home/PainSection.module.css
  - src/components/home/ProcessTimeline.module.css
  - src/components/home/ServicesPreview.module.css
  - src/components/home/SocialProof.module.css
autonomous: true
requirements: [STYLE-02, STYLE-03, STYLE-05]

must_haves:
  truths:
    - "No hardcoded hex colors remain in any of the eight homepage-section CSS modules"
    - "The legacy var(--gold, #C9A060) and var(--gold-light, #E0B878) fallback pattern is gone from every homepage component, replaced by var(--accent) / var(--accent-hover)"
    - "The legacy var(--bg-base, #04080F) dark-surface fallback is gone from HeroSection, ProcessTimeline, and ServicesPreview, replaced by var(--surface-1)"
  artifacts:
    - path: "src/components/home/HeroSection.module.css"
      provides: "Token-only color/spacing/type declarations"
    - path: "src/components/home/ServicesPreview.module.css"
      provides: "Token-only color/spacing/type declarations"
  key_links:
    - from: "src/components/home/HeroSection.module.css"
      to: "src/app/globals.css"
      via: "var(--surface-1), var(--accent), var(--accent-hover) custom property references"
      pattern: "var\\(--(surface|ink|accent|border|fontSize|fontWeight|spacing)-?"
---

<objective>
Convert all eight homepage section CSS modules from hardcoded hex colors and legacy `var(--gold*, #hex)` / `var(--bg-base, #hex)` fallback syntax to Phase 6 semantic-scale tokens.

Purpose: These files are Phase 6's most common "partial migration" pitfall (RESEARCH.md Pitfall 2) — they already reference token names but keep the old v1.0 hex as a CSS custom-property fallback, so since `--gold`/`--bg-base` don't exist in globals.css, the fallback hex is what's actually rendering today. This plan removes every fallback and resolves directly to the canonical token, fixing both STYLE-02 (hex audit) and STYLE-03 (gold/dark-surface collision resolution) for the homepage.
Output: 8 files converted to 100% token-driven color/spacing/type values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-design-system-rollout/08-RESEARCH.md
@.planning/phases/08-design-system-rollout/08-PATTERNS.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert HeroSection, Footer, AboutSection, LeadMagnetStrip to tokens</name>
  <files>src/components/home/HeroSection.module.css, src/components/home/Footer.module.css, src/components/home/AboutSection.module.css, src/components/home/LeadMagnetStrip.module.css</files>
  <read_first>
    src/components/home/HeroSection.module.css (full file — contains `var(--bg-base, #04080F)` on line 3, `var(--gold, #C9A060)` on lines 62/71/103, `var(--gold-light, #E0B878)` on line 118)
    src/components/home/Footer.module.css (full file — contains `var(--gold, #C9A060)` on lines 28/89)
    src/components/home/AboutSection.module.css (full file — contains `var(--gold, #C9A060)` on line 93, `var(--gold-light, #E0B878)` on lines 104/105)
    src/components/home/LeadMagnetStrip.module.css (full file — contains `var(--gold, #C9A060)` on line 66, `var(--gold-light, #E0B878)` on line 81)
    src/app/globals.css (token definitions, lines 3-58)
  </read_first>
  <action>
    Read all four files in full, then apply this deterministic legacy-token replacement (confirmed exact occurrences during planning):

    - `var(--bg-base, #04080F)` (any property) → `var(--surface-1)` if used as `background`/`background-color`; `var(--ink-1)` if used as `color`/text (verify property context per line before choosing).
    - `var(--gold, #C9A060)` → `var(--accent)` (used as `background` on CTAs/highlights) or `var(--ink-1)`-paired-with-`var(--accent)`-fill context — check each occurrence's property: `color:` → `var(--accent)` stays as `var(--accent)` (accent is also valid as a text/icon color for gold accents, not just fills); `background:` → `var(--accent)`.
    - `var(--gold-light, #E0B878)` → `var(--accent-hover)` (this was the lighter gold variant, now maps to the hover state token).

    Then handle any remaining plain hex colors (not in `var(--x, #hex)` fallback form) in these four files using the standard property-context rule table from PATTERNS.md: `background`/`background-color` → `var(--surface-N)` or `var(--accent)`; `color` → `var(--ink-N)` or semantic; `border-color` → `var(--border-1)`/`var(--border-2)`; `box-shadow` → `var(--shadow-N)`.

    Apply STYLE-05 spacing/type normalization: any hardcoded `font-size` in px/rem/clamp() → nearest `--fontSize-N` token; any `font-weight` outside 400/700 → `var(--fontWeight-bold)`; any `padding`/`margin`/`gap` in rem/px → nearest `--spacing-N` token. Leave one-off layout constants (`max-width`, `grid-template-columns`) untouched.
  </action>
  <verify>
    <automated>grep -cE "var\(--(gold|bg-base|foreground|muted)\b|#[0-9a-fA-F]{3,8}" src/components/home/HeroSection.module.css src/components/home/Footer.module.css src/components/home/AboutSection.module.css src/components/home/LeadMagnetStrip.module.css</automated>
  </verify>
  <done>All four files return 0 from the grep above (no legacy token fallbacks, no raw hex); `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Convert PainSection, ProcessTimeline, ServicesPreview, SocialProof to tokens</name>
  <files>src/components/home/PainSection.module.css, src/components/home/ProcessTimeline.module.css, src/components/home/ServicesPreview.module.css, src/components/home/SocialProof.module.css</files>
  <read_first>
    src/components/home/PainSection.module.css (full file — contains `var(--gold, #C9A060)` on lines 43/61)
    src/components/home/ProcessTimeline.module.css (full file — contains `var(--bg-base, #04080F)` on line 3, `var(--gold, #C9A060)` on lines 21/57/58)
    src/components/home/ServicesPreview.module.css (full file — contains `var(--bg-base, #04080F)` on line 3, `var(--gold, #C9A060)` on lines 21/111/128/163/193)
    src/components/home/SocialProof.module.css (full file — contains `var(--gold, #C9A060)` on lines 37/115)
    src/app/globals.css (token definitions, lines 3-58)
  </read_first>
  <action>
    Apply the identical deterministic legacy-token replacement from Task 1 to these four files: `var(--bg-base, #04080F)` → `var(--surface-1)` (background) or `var(--ink-1)` (text, check property context first); `var(--gold, #C9A060)` → `var(--accent)`; `var(--gold-light, #E0B878)` → `var(--accent-hover)` (if present).

    Then handle any remaining plain hex colors in these four files using the property-context rule table from PATTERNS.md (background→surface/accent, text→ink/semantic, border→border-1/2, box-shadow→shadow-N).

    Apply the same STYLE-05 spacing/type normalization described in Task 1's action to all four files.
  </action>
  <verify>
    <automated>grep -cE "var\(--(gold|bg-base|foreground|muted)\b|#[0-9a-fA-F]{3,8}" src/components/home/PainSection.module.css src/components/home/ProcessTimeline.module.css src/components/home/ServicesPreview.module.css src/components/home/SocialProof.module.css</automated>
  </verify>
  <done>All four files return 0 from the grep above; `npm run build` succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Pure CSS token substitution on the marketing homepage; no new user input, no new external dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Information Disclosure (visual regression) | Homepage hero/CTA color flips (highest-traffic page) | mitigate | `npm run verify:contrast` (Plan 07) gates on every ink/surface/accent pairing; homepage is explicitly in Plan 07's manual route-check list via `/` being the site's primary entry, covered by the general build+contrast gate. |
</threat_model>

<verification>
Run `npm run build` after both tasks — must exit 0. Run `grep -cE "var\(--(gold|bg-base|foreground|muted)\b|#[0-9a-fA-F]{3,8}" src/components/home/*.module.css` filtered to the 8 files in this plan — all must return 0.
</verification>

<success_criteria>
- Zero hardcoded hex colors and zero legacy `--gold`/`--bg-base`/`--foreground`/`--muted` fallback references remain in the eight homepage CSS modules
- `npm run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/08-design-system-rollout/08-04-SUMMARY.md` when done
</output>
