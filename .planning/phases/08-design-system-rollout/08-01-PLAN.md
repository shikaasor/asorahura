---
phase: 08-design-system-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.module.css
  - src/components/Testimonials.tsx
  - src/components/Testimonials.module.css
  - src/components/SaasShowcase.tsx
  - src/components/SaasShowcase.module.css
  - src/components/LinkedInFeed.tsx
  - src/components/LinkedInFeed.module.css
  - src/components/YouTubeFeed.tsx
  - src/components/YouTubeFeed.module.css
  - public/images/testimonials/lloydlist.jpg
  - public/images/testimonials/lloydlist.png
  - src/app/services/page.tsx
  - src/app/checkout/page.tsx
autonomous: true
requirements: [STYLE-01]

must_haves:
  truths:
    - "Dead component files, their CSS modules, the untracked lloydlist images, and the two dead code lines no longer exist in the repo"
    - "npm run build succeeds with zero broken-import errors after all deletions"
  artifacts:
    - path: "src/app/services/page.tsx"
      provides: "ServicesPage without the unused `const year` local"
    - path: "src/app/checkout/page.tsx"
      provides: "CheckoutInner without the always-false isEnterprise branch"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/app/page.module.css"
      via: "no import (file deleted, confirmed zero references before deletion)"
      pattern: "page.module.css"
---

<objective>
Delete all dead code identified in Phase 8 research BEFORE any style/token conversion work begins, per the ROADMAP's hard ordering constraint. This is Wave 1 and must complete (and be committed) before Wave 2 color-conversion plans start, since those plans assume a clean file tree with no orphaned dead-code noise competing with the real conversion targets.

Purpose: Remove ~1,083 lines of unused components, CSS modules, and dead branches so Phase 8's remaining work (hex-to-token conversion) only touches live, rendered code — and so future greps/audits aren't polluted by dead files.
Output: 11 files/assets deleted, 2 files edited to remove dead lines, verified via `npm run build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-system-rollout/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead components, their CSS modules, and untracked test images</name>
  <files>src/app/page.module.css, src/components/Testimonials.tsx, src/components/Testimonials.module.css, src/components/SaasShowcase.tsx, src/components/SaasShowcase.module.css, src/components/LinkedInFeed.tsx, src/components/LinkedInFeed.module.css, src/components/YouTubeFeed.tsx, src/components/YouTubeFeed.module.css, public/images/testimonials/lloydlist.jpg, public/images/testimonials/lloydlist.png</files>
  <read_first>
    .planning/phases/08-design-system-rollout/08-RESEARCH.md (Dead Code Inventory section, lines 46-67) — confirms all 11 items have zero live imports.
  </read_first>
  <action>
    Before deleting, re-run the verification greps yourself (do not trust research blindly — it is your job to confirm before deleting per the phase's ordering constraint):
    - `grep -rn "Testimonials" src --include="*.tsx" --include="*.ts"` — expect only the file being deleted and possibly a stale comment in services/page.tsx (a comment reference, not an import, is fine to leave since it's a comment, not code — but if you find an actual `import Testimonials` anywhere outside Testimonials.tsx itself, STOP and report instead of deleting).
    - `grep -rn "SaasShowcase" src --include="*.tsx" --include="*.ts"` — expect zero matches outside the file itself.
    - `grep -rn "LinkedInFeed" src --include="*.tsx" --include="*.ts"` — expect zero matches outside the file itself.
    - `grep -rn "YouTubeFeed" src --include="*.tsx" --include="*.ts"` — expect zero matches outside the file itself.
    - `grep -rn "page.module.css" src/app/page.tsx` — expect zero matches (root page.tsx has no CSS module import; confirmed during planning).
    - `grep -rln "lloydlist" src public` — expect zero references (images are untracked per git status).

    If all checks confirm zero live usage, delete the 11 files/assets: `src/app/page.module.css`, `src/components/Testimonials.tsx`, `src/components/Testimonials.module.css`, `src/components/SaasShowcase.tsx`, `src/components/SaasShowcase.module.css`, `src/components/LinkedInFeed.tsx`, `src/components/LinkedInFeed.module.css`, `src/components/YouTubeFeed.tsx`, `src/components/YouTubeFeed.module.css`, `public/images/testimonials/lloydlist.jpg`, `public/images/testimonials/lloydlist.png`. Use `git rm` for tracked files; plain delete for the two untracked lloydlist images (they will show as deletions of untracked files, i.e. simply gone — no git rm needed since they were never added).
  </action>
  <verify>
    <automated>grep -rn "Testimonials\|SaasShowcase\|LinkedInFeed\|YouTubeFeed" src --include="*.tsx" --include="*.ts" | grep -v "^src/components/Testimonials.tsx\|^src/components/SaasShowcase.tsx\|^src/components/LinkedInFeed.tsx\|^src/components/YouTubeFeed.tsx" | wc -l</automated>
  </verify>
  <done>All 11 files/assets no longer exist; the grep above returns 0 (excluding the deleted files themselves, which no longer exist to match anyway).</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead const year and dead isEnterprise branch</name>
  <files>src/app/services/page.tsx, src/app/checkout/page.tsx</files>
  <read_first>
    src/app/services/page.tsx (line 76-80, `const year = new Date().getFullYear();` inside `export default function ServicesPage()`, never referenced elsewhere in the function body)
    src/app/checkout/page.tsx (line 24 `const isEnterprise = false;` and lines 115-123 the `{isEnterprise ? (...) : (...)}` conditional)
  </read_first>
  <action>
    In `src/app/services/page.tsx`: delete the line `const year = new Date().getFullYear();` (currently line 78, inside `ServicesPage()`). Re-verify first with `grep -n "\byear\b" src/app/services/page.tsx` that `year` is not referenced anywhere else in the file — if it is, stop and report instead of deleting.

    In `src/app/checkout/page.tsx`: re-verify first with `grep -rn "isEnterprise" src` that the `isEnterprise` in `checkout/page.tsx` is a distinct local variable (confirmed during planning: `engage/page.tsx` and `engage/actions.ts` have their own separate `isEnterprise` locals unrelated to checkout — do not touch those files). Confirm `const isEnterprise = false;` on line 24 of `checkout/page.tsx` is never reassigned and never receives a prop/query value anywhere in the file (it is a hardcoded literal). Once confirmed:
    - Delete `const isEnterprise = false;` (line 24).
    - Replace the ternary `{isEnterprise ? (<div className={styles.enterpriseCta}>...</div>) : (<div className={styles.paymentPanel}>...</div>)}` (lines 115-129) with just the unconditional `<div className={styles.paymentPanel}>...</div>` block (the always-true branch), removing the `<div className={styles.enterpriseCta}>` dead branch entirely.
    - Leave `.enterpriseCta`, `.enterpriseCtaText`, `.enterpriseBtn` CSS rules in `checkout.module.css` alone in this task — Plan 02 (wave 2) will clean up now-unused CSS classes as part of its token-conversion pass over that same file, since editing that CSS module in two different wave-2/wave-1 plans in the same file would create a merge conflict. Do not modify checkout.module.css in this task.
  </action>
  <verify>
    <automated>grep -c "isEnterprise" src/app/checkout/page.tsx || true; grep -c "const year" src/app/services/page.tsx || true</automated>
  </verify>
  <done>`src/app/services/page.tsx` has no `const year` declaration; `src/app/checkout/page.tsx` has no `isEnterprise` identifier and renders the payment panel unconditionally; `npm run build` succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Pure dead-code deletion; no new trust boundary, no user input, no external dependency introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Deletion of `isEnterprise` branch in checkout/page.tsx | accept | Branch was confirmed unreachable (hardcoded `false`, never reassigned) before deletion; no behavior change for any real user path. |
| T-08-02 | Denial of Service (broken build) | File deletions breaking imports elsewhere | mitigate | Re-verify zero-import status via grep immediately before each deletion (in-task); `npm run build` gate after all deletions. |
</threat_model>

<verification>
Run `npm run build` after both tasks complete — must exit 0 with no "Module not found" errors referencing any of the 11 deleted files, and no TypeScript errors referencing `year` or `isEnterprise` in the two edited files.
</verification>

<success_criteria>
- All 11 dead files/assets removed from the working tree
- `const year` removed from services/page.tsx; `isEnterprise` and its dead branch removed from checkout/page.tsx
- `npm run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/08-design-system-rollout/08-01-SUMMARY.md` when done
</output>
