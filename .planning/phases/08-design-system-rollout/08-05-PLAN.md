---
phase: 08-design-system-rollout
plan: 05
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/privacy/privacy.module.css
  - src/app/terms/terms.module.css
  - src/app/refund/refund.module.css
  - src/app/articles/articles.module.css
  - src/app/articles/[slug]/article.module.css
  - src/app/blog/page.module.css
  - src/app/blog/[slug]/article.module.css
autonomous: true
requirements: [STYLE-02, STYLE-05]

must_haves:
  truths:
    - "No hardcoded hex colors remain in any of the seven legal/blog/article page CSS modules"
    - "The legacy var(--foreground), var(--muted), and var(--border) aliases (deleted in Phase 6) no longer appear anywhere in these files"
    - "/privacy, /terms, and /articles continue to render on the light theme with correctly resolved (non-fallback) token colors"
  artifacts:
    - path: "src/app/privacy/privacy.module.css"
      provides: "Token-only color/spacing/type declarations"
    - path: "src/app/blog/[slug]/article.module.css"
      provides: "Token-only color/spacing/type declarations"
  key_links:
    - from: "src/app/privacy/privacy.module.css"
      to: "src/app/globals.css"
      via: "var(--ink-1), var(--ink-2), var(--border-1) custom property references"
      pattern: "var\\(--(surface|ink|border|fontSize|fontWeight|spacing)-?"
---

<objective>
Convert the seven legal-page and blog/article CSS modules from Phase 6's now-deleted legacy token aliases (`var(--foreground)`, `var(--muted)`, `var(--border)` — all defined with no fallback, meaning they currently resolve to nothing since these custom properties were removed from `globals.css`) and any raw hex colors to the canonical semantic-scale tokens.

Purpose: These pages already render correctly today by accident (the legacy alias references produce no declared value, so the browser falls back to inherited/initial styles) — Phase 8 must replace the broken references with real tokens so the pages are actually token-driven, not silently relying on CSS fallback behavior. `/privacy`, `/terms`, and `/articles` are also two of the five routes named explicitly in STYLE-04's route audit as "already light, keep as-is" — this plan ensures they stay light for the right reason (real tokens), not by accident.
Output: 7 files converted to 100% token-driven color/spacing/type values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-design-system-rollout/08-RESEARCH.md
@.planning/phases/08-design-system-rollout/08-PATTERNS.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert privacy.module.css, terms.module.css, refund.module.css, articles.module.css, and articles/[slug]/article.module.css to tokens</name>
  <files>src/app/privacy/privacy.module.css, src/app/terms/terms.module.css, src/app/refund/refund.module.css, src/app/articles/articles.module.css, src/app/articles/[slug]/article.module.css</files>
  <read_first>
    src/app/privacy/privacy.module.css (contains `var(--muted)` line 22, `var(--foreground)` line 59, `var(--border)` line 10)
    src/app/terms/terms.module.css (same pattern: `var(--muted)` line 22, `var(--foreground)` line 59, `var(--border)` line 10)
    src/app/refund/refund.module.css (same pattern: `var(--muted)` line 22, `var(--foreground)` line 59, `var(--border)` line 10)
    src/app/articles/articles.module.css (contains `var(--muted)` lines 18/31/58/91/105/138, `var(--foreground)` lines 76/122, `var(--border)` lines 10/46/107/124/139)
    src/app/articles/[slug]/article.module.css (contains `var(--muted)` lines 20/61/75, `var(--foreground)` lines 28/46/93/117/177, `var(--border)` lines 77/85/107/178)
    src/app/globals.css (token definitions, lines 3-58)
  </read_first>
  <action>
    Apply this exact deterministic replacement to all five files (privacy, terms, and refund share an identical structural pattern per RESEARCH.md):

    - `var(--foreground)` (used for `color:` — primary text) → `var(--ink-1)`
    - `var(--muted)` (used for `color:` — secondary/meta text) → `var(--ink-2)`
    - `var(--border)` used as `border-bottom: 1px solid var(--border)` / `border-top: 1px solid var(--border)` (hairline divider) → `var(--border-1)`
    - `var(--border)` used as `border: 1px solid var(--border)` (a full card/box outline, check `articles.module.css` line 107 and `article.module.css` line 77) → `var(--border-2)`
    - `var(--border)` used as `background: var(--border)` (articles/[slug]/article.module.css line 85 — a thin rule/separator bar rendered as a background fill) → `var(--border-2)`

    After the legacy-alias pass, check each of the five files for any remaining plain hex colors (grep result before this task showed only these legacy `var()` patterns, but re-verify with `grep -E "#[0-9a-fA-F]{3,8}" <file>` per file) and apply the property-context rule table from PATTERNS.md if any are found (background→surface, text→ink, border→border-1/2, box-shadow→shadow-N).

    Apply STYLE-05: any hardcoded `font-size`/`font-weight`/`padding`/`margin`/`gap` values map to the nearest token per the standard scale (`--fontSize-1..7`, `--fontWeight-normal/bold`, `--spacing-1..6`). Leave one-off layout constants (`max-width`) untouched.
  </action>
  <verify>
    <automated>grep -cE "var\(--(foreground|muted|border)\)|#[0-9a-fA-F]{3,8}" src/app/privacy/privacy.module.css src/app/terms/terms.module.css src/app/refund/refund.module.css src/app/articles/articles.module.css "src/app/articles/[slug]/article.module.css"</automated>
  </verify>
  <done>All five files return 0 from the grep above; `npm run build` succeeds; `/privacy`, `/terms`, and `/articles` still render on the light theme.</done>
</task>

<task type="auto">
  <name>Task 2: Convert blog/page.module.css and blog/[slug]/article.module.css to tokens</name>
  <files>src/app/blog/page.module.css, src/app/blog/[slug]/article.module.css</files>
  <read_first>
    src/app/blog/page.module.css (full file — 16 hex color occurrences per audit; not yet read in detail, read fully before editing)
    src/app/blog/[slug]/article.module.css (full file — 16 hex occurrences including `#d4d4d4` on lines 83/96)
    src/app/globals.css (token definitions, lines 3-58)
  </read_first>
  <action>
    Read both files in full. Apply the property-context rule table from PATTERNS.md to every hex color found: `background`/`background-color` set to a dark value (`#0a0a0a`, `#04080F`, near-black) → `var(--surface-1)`; set to a light gray → `var(--surface-2)`/`var(--surface-3)`. `color:` set to white/near-white (text-on-dark, since these blog pages may carry the same v1.0 dark-hero legacy as checkout/assessment — check each rule's surrounding context) → `var(--ink-1)`; set to a mid-gray like `#d4d4d4`/`#9ca3af`/`#6b7280` → `var(--ink-2)` or `var(--ink-3)` depending on emphasis (body/lead text → ink-2, captions/meta → ink-3). `border-color:` → `var(--border-1)`/`var(--border-2)`. `box-shadow:` rgba → `var(--shadow-1)`/`var(--shadow-2)`/`var(--shadow-3)`.

    If either file has a hero/header section using a dark background (`#0a0a0a`/`#04080F`) that contradicts the rest of the page being light, flip it to `var(--surface-1)`/`var(--ink-1)` consistent with the STYLE-04 pattern applied to checkout/assessment in Plan 08-02 — the ROADMAP's STYLE-04 route list only names `/articles`, `/privacy`, `/terms`, `/checkout`, `/assessment` explicitly, but the underlying principle (no route renders with a dark section contradicting its light body) applies site-wide per STYLE-03's collision-resolution intent. If you find such a section, convert it and note it in the plan's SUMMARY as an additional theme-unification fix beyond the five explicitly named routes.

    Apply STYLE-05 spacing/type-scale normalization identically to prior tasks.
  </action>
  <verify>
    <automated>grep -cE "#[0-9a-fA-F]{3,8}" src/app/blog/page.module.css "src/app/blog/[slug]/article.module.css"</automated>
  </verify>
  <done>Both files return 0 from the grep above; `npm run build` succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Pure CSS token substitution on static content pages; no new user input, no new external dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Information Disclosure (visual regression) | Legal-page/blog text becoming invisible if a legacy alias resolves to a color that happens to match its background after conversion | mitigate | `npm run verify:contrast` (Plan 07) validates every ink/surface pairing after conversion; legacy alias replacement targets are drawn from the already-verified Phase 6 contrast table, not guessed values. |
</threat_model>

<verification>
Run `npm run build` after both tasks — must exit 0. Run `grep -cE "var\(--(foreground|muted|border)\)|#[0-9a-fA-F]{3,8}" src/app/privacy/privacy.module.css src/app/terms/terms.module.css src/app/refund/refund.module.css src/app/articles/articles.module.css "src/app/articles/[slug]/article.module.css" src/app/blog/page.module.css "src/app/blog/[slug]/article.module.css"` — all seven files must return 0.
</verification>

<success_criteria>
- Zero hardcoded hex colors and zero legacy `--foreground`/`--muted`/`--border` alias references remain in the seven legal/blog/article CSS modules
- `npm run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/08-design-system-rollout/08-05-SUMMARY.md` when done
</output>
