---
phase: 08-design-system-rollout
plan: 07
type: execute
wave: 3
depends_on: ["08-02", "08-03", "08-04", "08-05", "08-06"]
files_modified: []
autonomous: false
requirements: [STYLE-02, STYLE-03, STYLE-04, STYLE-06]

must_haves:
  truths:
    - "grep -rE '#[0-9a-fA-F]{3,8}' src --include=*.css returns only token definitions in globals.css (and, as a documented exception, the /internal/palette-review comparison board which intentionally displays all three Phase 6 directions side-by-side)"
    - "npm run verify:contrast exits 0 with zero WCAG failures across every text/surface pairing site-wide"
    - "npm run build and npm run test both exit 0"
    - "A human has visually confirmed /articles, /privacy, /terms, /checkout, and /assessment all render on the single light theme with no dark-section flip"
  artifacts:
    - path: "scripts/verify-contrast.js"
      provides: "Automated WCAG contrast gate (unchanged from Phase 6, re-run here as the phase-closing check)"
  key_links:
    - from: "src (all *.css files)"
      to: "src/app/globals.css"
      via: "every color declaration resolves through a var(--token) reference, none through raw hex"
      pattern: "^(?!.*globals\\.css)(?!.*palette-review).*#[0-9a-fA-F]{3,8}"
---

<objective>
Close out Phase 8 with a sitewide sweep for any hardcoded-color stragglers Plans 08-02 through 08-06 missed, then run the full automated gate (build, contrast, test) plus a human visual check of the five routes named in STYLE-04, per the phase's success criteria.

Purpose: This is the phase's final gate — the ROADMAP's stated success criteria are grep-verifiable and script-verifiable, and this plan is where all of them get checked together for the first time (each Wave 2 plan only verified its own files in isolation).
Output: A clean sitewide hex-color grep, a passing contrast script, a passing build/test suite, and human confirmation of theme unity across the five named routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-design-system-rollout/08-RESEARCH.md
@.planning/phases/08-design-system-rollout/08-VALIDATION.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sitewide hex-color sweep — find and fix any straggler not covered by Plans 08-02 through 08-06</name>
  <files>Any *.css file under src that the sweep grep flags (none expected; fix inline if found)</files>
  <read_first>
    .planning/phases/08-design-system-rollout/08-RESEARCH.md (Hardcoded Color Audit section — full file inventory used to build Plans 08-02 through 08-06)
  </read_first>
  <action>
    Run `grep -rlE "#[0-9a-fA-F]{3,8}" src --include=*.css` and compare the result against exactly two expected survivors: `src/app/globals.css` (the token definitions themselves — expected and correct) and `src/app/internal/palette-review/styles.module.css` (the Phase 6 comparison board, which intentionally hardcodes all three palette directions' hex swatches for side-by-side visual comparison — this is a documented, deliberate exception, not a STYLE-02 violation, since converting it to tokens would defeat its purpose as a static multi-direction reference board).

    If any file OTHER than those two appears in the grep output, it is a straggler that Plans 08-02 through 08-06 missed. Read that file, classify each hex color's CSS property (background/color/border/box-shadow), and apply the same property-context rule table used throughout the phase (background→`var(--surface-N)` or `var(--accent)`; text color→`var(--ink-N)` or semantic; border→`var(--border-1)`/`var(--border-2)`; box-shadow→`var(--shadow-N)`) to convert it. Also re-run `grep -rn "var(--gold\|var(--bg-base\|var(--foreground\|var(--muted\|var(--border)" src --include="*.css"` to confirm zero legacy-alias survivors anywhere in the codebase (this should already be empty given Plans 08-02 through 08-06's coverage — if not empty, convert per the same mapping used in those plans: `--gold`→`--accent`, `--gold-light`→`--accent-hover`, `--bg-base`→`--surface-1`, `--foreground`→`--ink-1`, `--muted`→`--ink-2`, `--border`→`--border-1`).
  </action>
  <verify>
    <automated>grep -rlE "#[0-9a-fA-F]{3,8}" src --include=*.css | grep -v "globals.css" | grep -v "palette-review"</automated>
  </verify>
  <done>The verify grep returns empty output (zero files besides the two documented exceptions).</done>
</task>

<task type="auto">
  <name>Task 2: Run the full automated gate — build, contrast script, and test suite</name>
  <files>None (verification only)</files>
  <read_first>
    .planning/phases/08-design-system-rollout/08-VALIDATION.md (Sampling Rate and Per-Task Verification Map sections)
    scripts/verify-contrast.js
  </read_first>
  <action>
    Run, in order, and capture output for each: `npm run build`, `npm run verify:contrast`, `npm run test`. All three must exit 0. If `npm run verify:contrast` reports any failing pairing, that means either a Phase 6 token itself needs revisiting (out of scope for this plan — flag it, do not modify globals.css) or a component is applying an incorrect ink/surface combination introduced during Wave 2 conversion (in scope — find the offending CSS rule via the failing pairing's token names and fix the specific declaration to use a correctly-paired token). If `npm run test` fails on anything other than the contrast test file, investigate whether a Wave 2 conversion broke an unrelated existing test (e.g., a snapshot test asserting a specific class name or rendered value) and fix the regression, not the test.
  </action>
  <verify>
    <automated>npm run build && npm run verify:contrast && npm run test</automated>
  </verify>
  <done>All three commands exit 0 with no failures reported.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual confirmation of theme unity across the five STYLE-04 routes</name>
  <files>None (verification only)</files>
  <action>No automated action — this is the phase-closing human verification of theme unity across the five routes named in STYLE-04, after Tasks 1-2 confirm the automated gates (grep, build, contrast, test) all pass.</action>
  <verify>
    <automated>MISSING — theme-unity visual comparison has no automated equivalent; VALIDATION.md documents this as a Manual-Only Verification (no tool distinguishes "renders light" from "renders dark" without a screenshot-diff harness, which is out of scope for this phase)</automated>
  </verify>
  <done>Human types "approved" confirming all five routes render on the single light theme with no dark-section flip.</done>
  <what-built>Five routes (`/articles`, `/privacy`, `/terms`, `/checkout`, `/assessment`) have been converted to run entirely on Phase 6 light-theme tokens, including the checkout and assessment hero sections which previously rendered dark (`#0a0a0a`) against an otherwise light page.</what-built>
  <how-to-verify>
    Start the dev server (`npm run dev`) and visit each of the following in a browser, confirming NONE of them show a dark section anywhere on the page (no black/near-black hero, no white-on-dark text block) — every section should be on the warm cream/off-white Phase 6 surface with dark ink text:
    1. `/articles` — article listing page
    2. `/privacy` — privacy policy page
    3. `/terms` — terms of service page
    4. `/checkout` — the hero at the top of the page (below the site nav) should now be light cream, not black
    5. `/assessment` — the hero at the top of the page should now be light cream, not black

    Also spot-check that gold/accent-colored elements (buttons, links, highlights) look the same warm gold across all five pages plus the homepage `/` and `/services` — no page should show a visibly different gold shade from another.
  </how-to-verify>
  <resume-signal>Type "approved" once all five routes render on the single light theme, or describe which route(s) still show a dark section or color mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Verification-only plan; no code changes beyond fixing stragglers found by the sweep. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Information Disclosure (silent visual regression shipping to production) | Entire Phase 8 rollout | mitigate | This plan is the explicit phase-closing gate combining automated (build/contrast/test/grep) and human (5-route visual) verification before Phase 8 is considered complete; nothing ships past this plan without both passing. |
</threat_model>

<verification>
`npm run build`, `npm run verify:contrast`, and `npm run test` all exit 0. `grep -rlE "#[0-9a-fA-F]{3,8}" src --include=*.css | grep -v "globals.css" | grep -v "palette-review"` returns empty. Human checkpoint approved.
</verification>

<success_criteria>
- `grep -rE "#[0-9a-fA-F]{3,8}" src --include=*.css` returns only `globals.css` token definitions (plus the documented `/internal/palette-review` exception) — STYLE-02
- Two golds and two dark surfaces are confirmed collapsed to one accent / one surface token sitewide — STYLE-03
- All five named routes render on a single light theme, confirmed by human checkpoint — STYLE-04
- `npm run verify:contrast` passes with zero failures — STYLE-06
</success_criteria>

<output>
Create `.planning/phases/08-design-system-rollout/08-07-SUMMARY.md` when done
</output>
