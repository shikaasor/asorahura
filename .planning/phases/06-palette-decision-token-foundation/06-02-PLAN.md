---
phase: 06-palette-decision-token-foundation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true
requirements: [DESIGN-03, DESIGN-04, DESIGN-05, DESIGN-06]

must_haves:
  truths:
    - "globals.css defines the full semantic-scale token set (color, type, spacing, radius, shadow, transition) selected as Direction B, with zero legacy `--bg-*`/`--gold-*`/`--text-*` names or aliases remaining"
    - "Playfair Display is no longer imported, loaded, or referenced anywhere in src/"
    - "Heading font-weight (700) and letter-spacing (-0.02em) are unchanged from before this plan"
    - "`npm run verify:contrast` exits 0 against the new Direction B tokens"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Direction B semantic-scale token :root block; no legacy aliases; no .serif class; no h1-h4 font-family override"
      contains: "--surface-1"
    - path: "src/app/layout.tsx"
      provides: "Root layout importing only Inter (no Playfair_Display)"
  key_links:
    - from: "src/app/layout.tsx"
      to: "next/font/google"
      via: "import { Inter } from"
      pattern: "import \\{ Inter \\} from \"next/font/google\""
    - from: "src/app/globals.css body rule"
      to: "--surface-1 / --ink-1 tokens"
      via: "var()"
      pattern: "background: var\\(--surface-1\\)"
---

<objective>
Replace the dark-luxury `:root` token block in `globals.css` with the full Direction B light-first semantic-scale token system (DESIGN-03/04/05), and resolve the Playfair Display question by dropping it entirely (DESIGN-06): remove the font loading from `layout.tsx`, delete the `.serif` utility class and the redundant h1-h4 sans override, while preserving heading weight/tracking exactly as-is (D-13).

Purpose: This is the core deliverable of Phase 6 — the actual token system Phase 7 builds `/automate` against. Per D-07 (accepted risk), removing the legacy token names will break `var()` references across the other 35 stylesheets until Phase 8; this is intentional and no compatibility shim is added.
Output: `globals.css` with the new token block; `layout.tsx` with Playfair Display removed; `tests/test-token-naming.test.ts` (from Plan 06-01) now GREEN; `npm run verify:contrast` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-palette-decision-token-foundation/06-CONTEXT.md
@.planning/phases/06-palette-decision-token-foundation/06-UI-SPEC.md
@.planning/phases/06-palette-decision-token-foundation/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the :root token block in globals.css with Direction B semantic-scale tokens</name>
  <files>src/app/globals.css</files>
  <read_first>src/app/globals.css, .planning/phases/06-palette-decision-token-foundation/06-UI-SPEC.md</read_first>
  <action>
    Replace lines 3–38 of `src/app/globals.css` (the entire current `:root` block, including the "Legacy aliases kept for compatibility" section) with a new `:root` block containing exactly these tokens, using Direction B's values from 06-UI-SPEC.md:

    Color — `--surface-1: #FDFAF4`, `--surface-2: #F9F4ED`, `--surface-3: #F2EBDE`, `--surface-4: #FFFEF9`, `--ink-1: #1F1B17`, `--ink-2: #5D564E`, `--ink-3: #9A9087`, `--accent: #C9A86D`, `--accent-hover: #B5985B`, `--accent-active: #A1854A`, `--success: #3D6B1F`, `--error: #AA3918`, `--warn: #9B5F1A`, `--border-1: rgba(31, 27, 23, 0.08)`, `--border-2: rgba(31, 27, 23, 0.16)`.

    Type — `--fontSize-1: 12px` through `--fontSize-7: 48px` (steps: 12, 16, 18, 20, 24, 32, 48), `--fontWeight-normal: 400`, `--fontWeight-bold: 700`, `--lineHeight-tight: 1.1`, `--lineHeight-normal: 1.5`, `--lineHeight-relaxed: 1.6`. Use numbered steps for `--fontSize-*` per D-08 ("numbered steps, not role-based names") — this deliberately diverges from 06-RESEARCH.md's `--fontSize-xs/sm/base/lg/xl/2xl/3xl` example naming, which conflicts with D-08's explicit instruction; D-08 is the locked decision and wins. `--fontWeight-*` and `--lineHeight-*` remain role-named (like `--accent`/`--success`) since they are not step-progression scales.

    Spacing — `--spacing-1: 4px`, `--spacing-2: 8px`, `--spacing-3: 16px`, `--spacing-4: 24px`, `--spacing-5: 32px`, `--spacing-6: 48px` (numbered per D-08, diverging from RESEARCH.md's xs/sm/md/lg/xl/2xl naming for the same reason as fontSize above).

    Radius — `--radius-1: 4px`, `--radius-2: 8px`, `--radius-3: 12px`.

    Shadow — `--shadow-1: 0 1px 2px rgba(31, 27, 23, 0.05)`, `--shadow-2: 0 4px 8px rgba(31, 27, 23, 0.1)`, `--shadow-3: 0 12px 24px rgba(31, 27, 23, 0.12)`.

    Transition — `--duration-1: 150ms`, `--duration-2: 300ms`, `--duration-3: 500ms`.

    Font stack — keep `--font-sans: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif` (unchanged value). Do NOT define `--font-serif`. Update `--selection` from `rgba(201, 160, 96, 0.25)` to `rgba(201, 168, 109, 0.25)` (matches new `--accent` hue).

    Do NOT add `--background`, `--foreground`, `--muted`, `--border`, or any legacy alias — D-06 removes these entirely, no compatibility bridge.

    Update the `::selection` rule's `color:` from `var(--text-primary)` to `var(--ink-1)`. Update the `body` rule: `background: var(--bg-base)` → `background: var(--surface-1)`; `color: var(--text-primary)` → `color: var(--ink-1)`; keep `font-family: var(--font-sans)` unchanged.

    On the `h1, h2, h3, h4` rule (currently `font-family: var(--font-sans); font-weight: 700; letter-spacing: -0.02em;`), delete only the `font-family: var(--font-sans);` line — it is redundant now that there is no serif to override (D-12). Keep `font-weight: 700;` and `letter-spacing: -0.02em;` exactly as-is (D-13).

    Delete the `.serif { font-family: var(--font-serif); }` rule entirely (confirmed unused in src/ per CONTEXT.md and PATTERNS.md).

    Leave everything else in the file unchanged: `*`, `html`, the img/media comment, `a`, `a:hover`, `button`, `.container`, `.section-padding`, `@keyframes shimmer`, `@keyframes flow`, `.animate-flow`, `.nav-offset`, and the two `@media` blocks.
  </action>
  <acceptance_criteria>
    - `grep -c "^  --" src/app/globals.css` shows the new token count (color 15 + type 7+2+3 + spacing 6 + radius 3 + shadow 3 + duration 3 + font-sans 1 + selection 1 = 44 custom properties)
    - `grep -E "bg-base|gold|text-primary|text-secondary|text-muted|font-serif" src/app/globals.css` returns nothing
    - `grep -n "font-family: var(--font-sans);" src/app/globals.css` shows exactly one match (the `body` rule), not two (h1-h4 no longer has it)
    - `grep -n "\.serif" src/app/globals.css` returns nothing
    - `npx vitest run tests/test-token-naming.test.ts` exits 0 (now GREEN)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/test-token-naming.test.ts</automated>
  </verify>
  <done>globals.css :root block contains only the new Direction B semantic-scale tokens; no legacy names/aliases remain; h1-h4 override keeps weight/tracking only; .serif class deleted; token-naming test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Playfair Display font loading from layout.tsx</name>
  <files>src/app/layout.tsx</files>
  <read_first>src/app/layout.tsx</read_first>
  <action>
    Change the import on line 2 from `import { Inter, Playfair_Display } from "next/font/google";` to `import { Inter } from "next/font/google";`. Delete the `const playfair = Playfair_Display({...})` declaration (lines 9–15). Change the `<body className={...}>` on line 55 from `` `${inter.variable} ${playfair.variable}` `` to just `inter.variable`. Leave the `inter` const, `viewport`, `metadata`, and the rest of `RootLayout` (ParticleWave, Navigation, nav-offset div, Footer) untouched — this task only removes Playfair.
  </action>
  <acceptance_criteria>
    - `grep -n "Playfair" src/app/layout.tsx` returns nothing
    - `grep -n "playfair" src/app/layout.tsx` returns nothing
    - `grep -n "className={inter.variable}" src/app/layout.tsx` shows one match
    - `grep -r "Playfair" src/` (repo-wide) returns nothing
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:contrast</automated>
  </verify>
  <done>layout.tsx imports and loads only Inter; body className is inter.variable only; repo-wide grep for "Playfair" is empty; contrast script passes against the new tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| globals.css :root → 35 other stylesheets | Removing legacy token names breaks `var()` resolution in files outside this phase's scope; accepted per D-07, not a security boundary but a documented availability/rendering gap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Denial of Service (visual/rendering, not security-critical) | 35 legacy stylesheets referencing removed `--bg-*`/`--gold-*`/`--text-*` tokens | accept | D-07 explicitly accepts this breakage until Phase 8 rolls out the new tokens site-wide; no compatibility shim added by design |
| T-06-04 | Tampering | Font loading (Playfair Display removal) | accept | No security implication — dead code removal only; verified via repo-wide grep that no references remain |
</threat_model>

<verification>
1. `npx vitest run tests/test-token-naming.test.ts` exits 0.
2. `npm run verify:contrast` exits 0.
3. `grep -r "Playfair\|font-serif\|\.serif" src/` returns nothing.
</verification>

<success_criteria>
- globals.css contains the full Direction B semantic-scale token set with no legacy names/aliases.
- Playfair Display is fully removed (import, font loading, CSS variable, utility class, redundant override).
- Contrast script and token-naming test both pass against the new tokens.
</success_criteria>

<output>
Create `.planning/phases/06-palette-decision-token-foundation/06-02-SUMMARY.md` when done
</output>
