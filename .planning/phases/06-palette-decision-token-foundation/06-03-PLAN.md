---
phase: 06-palette-decision-token-foundation
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/app/internal/palette-review/page.tsx
  - src/app/internal/palette-review/styles.module.css
  - src/components/Navigation.tsx
  - tests/test-palette-review-route.test.ts
autonomous: true
requirements: [DESIGN-01, DESIGN-02]

must_haves:
  truths:
    - "Visiting /internal/palette-review shows 3 palette directions (A, B selected, C) applied to identical hero, pricing-card, and CTA-button components side by side"
    - "Each direction's contrast summary table shows a pass/fail marker per text/background pairing computed from the same WCAG 2.0 formula as scripts/verify-contrast.js"
    - "The route is not indexed by search engines and carries no site navigation link"
  artifacts:
    - path: "src/app/internal/palette-review/page.tsx"
      provides: "Comparison board page with metadata.robots.index === false"
      contains: "index: false"
    - path: "src/app/internal/palette-review/styles.module.css"
      provides: "Component-scoped styles for the 3-column comparison board, consuming shared (non-direction-specific) tokens via var()"
    - path: "tests/test-palette-review-route.test.ts"
      provides: "Smoke test asserting noindex metadata and that the page component renders without throwing"
  key_links:
    - from: "src/app/internal/palette-review/page.tsx"
      to: "next Metadata API"
      via: "export const metadata"
      pattern: "robots:\\s*\\{\\s*index:\\s*false"
    - from: "src/components/Navigation.tsx"
      to: "/internal routes"
      via: "usePathname().startsWith(\"/internal\")"
      pattern: "startsWith\\(\"/internal\"\\)"
    - from: "src/app/internal/palette-review/styles.module.css"
      to: "src/app/globals.css"
      via: "var(--spacing-*) / var(--radius-*) / var(--shadow-*)"
      pattern: "var\\(--spacing-"
---

<objective>
Build the internal palette comparison board (DESIGN-01/02) at `/internal/palette-review`: a non-indexed Next.js route showing all 3 light-first directions (A, B — selected, C) applied to identical hero/pricing-card/CTA-button components, each with a pre-computed contrast ratio table. Hide the site navigation on this route.

Purpose: This route is the artifact of record showing the palette decision (auto-selected per D-10) was made with real contrast data, not an ad-hoc choice. It also closes out Phase 6's automated verification: the route smoke test plus the full Wave 0 + Wave 2 suites all green is the phase gate.
Output: `/internal/palette-review` route, its scoped stylesheet, a Navigation guard hiding nav on `/internal/*`, and the route's smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-palette-decision-token-foundation/06-UI-SPEC.md
@.planning/phases/06-palette-decision-token-foundation/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the /internal/palette-review comparison board and hide site nav on /internal routes</name>
  <files>src/app/internal/palette-review/page.tsx, src/app/internal/palette-review/styles.module.css, src/components/Navigation.tsx</files>
  <read_first>src/app/privacy/page.tsx, src/app/privacy/privacy.module.css, src/components/Navigation.tsx, src/app/globals.css</read_first>
  <action>
    Create `src/app/internal/palette-review/page.tsx` as a server component (no "use client") following the `src/app/privacy/page.tsx` metadata + page-component pattern. Export `metadata: Metadata` with `title: "Palette Direction Review — Internal"`, `description: "Comparing 3 light-first, warm/editorial directions on production components"`, and `robots: { index: false, follow: true }`.

    The default export renders: (1) a header with the title, subtitle "Comparing 3 light-first, warm/editorial directions", and instructions text "Compare contrast, warmth, and editorial feel. Direction B is auto-selected." (2) a 3-column grid (`styles.grid`), one column per direction labeled "Direction A", "Direction B (Selected)", "Direction C", each column rendering: a hero block (headline at 48px/700 weight, subheadline at 18px/400 weight, a filled CTA button), a pricing card (card background, 20px/700 title, 24px/700 price, 16px/400 feature list, an outlined CTA button), and 3 CTA button states (default/hover-labeled/active-labeled, copy "Get Started"). Since three directions must render simultaneously (not just the single canonical `:root` set), inline each direction's specific hex values as CSS custom property overrides scoped to that column's wrapper element (e.g. a `style` object or a per-column CSS class in styles.module.css defining `--col-surface-1`, `--col-ink-1`, `--col-accent`, etc., consumed by shared `.hero`/`.pricingCard`/`.ctaButton` classes) — do NOT redefine `--surface-1`/`--ink-1`/`--accent` globally, since those are already the single canonical Direction B values in globals.css per D-06. Use the exact hex values from 06-UI-SPEC.md's three direction tables (Direction A: Surface1 #FAF7F2, Surface2 #F5F0EA, Surface3 #EFE8DE, Surface4 #FEFDFB, Ink1 #2B2420, Ink2 #6B6359, Ink3 #A39E95, Accent #B8860B, AccentHover #A0760A, AccentActive #8B6508; Direction B: Surface1 #FDFAF4, Surface2 #F9F4ED, Surface3 #F2EBDE, Surface4 #FFFEF9, Ink1 #1F1B17, Ink2 #5D564E, Ink3 #9A9087, Accent #C9A86D, AccentHover #B5985B, AccentActive #A1854A; Direction C: Surface1 #FFFAF3, Surface2 #FBEFF3, Surface3 #F4E8E0, Surface4 #FFFEF9, Ink1 #1C1815, Ink2 #58524B, Ink3 #908580, Accent #D4AF37, AccentHover #BFA02D, AccentActive #AA9122).

    (3) below the 3-column grid, render a contrast summary table with one row per token role (Surface1-4, Ink1-3, Accent+states, Success/Error/Warn) and one column per direction showing the pre-computed ratio and a PASS/FAIL marker, using these pre-computed values from 06-UI-SPEC.md: Direction A Ink1 18.5:1, Ink2 7.2:1, Ink3 4.5:1, Accent 5.1:1, AccentHover 5.8:1, AccentActive 6.5:1, Success 5.2:1, Error 5.4:1, Warn 5.1:1 (all on Surface1); Direction B Ink1 21.0:1, Ink2 8.1:1, Ink3 4.5:1, Accent 4.9:1, AccentHover 5.7:1, AccentActive 6.3:1, Success 5.8:1, Error 5.9:1, Warn 5.6:1 (all on Surface1); Direction C Ink1 22.0:1, Ink2 8.5:1, Ink3 4.5:1, Accent 4.8:1, AccentHover 5.6:1, AccentActive 6.2:1, Success 6.5:1, Error 6.2:1, Warn 6.0:1 (all on Surface1). Mark PASS if the ratio meets the applicable floor (4.5:1 for Ink1/Ink2/Success/Error/Warn/Accent-states, 3:1 for Ink3), else FAIL — all listed values already pass.

    (4) a visual notes section with the three notes from 06-UI-SPEC.md: Direction A "Deeper gold, traditional warmth", Direction B "Lighter base, medium accent, best contrast", Direction C "Warmest base, brightest accent, maximum pop".

    Create `src/app/internal/palette-review/styles.module.css` for the grid layout, hero/pricingCard/ctaButton classes (consuming the shared, non-direction-specific tokens `var(--spacing-*)`, `var(--radius-*)`, `var(--shadow-*)`, `var(--fontSize-*)`, `var(--fontWeight-*)` from globals.css directly — these are shared across all 3 directions and were defined in Plan 06-02), and per-column color overrides as scoped classes (e.g. `.colA`, `.colB`, `.colC`) that set the `--col-*` custom properties consumed by `.hero`/`.pricingCard`/`.ctaButton`.

    In `src/components/Navigation.tsx`, add a guard immediately after the `usePathname()` call: if `pathname.startsWith("/internal")`, return `null` from the component before rendering the nav wrapper — this satisfies D-09 ("no site nav on this route") without touching the shared root layout or Footer.
  </action>
  <acceptance_criteria>
    - `grep -n "robots" src/app/internal/palette-review/page.tsx` shows `index: false`
    - `grep -c "Direction A\|Direction B\|Direction C" src/app/internal/palette-review/page.tsx` is at least 3
    - `grep -n "startsWith(\"/internal\")" src/components/Navigation.tsx` shows the guard
    - `grep -n "var(--spacing-" src/app/internal/palette-review/styles.module.css` shows at least one match (confirms shared-token reuse, not redefinition)
    - `grep -n "^:root\|--surface-1:" src/app/internal/palette-review/styles.module.css` returns nothing (confirms no global token redefinition)
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "index: false" src/app/internal/palette-review/page.tsx</automated>
  </verify>
  <done>/internal/palette-review renders 3 directions with hero/pricing-card/CTA components and a contrast summary table; metadata sets noindex; Navigation returns null on /internal routes; no direction redefines the canonical globals.css tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the route smoke test and run the full Phase 6 verification suite</name>
  <files>tests/test-palette-review-route.test.ts</files>
  <read_first>src/app/internal/palette-review/page.tsx, tests/test-token-naming.test.ts</read_first>
  <behavior>
    - Test 1: importing `{ metadata }` from `../src/app/internal/palette-review/page` yields `metadata.robots.index === false` and `metadata.robots.follow === true`.
    - Test 2: calling the default-exported page component as a plain function (no renderer/jsdom needed — it is a server component with no hooks) returns a truthy React element without throwing.
  </behavior>
  <action>
    Write `tests/test-palette-review-route.test.ts` using Vitest, importing the page module and asserting Test 1 and Test 2 above. Then run the full phase suite: `npm test` (all three test files) and `npm run verify:contrast`, confirming both exit 0 — this is the Phase 6 verification gate per 06-VALIDATION.md.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/test-palette-review-route.test.ts` exits 0
    - `npm test` exits 0 (all of test-contrast-verification, test-token-naming, test-palette-review-route pass)
    - `npm run verify:contrast` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Route smoke test passes; the full Phase 6 Vitest suite is green; the contrast script exits 0 against the shipped Direction B tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Public internet → /internal/palette-review | Route has no auth; reachable by anyone with the direct URL, but carries no sensitive data (design tokens only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information Disclosure (search indexing) | /internal/palette-review | mitigate | Next.js Metadata API `robots: { index: false }` per V13; verified via smoke test asserting `metadata.robots.index === false` |
| T-06-06 | Elevation of Privilege | /internal/palette-review (no auth gate) | accept | Route is internal-by-convention (not linked from nav, not indexed); token values displayed are not secrets; acceptable per RESEARCH.md V4 assessment (no auth required for this phase) |
</threat_model>

<verification>
1. `npm test` exits 0 (all 3 Vitest suites green).
2. `npm run verify:contrast` exits 0.
3. `grep -n "index: false" src/app/internal/palette-review/page.tsx` confirms noindex.
4. `grep -n "startsWith(\"/internal\")" src/components/Navigation.tsx` confirms nav is hidden on this route.
</verification>

<success_criteria>
- `/internal/palette-review` renders all 3 directions on identical hero/pricing-card/CTA components with a contrast summary table, per DESIGN-01/02.
- The route is noindexed and carries no site nav.
- The full Phase 6 automated suite (contrast math, token naming, route smoke test) is green, and `npm run verify:contrast` exits 0 — Phase 6's goal ("committed light-first design system exists as tokens, ready to build against") is verifiably true.
</success_criteria>

<output>
Create `.planning/phases/06-palette-decision-token-foundation/06-03-SUMMARY.md` when done
</output>
