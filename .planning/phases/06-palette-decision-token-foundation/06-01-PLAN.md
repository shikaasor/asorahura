---
phase: 06-palette-decision-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - scripts/verify-contrast.js
  - tests/test-contrast-verification.test.js
  - tests/test-token-naming.test.ts
autonomous: false
requirements: [DESIGN-07]

must_haves:
  truths:
    - "Running `npm run verify:contrast` reports a pass/fail line for every ink/surface/semantic color pairing and exits non-zero if any pairing fails the WCAG floor"
    - "The contrast script never prints the globals.css file path or raw file content on error — only token names and computed ratios"
    - "`npm test` runs the Vitest suite once and exits (no watch mode)"
  artifacts:
    - path: "scripts/verify-contrast.js"
      provides: "WCAG 2.0 contrast verification, CommonJS module exporting getRelativeLuminance/getContrastRatio/parseTokens/checkContrast plus a CLI entry point"
      contains: "module.exports"
    - path: "vitest.config.ts"
      provides: "Vitest project config, node test environment, tests/ include glob"
    - path: "tests/test-contrast-verification.test.js"
      provides: "Unit tests for luminance/contrast math against known WCAG reference pairs"
    - path: "tests/test-token-naming.test.ts"
      provides: "Scaffold test asserting globals.css uses semantic-scale token names (expected to fail/RED until Plan 06-02 lands)"
  key_links:
    - from: "scripts/verify-contrast.js"
      to: "src/app/globals.css"
      via: "fs.readFileSync + postcss.parse(':root')"
      pattern: "readFileSync.*globals\\.css"
    - from: "package.json scripts.verify:contrast"
      to: "scripts/verify-contrast.js"
      via: "npm script"
      pattern: "verify:contrast.*verify-contrast\\.js"
---

<objective>
Establish the Wave 0 test/verification infrastructure for Phase 6: install Vitest (with a blocking legitimacy checkpoint, since it is not in RESEARCH.md's Package Legitimacy Audit table), install the already-audited `postcss` + `parse-css-color` packages, build the WCAG contrast-verification script (DESIGN-07) as a pure-function module with a CLI entry point, and scaffold the two Wave-0 test files that later plans in this phase depend on for automated verification.

Purpose: No test framework exists in this repo (confirmed via package.json). Per 06-VALIDATION.md, Wave 0 must install a framework and create the contrast-math tests and token-naming scaffold test before token/route work can be verified automatically.
Output: `scripts/verify-contrast.js` (CLI + testable exports), `vitest.config.ts`, `tests/test-contrast-verification.test.js` (GREEN), `tests/test-token-naming.test.ts` (RED until 06-02), updated `package.json` scripts/devDependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-palette-decision-token-foundation/06-CONTEXT.md
@.planning/phases/06-palette-decision-token-foundation/06-RESEARCH.md
@.planning/phases/06-palette-decision-token-foundation/06-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Verify vitest package legitimacy before install</name>
  <files>package.json</files>
  <action>No automated action — this is a human legitimacy sign-off gate before `npm install --save-dev vitest` runs. `vitest` does not appear in 06-RESEARCH.md's Package Legitimacy Audit table (only `postcss` and `parse-css-color` were audited there), so per the planner's package-legitimacy gate it is treated as [ASSUMED] and requires explicit human approval before install — never auto-approvable.</action>
  <verify>Human confirms vitest's npm/GitHub legitimacy per the how-to-verify checklist below before Task 1 proceeds</verify>
  <done>User types "approved" (or names an alternative test framework) — no package is installed until this checkpoint is resolved</done>
  <what-built>Nothing yet — this checkpoint gates the Wave 0 test-framework install. `vitest` does not appear in 06-RESEARCH.md's Package Legitimacy Audit table (only `postcss` and `parse-css-color` were audited there), so per the planner's package-legitimacy gate it is treated as [ASSUMED] and requires human sign-off before `npm install` runs.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/vitest and confirm: publisher is the vitest-dev org, weekly downloads are in the millions, latest version is actively maintained.
    2. Confirm the source repo is https://github.com/vitest-dev/vitest (verified during planning: version 4.1.10, created 2021-12-03, matches npm registry).
    3. Confirm no typosquat risk (package name is exactly `vitest`, not `vite-test` or similar).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `npm install --save-dev vitest`, or name a different test framework to use instead.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install test framework and contrast-script dependencies</name>
  <files>package.json, package-lock.json, vitest.config.ts</files>
  <read_first>package.json, tsconfig.json</read_first>
  <action>
    Run `npm install --save-dev vitest postcss parse-css-color` (only after Task 0 approval). Add to package.json `scripts`: `"test": "vitest run"` (single run, no watch flag — Nyquist forbids watch-mode) and `"verify:contrast": "node scripts/verify-contrast.js"`. Create `vitest.config.ts` at the project root using `defineConfig` from `vitest/config` with `test.environment` set to `"node"` (no jsdom needed — no component rendering occurs in this phase's tests) and `test.include` set to `["tests/**/*.test.{js,ts}"]`. Do not add `@testing-library/react` or `jsdom` — not needed for Phase 6's test scope.
  </action>
  <acceptance_criteria>
    - `package.json` "devDependencies" contains "vitest", "postcss", "parse-css-color"
    - `package.json` "scripts" contains `"test": "vitest run"` and `"verify:contrast": "node scripts/verify-contrast.js"`
    - `vitest.config.ts` exists and exports a config with `test.environment: "node"` and `test.include` matching `tests/**/*.test.{js,ts}`
    - `npx vitest --version` prints a version number (confirms binary installed)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest --version</automated>
  </verify>
  <done>vitest, postcss, and parse-css-color are installed as devDependencies; package.json has test and verify:contrast scripts; vitest.config.ts exists and is valid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the contrast verification script and Wave 0 test scaffolds</name>
  <files>scripts/verify-contrast.js, tests/test-contrast-verification.test.js, tests/test-token-naming.test.ts</files>
  <read_first>src/app/globals.css, .planning/phases/06-palette-decision-token-foundation/06-RESEARCH.md, .planning/phases/06-palette-decision-token-foundation/06-PATTERNS.md</read_first>
  <behavior>
    - Test 1 (`getRelativeLuminance`): white (255,255,255) and black (0,0,0) plugged into `getContrastRatio` of their luminances must yield exactly 21 (±0.01). Black-on-white and white-on-black are symmetric (both 21).
    - Test 2 (`getContrastRatio`): mid-gray rgb(136,136,136) — i.e. `#888888` — against white (255,255,255) yields approximately 6.3 (assert within 6.2–6.4).
    - Test 3 (`parseTokens`): given a synthetic CSS string containing a `:root { --surface-1: #FDFAF4; --ink-1: #1F1B17; }` block, returns an object `{ "--surface-1": "#FDFAF4", "--ink-1": "#1F1B17" }` (parsed via PostCSS, not regex).
    - Test 4 (`checkContrast`): given synthetic tokens where an ink/surface pair has a contrast ratio below 4.5, `checkContrast` returns a `failures` array containing an entry that names the token pair and the computed ratio (not the file path or file content).
    - Test 5 (error handling / V7): when `scripts/verify-contrast.js` is required and its internal file-read helper is called against a nonexistent path, the thrown/caught error message must NOT contain the literal file path string — only a generic message.
    - Test 6 (`tests/test-token-naming.test.ts`, scaffold — expected RED until Plan 06-02): reads `src/app/globals.css` and asserts regex matches exist for `--surface-1` through `--surface-4`, `--ink-1` through `--ink-3`, `--accent`, `--accent-hover`, `--accent-active`, `--success`, `--error`, `--warn`, `--border-1`, `--border-2`, `--fontSize-1` through `--fontSize-7`, `--spacing-1` through `--spacing-6`, `--radius-1` through `--radius-3`, `--shadow-1` through `--shadow-3`, `--duration-1` through `--duration-3`; and asserts absence of `--bg-base`, `--gold`, `--text-primary`, `--text-secondary`, `--text-muted`, `--font-serif`, and the bare legacy aliases `--background:`, `--foreground:`, `--muted:`. This test is written now and MUST fail against the current (still-dark) globals.css — that is expected; Plan 06-02 makes it pass.
  </behavior>
  <action>
    Write `scripts/verify-contrast.js` as a CommonJS module. Export named functions `getRelativeLuminance(r, g, b)`, `getContrastRatio(L1, L2)`, `parseTokens(cssString)` (uses `postcss.parse(cssString)` and `walkRules(':root')` + `walkDecls` to build a `{ [customPropName]: value }` map — do not use regex line-matching per RESEARCH.md's Pitfall 3/"Don't Hand-Roll" guidance), and `checkContrast(tokens)` (uses `parse-css-color` to normalize each `--ink-*`/`--surface-*`/`--success`/`--error`/`--warn` token to RGB, computes luminance and ratio for every ink×surface pair plus every semantic×surface pair, applies the floor: `--ink-3` and large text require ≥3:1, all other ink/semantic pairings require ≥4.5:1, returns `{ failures: string[] }` where each failure string is exactly `"${tokenA} on ${tokenB}: ${ratio.toFixed(2)}:1 (required ${min}:1)"` — no file paths or CSS content in these strings).
    Guard the file-read step in a try/catch: on read failure, throw `new Error("Unable to read design tokens file")` (generic — no path interpolation) per the V7 threat mitigation.
    Add a CLI entry point guarded by `if (require.main === module) { ... }` that reads `src/app/globals.css` from a hardcoded relative path, calls `parseTokens` then `checkContrast`, prints each failure with `console.error`, prints `"✓ All text/surface pairings pass WCAG AA"` and exits 0 on success, or exits 1 if `failures.length > 0`.
    Write `tests/test-contrast-verification.test.js` covering Test 1–5 above, importing functions via `require('../scripts/verify-contrast.js')`.
    Write `tests/test-token-naming.test.ts` covering Test 6 above, reading `src/app/globals.css` via `fs.readFileSync`.
  </action>
  <acceptance_criteria>
    - `scripts/verify-contrast.js` exports `getRelativeLuminance`, `getContrastRatio`, `parseTokens`, `checkContrast` via `module.exports`
    - `npx vitest run tests/test-contrast-verification.test.js` exits 0 (all assertions pass)
    - `npx vitest run tests/test-token-naming.test.ts` exits non-zero (expected RED — old dark tokens still present; this is correct at this stage)
    - `grep -n "readFileSync" scripts/verify-contrast.js` shows the read wrapped in a try/catch whose catch throws a message that does not contain the string `"globals.css"` or a full path
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/test-contrast-verification.test.js</automated>
  </verify>
  <done>verify-contrast.js exports pure functions plus a CLI; contrast-math unit tests are green; token-naming scaffold test exists and is (expectedly) red pending Plan 06-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Build/CI process → filesystem | `scripts/verify-contrast.js` reads `src/app/globals.css` from a hardcoded local path; no user input reaches this script |
| npm registry → devDependencies | New package (`vitest`) added to the dependency tree; supply-chain risk on install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | scripts/verify-contrast.js error handling | mitigate | File-read failures throw a generic "Unable to read design tokens file" message; no path or CSS content is interpolated into logged output (V7) |
| T-06-02 | Information Disclosure | scripts/verify-contrast.js success/failure output | mitigate | Reported failure strings contain only token names and numeric ratios, never raw CSS declarations |
| T-06-SC | Tampering (supply chain) | npm install of vitest, postcss, parse-css-color | mitigate | postcss/parse-css-color pre-audited in 06-RESEARCH.md (Approved); vitest gated behind Task 0 blocking human-verify checkpoint per package-legitimacy gate — never auto-approvable |
</threat_model>

<verification>
1. `npx vitest --version` prints a version.
2. `npx vitest run tests/test-contrast-verification.test.js` exits 0.
3. `npx vitest run tests/test-token-naming.test.ts` exits non-zero (expected at this stage — documents RED state for Plan 06-02 to turn GREEN).
4. `grep -n "module.exports" scripts/verify-contrast.js` shows the four named exports.
</verification>

<success_criteria>
- Vitest, postcss, and parse-css-color installed as devDependencies after explicit human sign-off on vitest's legitimacy.
- `scripts/verify-contrast.js` is a working, testable CommonJS module with a CLI entry point that will gate CI/CD (DESIGN-07).
- Wave 0 test scaffolds exist per 06-VALIDATION.md, with the contrast-math suite green and the token-naming suite documented as red pending Plan 06-02.
</success_criteria>

<output>
Create `.planning/phases/06-palette-decision-token-foundation/06-01-SUMMARY.md` when done
</output>
