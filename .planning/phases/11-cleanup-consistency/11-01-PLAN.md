---
phase: 11-cleanup-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-polish-redirects.test.ts
  - src/components/home/Footer.tsx
  - src/components/checkout/TrustBadges.tsx
  - src/app/checkout/page.tsx
  - src/app/services/page.tsx
  - src/app/automate/instagram/success/page.tsx
  - src/components/assessment/RevenueResultsScreen.tsx
  - src/components/automate/BuildMapForm.tsx
  - src/components/checkout/OrderSummary.tsx
autonomous: true
requirements: [POLISH-01, POLISH-02, POLISH-03]

must_haves:
  truths:
    - "Old Reel comments/links to /flowmorph and /automate/success still resolve to their current pages, and the two in-page redirects (/articles, /assessment/deep) are regression-tested"
    - "The footer displays the current year, computed at render time via new Date().getFullYear(), not a hardcoded literal"
    - "Every mixed emoji/unicode checkmark, lock, and trophy glyph in the purchase-funnel UI (TrustBadges, checkout, services, automate success, assessment results, Build Map form, order summary) renders as a lucide-react icon component"
  artifacts:
    - path: "tests/test-polish-redirects.test.ts"
      provides: "Regression coverage for all 4 retired/moved routes (2 next.config.mjs redirects + 2 in-page redirect() calls)"
    - path: "src/components/home/Footer.tsx"
      provides: "Dynamic copyright year"
    - path: "src/components/checkout/TrustBadges.tsx"
      provides: "lucide-react Trophy/Lock/CheckCircle icons instead of emoji"
  key_links:
    - from: "src/components/home/Footer.tsx"
      to: "rendered copyright text"
      via: "new Date().getFullYear() called in component body"
      pattern: "new Date\\(\\)\\.getFullYear\\(\\)"
    - from: "src/components/checkout/TrustBadges.tsx"
      to: "lucide-react"
      via: "named icon imports"
      pattern: "from \"lucide-react\""
---

<objective>
Close out POLISH-01 (redirect regression coverage), POLISH-02 (dynamic footer year), and POLISH-03 (replace the mixed emoji/glyph icon set across the purchase funnel with lucide-react components).

Purpose: Old Reel comments and shared links must keep resolving after routes moved across Phases 4/9.1/10; the footer copyright must not go stale annually; and the funnel's icon usage must be consistent, accessible, and themeable instead of raw emoji/unicode glyphs.

Output: `tests/test-polish-redirects.test.ts` (new), dynamic year in `Footer.tsx`, lucide-react icons in `TrustBadges.tsx`, `checkout/page.tsx`, `services/page.tsx`, `automate/instagram/success/page.tsx`, `RevenueResultsScreen.tsx`, `BuildMapForm.tsx`, `OrderSummary.tsx`.

Scope note (POLISH-03): 11-PATTERNS.md flagged 9 files containing glyph characters; this plan covers the 7 purchase-funnel files above. `src/lib/articles.ts` is excluded — its glyphs are testimonial/article copy content, not UI icon markup. `src/app/unsubscribe/page.tsx` is excluded — it's outside the purchase funnel and uses its own inline dark-theme styling; out of scope for this phase, tracked as follow-up debt rather than silently dropped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cleanup-consistency/11-RESEARCH.md
@.planning/phases/11-cleanup-consistency/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redirect regression test + dynamic footer year</name>
  <files>tests/test-polish-redirects.test.ts, src/components/home/Footer.tsx</files>
  <read_first>
    next.config.mjs (existing redirects() array — 2 entries: /flowmorph→/, /automate/success→/automate/instagram/success, both permanent:true)
    tests/test-automate-instagram-route.test.ts (full analog — import pattern for nextConfig, getRedirects() helper, readFileSync + path.resolve string-match pattern for hardcoded-reference tests)
    src/app/articles/page.tsx (redirect('/blog') — 1 line)
    src/app/assessment/deep/page.tsx (redirect("/assessment?depth=deep") — 1 line)
    src/components/home/Footer.tsx (line 63-65 hardcoded "&copy; 2026 Asor Ahura. All rights reserved."; component has no "use client" directive, no hooks)
  </read_first>
  <action>
    Create tests/test-polish-redirects.test.ts following the exact structure of tests/test-automate-instagram-route.test.ts: import { describe, it, expect } from "vitest"; import { readFileSync } from "node:fs"; import path from "node:path"; import nextConfig from "../next.config.mjs". Define an async getRedirects() helper identical to the analog. Write a describe("next.config.mjs redirects()") block with two `it` cases using expect(redirects).toContainEqual(expect.objectContaining({...})) — one for {source: "/flowmorph", destination: "/", permanent: true}, one for {source: "/automate/success", destination: "/automate/instagram/success", permanent: true}. Write a second describe("in-page redirects") block with two `it` cases using readFileSync(path.resolve(__dirname, "../src/app/articles/page.tsx"), "utf-8") asserting the contents contain "redirect('/blog')", and readFileSync(path.resolve(__dirname, "../src/app/assessment/deep/page.tsx"), "utf-8") asserting contents contain 'redirect("/assessment?depth=deep")'. Do not add new entries to next.config.mjs — both retired routes not covered by next.config.mjs already redirect via in-page redirect() calls; this task only adds regression coverage.

    In src/components/home/Footer.tsx, add `const year = new Date().getFullYear();` as the first statement inside the Footer function body (before the return), and replace the JSX text `&copy; 2026 Asor Ahura. All rights reserved.` with `&copy; {year} Asor Ahura. All rights reserved.` No import changes needed — this is a server component with zero hooks.
  </action>
  <acceptance_criteria>
    - `npm test -- tests/test-polish-redirects.test.ts` passes with 4 green assertions
    - `grep -c "new Date().getFullYear()" src/components/home/Footer.tsx` returns 1
    - `grep -c "2026 Asor Ahura" src/components/home/Footer.tsx` returns 0
    - `grep -c "{year} Asor Ahura" src/components/home/Footer.tsx` returns 1
  </acceptance_criteria>
  <done>New test file exists and passes; Footer.tsx computes the year dynamically with no remaining hardcoded year literal.</done>
</task>

<task type="auto">
  <name>Task 2: Icon migration — core purchase funnel (TrustBadges, checkout, services)</name>
  <files>src/components/checkout/TrustBadges.tsx, src/app/checkout/page.tsx, src/app/services/page.tsx</files>
  <read_first>
    src/components/checkout/TrustBadges.tsx (full file — 22 lines, badges array with icon: "🏆"/"🔒"/"✅" strings)
    src/app/checkout/page.tsx (line 94 — `<span className={styles.deliverableCheck}>✓</span>` inside the deliverables .map)
    src/app/services/page.tsx (line 115 — `<span className={styles.bulletCheck}>✓</span>` inside the bullets .map)
    src/components/home/Footer.tsx (lines 2, 55-56 — lucide-react import/usage convention: `import { Linkedin } from "lucide-react";` then `<Linkedin size={18} />`)
  </read_first>
  <action>
    In TrustBadges.tsx: add `import { Trophy, Lock, CheckCircle } from "lucide-react";` at the top. Change the badges array from `{ label, icon: "🏆" }` shape to `{ label, Icon: Trophy }` shape (Oracle Certified → Trophy, Secure Payment via Paddle → Lock, 100% IP Ownership → CheckCircle). In the JSX `.map`, replace `<span>{b.icon}</span>` with `<b.Icon size={18} aria-hidden="true" />`.

    In checkout/page.tsx: add `import { Check } from "lucide-react";` to the import block. Replace `<span className={styles.deliverableCheck}>✓</span>` (line 94) with `<Check size={14} className={styles.deliverableCheck} aria-hidden="true" />`.

    In services/page.tsx: add `import { Check } from "lucide-react";` to the import block. Replace `<span className={styles.bulletCheck}>✓</span>` (line 115) with `<Check size={14} className={styles.bulletCheck} aria-hidden="true" />`.

    Do not touch `.deliverableCheck` / `.bulletCheck` CSS class definitions in checkout.module.css / services.module.css — they already define color/font-size that will still apply via className on the icon's `<svg>` element (lucide-react forwards className to the root svg).
  </action>
  <acceptance_criteria>
    - `grep -c "🏆\|🔒\|✅" src/components/checkout/TrustBadges.tsx` returns 0
    - `grep -c "from \"lucide-react\"" src/components/checkout/TrustBadges.tsx` returns 1
    - `grep -c ">✓<" src/app/checkout/page.tsx` returns 0 and `grep -c "from \"lucide-react\"" src/app/checkout/page.tsx` returns 1
    - `grep -c ">✓<" src/app/services/page.tsx` returns 0 and `grep -c "from \"lucide-react\"" src/app/services/page.tsx` returns 1
    - `npm run build` completes without type errors
  </acceptance_criteria>
  <done>TrustBadges, checkout page, and services page render lucide-react icons in place of emoji/unicode glyphs; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Icon migration — secondary flows (automate success, assessment results, Build Map form, order summary)</name>
  <files>src/app/automate/instagram/success/page.tsx, src/components/assessment/RevenueResultsScreen.tsx, src/components/automate/BuildMapForm.tsx, src/components/checkout/OrderSummary.tsx</files>
  <read_first>
    src/app/automate/instagram/success/page.tsx (line 148 — `✓ We&apos;ll be in touch soon. Check your email for next steps.` inside `<p className={styles.success}>`)
    src/components/assessment/RevenueResultsScreen.tsx (line 110 — `<span className={styles.emailCheck}>✓</span>` inside `.emailBanner`)
    src/components/automate/BuildMapForm.tsx (line 67 — `{isEmailValid && <span className={styles.checkmark}>✓</span>}`; line 79 — `<p className={styles.success}>✓ Check your inbox for your download link</p>`)
    src/components/checkout/OrderSummary.tsx (full file, 34 lines — line 18 `<span className="text-green-500 shrink-0">✓</span>` inside the deliverables `.map`; this component is currently unused/dead per grep but fixed for consistency since it lives in the checkout icon set — do not remove the file, only its glyph)
  </read_first>
  <action>
    In each file, add `import { Check } from "lucide-react";` to the top-level imports (React import already present via JSX runtime, no other changes to imports needed).

    automate/instagram/success/page.tsx line 148: replace the leading `✓ ` glyph in the JSX text with `<Check size={16} aria-hidden="true" /> ` immediately before the text node, keeping the rest of the sentence unchanged: `We&apos;ll be in touch soon. Check your email for next steps.`

    RevenueResultsScreen.tsx line 110: replace `<span className={styles.emailCheck}>✓</span>` with `<Check size={18} className={styles.emailCheck} aria-hidden="true" />`.

    BuildMapForm.tsx line 67: replace `<span className={styles.checkmark}>✓</span>` with `<Check size={16} className={styles.checkmark} aria-hidden="true" />`. Line 79: replace the leading `✓ ` in `<p className={styles.success}>✓ Check your inbox for your download link</p>` with `<Check size={16} aria-hidden="true" /> ` before the text, keeping `Check your inbox for your download link` as the trailing text node.

    OrderSummary.tsx line 18: replace `<span className="text-green-500 shrink-0">✓</span>` with `<Check size={16} className="text-green-500 shrink-0" aria-hidden="true" />`.
  </action>
  <acceptance_criteria>
    - `grep -c "✓" src/app/automate/instagram/success/page.tsx src/components/assessment/RevenueResultsScreen.tsx src/components/automate/BuildMapForm.tsx src/components/checkout/OrderSummary.tsx` returns 0 for each file
    - `grep -c "from \"lucide-react\"" src/app/automate/instagram/success/page.tsx src/components/assessment/RevenueResultsScreen.tsx src/components/automate/BuildMapForm.tsx src/components/checkout/OrderSummary.tsx` returns 1 for each file
    - `npm run build` completes without type errors
    - `npm test -- tests/` passes (no regression in existing suite)
  </acceptance_criteria>
  <done>All 4 secondary-flow files render lucide-react Check icons; no unicode checkmark glyphs remain anywhere in the purchase funnel (excluding src/lib/articles.ts, which is testimonial copy content, not a UI icon).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → Next.js server (redirects) | Old/shared URLs cross this boundary; redirect config determines destination |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | next.config.mjs redirects() | accept | Redirect entries already use exact string `source` matches (no wildcards); Task 1 only adds regression tests, does not modify the redirect rules themselves — open-redirect risk from over-broad patterns does not apply |
| T-11-02 | Tampering | Icon/copy changes (Tasks 2-3) | accept | Presentation-only changes (glyph → SVG icon component); no new input surface, no new dependency (lucide-react already installed) |

No new external package installs in this plan; lucide-react is an existing dependency per package.json. Package Legitimacy Gate not triggered.
</threat_model>

<verification>
Run `npm test -- tests/` (full suite) and `npm run build` after all 3 tasks complete. Both must be green before this plan is marked done.
</verification>

<success_criteria>
- tests/test-polish-redirects.test.ts exists and passes, covering all 4 retired/moved routes
- Footer.tsx renders the current year via new Date().getFullYear(), zero hardcoded year literals remain in Footer.tsx
- Zero emoji/unicode UI-icon glyphs (🏆 🔒 ✅ ✓) remain in TrustBadges.tsx, checkout/page.tsx, services/page.tsx, automate/instagram/success/page.tsx, RevenueResultsScreen.tsx, BuildMapForm.tsx, OrderSummary.tsx
- `npm run build` and `npm test -- tests/` both pass
</success_criteria>

<output>
Create `.planning/phases/11-cleanup-consistency/11-01-SUMMARY.md` when done
</output>
