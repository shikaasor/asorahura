---
phase: 11-cleanup-consistency
plan: 04
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - package.json
  - .lhcirc.json
autonomous: false
requirements: [POLISH-06]

must_haves:
  truths:
    - "Lighthouse can be run locally on demand against / and /automate via a single npm script, and reports scores for Performance, Accessibility, Best Practices, and SEO against agreed thresholds"
  artifacts:
    - path: ".lhcirc.json"
      provides: "Lighthouse CI config targeting http://localhost:3000/ and /automate with assertion thresholds"
    - path: "package.json"
      provides: "npm run lighthouse script"
      contains: "\"lighthouse\": \"lhci autorun\""
---

<objective>
Close out POLISH-06: install `@lhci/cli`, configure it to audit `/` and `/automate` against agreed thresholds, and run it to establish a baseline that a human reviews and accepts (or flags for follow-up).

Purpose: No Lighthouse tooling exists in this repo today. RESEARCH.md's Package Legitimacy Audit already approved `@lhci/cli` (official Google Chrome project, 4 yrs old, 100k+/week downloads, no ASSUMED/SUS/SLOP verdict) — install proceeds without a legitimacy checkpoint. This plan runs last (wave 3, depends on 11-02/11-03) so the audit reflects the accessibility and responsive fixes already landed, not the pre-cleanup baseline.

Output: `.lhcirc.json`, `npm run lighthouse` script, and a human-reviewed baseline run against `/` and `/automate`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cleanup-consistency/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @lhci/cli and configure Lighthouse CI</name>
  <files>package.json, .lhcirc.json</files>
  <read_first>
    package.json (existing "scripts" block — dev/build/start/lint/test/verify:contrast; existing "devDependencies" — vitest, typescript, eslint, postcss, parse-css-color)
  </read_first>
  <action>
    Run `npm install --save-dev @lhci/cli` to add it to devDependencies (approved per RESEARCH.md Package Legitimacy Audit — no checkpoint required).

    Create .lhcirc.json at the repo root with this exact shape: `ci.collect.url` = ["http://localhost:3000/", "http://localhost:3000/automate"], `ci.collect.startServerCommand` = "npm run start", `ci.collect.numberOfRuns` = 1, `ci.assert.preset` = "lighthouse:recommended", `ci.assert.assertions` = { "categories:performance": ["error", { "minScore": 0.90 }], "categories:accessibility": ["error", { "minScore": 0.95 }], "categories:best-practices": ["error", { "minScore": 0.90 }], "categories:seo": ["error", { "minScore": 1.0 }] }, `ci.upload.target` = "temporary-public-storage". Use numberOfRuns: 1 (not the RESEARCH example's 3) to keep local runs fast; this can be raised later if score variance becomes a problem.

    Add `"lighthouse": "lhci autorun"` to the "scripts" block in package.json, alongside the existing "verify:contrast" entry.
  </action>
  <acceptance_criteria>
    - `grep -c "@lhci/cli" package.json` returns 1 (in devDependencies)
    - `grep -c "\"lighthouse\": \"lhci autorun\"" package.json` returns 1
    - `.lhcirc.json` exists at repo root and is valid JSON (`node -e "JSON.parse(require('fs').readFileSync('.lhcirc.json'))"` exits 0)
    - `.lhcirc.json` contains both "http://localhost:3000/" and "http://localhost:3000/automate" in its url array
  </acceptance_criteria>
  <done>@lhci/cli is installed, .lhcirc.json targets / and /automate with the agreed thresholds, and `npm run lighthouse` is wired up.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Lighthouse CI tooling configured to audit / and /automate against Performance ≥90, Accessibility ≥95, Best Practices ≥90, SEO ≥100.</what-built>
  <how-to-verify>
    1. Run `npm run build` to produce a production build
    2. Run `npm run lighthouse` — this starts the production server via `npm run start` and audits both URLs once each
    3. Review the printed pass/fail table for each category on each URL
    4. If all four categories pass on both URLs at the stated thresholds, this checkpoint is approved as-is
    5. If any category fails, decide: (a) accept the current score as the agreed threshold for that category/route and note the deviation, or (b) flag specific fixes needed as a follow-up (do not silently lower the threshold in .lhcirc.json)
  </how-to-verify>
  <resume-signal>Type "approved" (all thresholds met or explicitly accepted) or list the specific category + route + score that needs follow-up work</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry → devDependencies | @lhci/cli is a new dev-only dependency pulled from npm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-SC | Tampering | npm install @lhci/cli | mitigate | Already vetted in 11-RESEARCH.md Package Legitimacy Audit (official Google Chrome project, github.com/GoogleChrome/lighthouse-ci, 4 yrs old, 100k+/week downloads, slopcheck [OK], disposition Approved) — dev-only dependency, never ships to production bundle, no blocking checkpoint required per the gate protocol (only [ASSUMED]/[SUS] packages require one) |
</threat_model>

<verification>
Run `npm run build && npm run lighthouse` and review the checkpoint output. Checkpoint task must receive explicit human approval (thresholds met or deviations explicitly accepted) before this plan is considered complete.
</verification>

<success_criteria>
- @lhci/cli installed as a devDependency
- .lhcirc.json audits / and /automate with Performance ≥90 / Accessibility ≥95 / Best Practices ≥90 / SEO ≥100 thresholds
- `npm run lighthouse` runs successfully end-to-end
- Human checkpoint approved: thresholds met, or deviations explicitly accepted and documented
</success_criteria>

<output>
Create `.planning/phases/11-cleanup-consistency/11-04-SUMMARY.md` when done
</output>
