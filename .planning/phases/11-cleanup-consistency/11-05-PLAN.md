---
phase: 11-cleanup-consistency
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [POLISH-07]

must_haves:
  truths:
    - "A developer reading README.md can find every token category (color, type, spacing, radius, shadow, duration) with its actual current values, plus the workflow for adding/modifying tokens and running the contrast verification script — without re-deriving it from globals.css"
  artifacts:
    - path: "README.md"
      provides: "Root-level documentation including a Design System & Tokens section"
      contains: "## Design System"
---

<objective>
Close out POLISH-07: create the repo's root README.md with a "Design System & Tokens" section documenting the token system defined in `src/app/globals.css`, so the light-first design system does not erode again after this milestone ships.

Purpose: No root README.md exists today. The design token system (color, type, spacing, radius, shadow, duration scales) is the single most important shared contract in the codebase — Phase 6 established it, Phase 8 rolled it out everywhere, and Phase 11 is the last chance to document it before the milestone closes.

Output: `README.md` at the repo root with a Design System & Tokens section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-cleanup-consistency/11-RESEARCH.md
@.planning/phases/06-palette-decision-token-foundation/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md with Design System & Tokens documentation</name>
  <files>README.md</files>
  <read_first>
    src/app/globals.css (lines 3-59 — the full :root token block, source of truth for exact names/values: --surface-1 through --surface-4, --ink-1/2/3, --accent/--accent-hover/--accent-active, --success/--error/--warn, --border-1/2, --fontSize-1 through --fontSize-7, --fontWeight-normal/bold, --lineHeight-tight/normal/relaxed, --spacing-1 through --spacing-6, --radius-1/2/3, --shadow-1/2/3, --duration-1/2/3)
    package.json (line 11 — "verify:contrast": "node scripts/verify-contrast.js")
    .planning/phases/11-cleanup-consistency/11-RESEARCH.md (lines 603-763 — the full "Design Token System Documentation (for README.md)" section, pre-authored and spot-checked against the actual globals.css values by 11-PATTERNS.md)
  </read_first>
  <action>
    Create README.md at the repo root. Start with a short project header (project name "asorahura", one-line description "Marketing site and self-serve automation product for small business owners" — no need to invent more than this, the README's substance is the Design System section). Then include the "## Design System & Tokens" section using the structure from 11-RESEARCH.md lines 609-763 verbatim: Overview, Token Structure (Color Tokens: Surfaces/Text/Accent/Semantic/Borders with their exact hex values from globals.css; Type Tokens: fontSize-1 through fontSize-7 with px values, fontWeight, lineHeight; Spacing Tokens: spacing-1 through spacing-6; Radius/Shadow/Duration tokens), Usage (CSS var() example), Adding New Tokens (5-step workflow), Modifying Existing Tokens (6-step workflow), Token Verification Script (npm run verify:contrast description), and References (link to `.planning/phases/06-palette-decision-token-foundation/06-RESEARCH.md`, Tailwind responsive docs, WCAG 2.0 contrast docs).

    Use the exact token values confirmed in globals.css: --surface-1: #FDFAF4, --accent: #C9A86D, --spacing-1: 4px through --spacing-6: 48px, --fontSize-1: 12px through --fontSize-7: 48px. Do not invent values not present in globals.css.
  </action>
  <acceptance_criteria>
    - `test -f README.md` succeeds
    - `grep -c "## Design System" README.md` returns 1 or more
    - `grep -c "verify:contrast" README.md` returns 1 or more
    - `grep -c -- "--surface-1" README.md` returns 1 or more
    - `grep -c -- "--accent" README.md` returns 1 or more
    - `npm run verify:contrast` still exits 0 (sanity check that the referenced script is real and passing)
  </acceptance_criteria>
  <done>README.md exists at the repo root with a Design System & Tokens section documenting every token category with real values, the usage pattern, the add/modify workflows, and the verify:contrast script.</done>
</task>

</tasks>

<threat_model>
No new trust boundary or input surface. This plan creates a documentation file only — no runtime code, no dependency, no user-controllable input. Threat surface: negligible — stated briefly per phase security policy rather than omitted.
</threat_model>

<verification>
`test -f README.md` and the grep assertions above; `npm run verify:contrast` must still pass (regression sanity check, unrelated to the README content itself but confirms the script this doc references is real).
</verification>

<success_criteria>
- README.md exists at the repo root
- Contains a "## Design System & Tokens" section documenting color/type/spacing/radius/shadow/duration tokens with real values from globals.css
- Documents the verify:contrast workflow and links to the Phase 6 RESEARCH.md deep-dive
</success_criteria>

<output>
Create `.planning/phases/11-cleanup-consistency/11-05-SUMMARY.md` when done
</output>
