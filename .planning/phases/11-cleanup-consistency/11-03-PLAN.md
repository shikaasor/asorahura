---
phase: 11-cleanup-consistency
plan: 03
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - src/app/globals.css
autonomous: false
requirements: [POLISH-05]

must_haves:
  truths:
    - "Every tested route renders without horizontal scroll or broken layout at 360, 768, 1024, and 1440px viewport widths"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Explicit 1024px and 1440px breakpoint rules for .container, closing the gap between the existing 768px/480px rules and the desktop default"
      contains: "max-width: 1024px"
---

<objective>
Close out POLISH-05: add the missing 1024px and 1440px breakpoint CSS rules, then verify all 5 representative routes render cleanly at all 4 required widths (360, 768, 1024, 1440).

Purpose: globals.css currently only has `@media (max-width: 768px)` and `@media (max-width: 480px)` rules for `.container`. There is no dedicated rule for 1024px (small laptop/iPad landscape) or 1440px (desktop), which RESEARCH.md and VALIDATION.md both flag as a gap. 360px is already covered by the 480px rule (max-width query fires at any width ≤480, including 360).

Output: New `.container` breakpoint rules at 1024px and 1440px in globals.css; human-verified responsive pass across 5 routes × 4 widths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cleanup-consistency/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 1024px and 1440px breakpoint rules</name>
  <files>src/app/globals.css</files>
  <read_first>
    src/app/globals.css (lines 115-119 `.container { max-width: 1300px; margin: 0 auto; padding: 0 4rem; }`; lines 146-164 existing `@media (max-width: 768px)` and `@media (max-width: 480px)` blocks, both only adjusting `.container` padding and `.section-padding`/`.nav-offset`)
  </read_first>
  <action>
    Append two new media query blocks to the end of src/app/globals.css, after the existing `@media (max-width: 480px)` block:

    `@media (max-width: 1024px) { .container { padding: 0 2rem; } }` — tightens horizontal padding for tablet/small-laptop widths between the 768px rule and the 1300px desktop default.

    `@media (min-width: 1440px) { .container { max-width: 1400px; } }` — widens the container max-width slightly for large desktop viewports.

    Do not modify the existing 768px/480px blocks or any component-level CSS modules; this task only adds the two missing top-level breakpoint rules identified in 11-RESEARCH.md's Responsive Design Specification.
  </action>
  <acceptance_criteria>
    - `grep -c "max-width: 1024px" src/app/globals.css` returns 1
    - `grep -c "min-width: 1440px" src/app/globals.css` returns 1
    - `npm run build` completes without CSS errors
  </acceptance_criteria>
  <done>globals.css defines explicit .container behavior at all 4 required breakpoints (360 via the 480px rule, 768, 1024, 1440).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Explicit 1024px and 1440px .container breakpoint rules, closing the CSS gap between 768px and the desktop default.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/ in Chrome DevTools responsive mode
    2. For each of these 5 routes — `/`, `/automate`, `/automate/instagram`, `/assessment`, `/blog` — set the viewport to each of 360×800, 768×1024, 1024×768, and 1440×900 and confirm:
       - No horizontal scrollbar appears
       - Text remains readable, no overlapping content
       - Buttons/links are touchable (roughly 44px minimum height on mobile)
       - Navigation collapses/reflows appropriately at 360/768 and shows full nav at 1024/1440
       - Images scale without distortion or overflow
    3. Note any route/width combination with a layout break
  </how-to-verify>
  <resume-signal>Type "approved" or list the specific route + width where layout breaks</resume-signal>
</task>

</tasks>

<threat_model>
No new trust boundary or input surface. This plan only adds CSS media query rules to an existing stylesheet — no new dependency, no user-controllable input, no data flow change. Threat surface: negligible — stated briefly per phase security policy rather than omitted.
</threat_model>

<verification>
Run `npm run build` after Task 1. Checkpoint task must receive explicit human approval (or a documented, accepted list of minor issues) before this plan is considered complete.
</verification>

<success_criteria>
- globals.css has explicit .container rules at 1024px and 1440px in addition to the existing 768px/480px rules
- Human checkpoint approved: no horizontal scroll or broken layout across /, /automate, /automate/instagram, /assessment, /blog at 360, 768, 1024, 1440px
</success_criteria>

<output>
Create `.planning/phases/11-cleanup-consistency/11-03-SUMMARY.md` when done
</output>
