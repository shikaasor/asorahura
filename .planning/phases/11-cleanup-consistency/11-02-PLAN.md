---
phase: 11-cleanup-consistency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/automate/OfferingCard.tsx
  - src/app/automate/page.tsx
  - src/app/automate/page.module.css
  - src/components/blog/BlogListingClient.tsx
  - src/app/blog/[slug]/page.tsx
  - src/components/PhoneMockup.tsx
  - src/components/AboutSection.tsx
  - src/components/HeroSection.tsx
  - src/components/Navigation.tsx
  - src/components/TestimonialCard.tsx
autonomous: false
requirements: [POLISH-04]

must_haves:
  truths:
    - "Keyboard users see a visible focus ring (2px accent outline) on every link, button, input, select, and textarea site-wide"
    - "No page has a heading level skip (e.g. h1 directly to h3 with no h2 in between)"
    - "A human can Tab through the site's interactive elements in a logical order and operate every control without a mouse"
    - "The 7 images flagged in 11-RESEARCH.md as missing alt text (BlogListingClient.tsx, blog/[slug]/page.tsx, PhoneMockup.tsx, AboutSection.tsx, HeroSection.tsx, Navigation.tsx, TestimonialCard.tsx) are confirmed to have non-empty alt attributes"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Site-wide :focus-visible rule for a/button/input/select/textarea"
      contains: ":focus-visible"
  key_links:
    - from: "src/app/globals.css"
      to: "interactive elements site-wide"
      via: ":focus-visible pseudo-class outline using var(--accent)"
      pattern: ":focus-visible"
---

<objective>
Close out POLISH-04 in full: add a site-wide `:focus-visible` style so keyboard users can see which element has focus, fix the one confirmed heading-order violation (h1 → h3 skip on the `/automate` catalog page), and verify with an executable check — not just cited claim — that the 7 images 11-RESEARCH.md flagged as missing alt text actually have it (11-PATTERNS.md claims this is already fixed; Task 3 proves it rather than assuming it).

Purpose: Screen reader and keyboard-only users currently have no visible focus indicator anywhere on the site (only 4 component-local `:focus-visible` rules exist, none in globals.css), and the `/automate` catalog page jumps from `<h1>` straight to `<h3>` (both the offering cards and the FAQ), which breaks screen-reader heading navigation.

Output: Global `:focus-visible` CSS rule; `OfferingCard.tsx` and `/automate` FAQ headings promoted to `<h2>`; human-verified keyboard navigation pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cleanup-consistency/11-RESEARCH.md
@.planning/phases/11-cleanup-consistency/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Site-wide :focus-visible rule</name>
  <files>src/app/globals.css</files>
  <read_first>
    src/app/globals.css (lines 3-59 :root token block — `--accent: #C9A86D`; lines 90-113 existing global element rules for h1-h4/a/button, no :focus-visible rule present anywhere in this file)
    src/components/assessment/EmailGate.module.css (lines 51-53 — existing local `.input:focus { border-color: var(--accent); }` pattern, not yet `:focus-visible`, shows the token already used for interaction state)
  </read_first>
  <action>
    Add the following block to src/app/globals.css immediately after the existing `button { background: none; border: none; cursor: pointer; font-family: inherit; }` rule (around line 113) and before the `.container` class rule:

    a:focus-visible, button:focus-visible, input:focus-visible, select:focus-visible, textarea:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }

    Use the existing `--accent` token (`#C9A86D`) — do not hardcode a hex value. Do not add a `:focus:not(:focus-visible)` suppression rule; none of the current per-component focus styles remove the default outline, so no conflicting override exists to clean up.
  </action>
  <acceptance_criteria>
    - `grep -c "focus-visible" src/app/globals.css` returns 1 or more
    - `grep -c "outline: 2px solid var(--accent)" src/app/globals.css` returns 1
    - `npm run build` completes without CSS/type errors
  </acceptance_criteria>
  <done>globals.css contains a single site-wide :focus-visible rule covering a/button/input/select/textarea using the --accent token.</done>
</task>

<task type="auto">
  <name>Task 2: Fix heading-order skip on /automate catalog page</name>
  <files>src/components/automate/OfferingCard.tsx, src/app/automate/page.tsx, src/app/automate/page.module.css</files>
  <read_first>
    src/app/automate/page.tsx (full file — h1 "Automate Everything Automatable" at line 39, CatalogGrid renders next, then `<h3>{faq.question}</h3>` at line 54 inside `.faq`, then `<h2 className={styles.closingHeading}>Ready to start?</h2>` at line 62)
    src/components/automate/CatalogGrid.tsx (renders `<OfferingCard>` per offering, no heading of its own)
    src/components/automate/OfferingCard.tsx (line 20 — `<h3 className={styles.title}>{offering.name}</h3>`, styled by class not tag)
    src/app/automate/page.module.css (line 35 — `.faq h3 { font-size: var(--fontSize-4); font-weight: var(--fontWeight-bold); color: var(--ink-1); margin-bottom: var(--spacing-1); }` — this is an element selector tied to the `h3` tag, must be updated in lockstep)
  </read_first>
  <action>
    Change src/components/automate/OfferingCard.tsx line 20 from `<h3 className={styles.title}>{offering.name}</h3>` to `<h2 className={styles.title}>{offering.name}</h2>`. Styling is class-based (`.title`), no CSS module change needed for this file.

    Change src/app/automate/page.tsx line 54 from `<h3>{faq.question}</h3>` to `<h2>{faq.question}</h2>`.

    Change src/app/automate/page.module.css line 35 selector from `.faq h3 {` to `.faq h2 {` (keep the declaration block contents unchanged) so the FAQ question styling still applies to the renamed tag.

    Resulting order on /automate: h1 (hero) → h2 (each OfferingCard title, ×5) → h2 (each FAQ question, ×3) → h2 (closing "Ready to start?") — no skipped levels.
  </action>
  <acceptance_criteria>
    - `grep -c "<h3 className={styles.title}>" src/components/automate/OfferingCard.tsx` returns 0
    - `grep -c "<h2 className={styles.title}>" src/components/automate/OfferingCard.tsx` returns 1
    - `grep -c "<h3>{faq.question}</h3>" src/app/automate/page.tsx` returns 0
    - `grep -c "<h2>{faq.question}</h2>" src/app/automate/page.tsx` returns 1
    - `grep -c "^.faq h3" src/app/automate/page.module.css` returns 0
    - `grep -c "^.faq h2" src/app/automate/page.module.css` returns 1
    - `npm run build` completes without type errors
  </acceptance_criteria>
  <done>No h1→h3 skip remains on /automate; FAQ question styling still renders correctly under the new h2 selector.</done>
</task>

<task type="auto">
  <name>Task 3: Verify alt text is present on the 7 flagged images</name>
  <files>src/components/blog/BlogListingClient.tsx, src/app/blog/[slug]/page.tsx, src/components/PhoneMockup.tsx, src/components/AboutSection.tsx, src/components/HeroSection.tsx, src/components/Navigation.tsx, src/components/TestimonialCard.tsx</files>
  <read_first>
    11-RESEARCH.md (lines 329-336 — originally flagged these 7 files as missing `alt` on `<Image>`/`<img>` elements)
    11-PATTERNS.md (line ~306 — claims re-inspection found all 7 already have alt attributes, with cited line numbers and alt values)
  </read_first>
  <action>
    Run: `grep -n "<Image\|<img" src/components/blog/BlogListingClient.tsx src/app/blog/\[slug\]/page.tsx src/components/PhoneMockup.tsx src/components/AboutSection.tsx src/components/HeroSection.tsx src/components/Navigation.tsx src/components/TestimonialCard.tsx`

    For every `<Image` or `<img` match returned, confirm the same element (same line or the immediately following attribute lines) includes an `alt="..."` attribute with non-empty content (not `alt=""` unless the image is explicitly decorative). If any match lacks a non-empty `alt`, add a concise descriptive `alt` attribute to that element using the surrounding content/context to write the description — do not leave any `<Image>`/`<img>` without alt text.
  </action>
  <acceptance_criteria>
    - `grep -n "<Image\|<img" src/components/blog/BlogListingClient.tsx src/app/blog/\[slug\]/page.tsx src/components/PhoneMockup.tsx src/components/AboutSection.tsx src/components/HeroSection.tsx src/components/Navigation.tsx src/components/TestimonialCard.tsx | grep -vc "alt="` returns 0 (every Image/img match line-region has an alt attribute)
    - `npm run build` completes without type errors
  </acceptance_criteria>
  <done>All `<Image>`/`<img>` usages across the 7 originally-flagged files have a non-empty alt attribute, confirmed by grep rather than by citation alone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Site-wide keyboard focus indicator (:focus-visible outline using the accent token), the /automate heading-order fix, and verified alt text coverage on the 7 previously-flagged images.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/
    2. Press Tab repeatedly through the homepage, navigation, and footer — confirm a visible gold outline (2px, offset) appears around every link/button/input as it receives focus, and no element is skipped or unreachable by keyboard
    3. Repeat the Tab check on /automate, /automate/instagram, /checkout, /assessment, /blog, /services, /engage
    4. Open browser DevTools Accessibility pane (or an extension like "HeadingsMap") on /automate and confirm the heading order reads h1 → h2 (offering cards) → h2 (FAQ) → h2 (closing), with no h3 appearing before an h2
    5. Confirm every interactive element (buttons, links, form fields) can be activated via Enter/Space without a mouse
    6. Spot-check /blog, a blog post, and the homepage hero/about sections in DevTools — confirm images show meaningful alt text (not filenames, not empty) when inspected
  </how-to-verify>
  <resume-signal>Type "approved" or describe any element with no visible focus ring / any remaining heading skip / any image with missing or meaningless alt text</resume-signal>
</task>

</tasks>

<threat_model>
No new trust boundary or input surface is introduced. Both tasks are presentation-layer changes (CSS outline rule, heading tag rename) with no new dependency, no new data flow, and no user-controllable input. Threat surface: negligible — stated briefly per phase security policy rather than omitted.
</threat_model>

<verification>
Run `npm run build` after Tasks 1-2. Checkpoint task must receive explicit human approval before this plan is considered complete.
</verification>

<success_criteria>
- globals.css has a single site-wide :focus-visible rule using var(--accent)
- /automate has no heading-level skip (h1 → h2 → h2, verified by source assertion and human accessibility-tree check)
- All 7 originally-flagged images have verified (grep-checked), non-empty alt text
- Human checkpoint approved: visible focus ring on every interactive element, logical heading order, full keyboard operability, meaningful alt text
</success_criteria>

<output>
Create `.planning/phases/11-cleanup-consistency/11-02-SUMMARY.md` when done
</output>
