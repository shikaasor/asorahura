---
phase: 09-homepage-funnel-reposition
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/SocialProof.tsx
  - src/components/home/SocialProof.module.css
  - src/app/engage/page.tsx
  - src/app/services/page.tsx
  - src/app/services/services.module.css
autonomous: true
requirements: [HOME-15, HOME-17, HOME-18, HOME-19]

must_haves:
  truths:
    - "SocialProof renders named testimonials with headshots sourced from testimonials.json, not hardcoded anonymized names"
    - "No anonymized testimonial initials (T.N., R.O., P.J.) appear anywhere in SocialProof.tsx"
    - "/engage always presents itself as the enterprise intake form regardless of query params"
    - "/services no longer offers a mismatched SMB-path (/assessment) CTA alongside its enterprise pricing tiers"
    - "/enterprise's case-study section remains a lower-priority credibility strip, not primary proof (verified, no change needed)"
  artifacts:
    - path: "src/components/home/SocialProof.tsx"
      provides: "testimonials.json-sourced proof with headshots + /enterprise escape hatch"
    - path: "src/app/engage/page.tsx"
      provides: "Enterprise-only intake copy"
  key_links:
    - from: "src/components/home/SocialProof.tsx"
      to: "src/content/testimonials.json"
      via: "static import"
      pattern: "import testimonials from \"@/content/testimonials.json\""
    - from: "src/components/home/SocialProof.tsx"
      to: "/enterprise"
      via: "escape-hatch Link after the proof grid"
      pattern: "href=\"/enterprise\""
---

<objective>
Replace SocialProof's hardcoded anonymized testimonials with named, headshot-bearing entries statically imported from testimonials.json; force /engage to always read as the enterprise intake form; remove the stray SMB-path CTA from the enterprise-only /services page.

Purpose: HOME-18 (named testimonials with headshots replace the anonymized set), HOME-19 (/engage reframed as enterprise-only, out of the primary path), HOME-15 (the /services enterprise-pricing page no longer signposts the SMB /assessment path), HOME-17 (verify — no code change required, /enterprise's existing case-study section already sits below the verticals as a credibility strip, not primary proof).
Output: Rewritten SocialProof.tsx/.module.css (JSON-sourced testimonials + escape hatch), reframed engage/page.tsx copy, cleaned services/page.tsx hero CTAs, plus a verification-only check against /enterprise's existing section order for HOME-17.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-homepage-funnel-reposition/09-UI-SPEC.md
@.planning/phases/09-homepage-funnel-reposition/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source SocialProof from testimonials.json with headshots</name>
  <files>src/components/home/SocialProof.tsx, src/components/home/SocialProof.module.css</files>
  <read_first>
    src/components/home/SocialProof.tsx
    src/components/home/SocialProof.module.css
    src/content/testimonials.json
    .planning/phases/09-homepage-funnel-reposition/09-RESEARCH.md (Pattern 3, Code Examples → SocialProof)
  </read_first>
  <action>
    Remove the hardcoded `testimonials` array (T.N., R.O., P.J. entries) entirely. Add `import Link from "next/link";` and `import testimonials from "@/content/testimonials.json";` to the top imports.
    Add a `proofItems` array built as: `[testimonials.services[0], testimonials.services[1], testimonials.hero]` (Pawel Janas, Aamna Mansoor, Maria Rios — each has `quote`, `name`, `title`, `headshot` fields per the JSON schema).
    Map over `proofItems` instead of the old array. In each card, replace `t.role` references with `t.title` (the JSON field is named `title`, not `role`). Inside `.attribution`, before the `<strong>{t.name}</strong>` block, conditionally render the headshot: `{t.headshot && <img src={t.headshot} alt={t.name} className={styles.headshot} />}`.
    After the closing `</div>` of `.grid`, add an escape-hatch paragraph: `<p className={styles.enterpriseNote}><Link href="/enterprise">Looking for enterprise case studies? →</Link></p>`.
    In SocialProof.module.css: add a `.headshot` rule — width 48px, height 48px, border-radius 50%, object-fit cover, margin-bottom var(--spacing-2) (place it above `.attribution strong` in the cascade, or restructure `.attribution` to a row layout with the image inline — pick whichever keeps `.attribution` a simple flex container, no new wrapper div needed). Add an `.enterpriseNote` rule mirroring PainSection's `.cta` styling: text-align center, margin-top var(--spacing-5), color var(--ink-2), font-size var(--fontSize-2), font-weight var(--fontWeight-bold), text-decoration underline, `:hover` color var(--accent).
  </action>
  <verify>
    <automated>grep -c "T\.N\.\|R\.O\.\|P\.J\." src/components/home/SocialProof.tsx (expect 0); grep -c "import testimonials from" src/components/home/SocialProof.tsx (expect 1)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import testimonials from" src/components/home/SocialProof.tsx` returns 1
    - `grep -c "T\.N\.\|R\.O\.\|P\.J\." src/components/home/SocialProof.tsx` returns 0
    - `grep -c "headshot" src/components/home/SocialProof.tsx` returns at least 1
    - `grep -c "href=\"/enterprise\"" src/components/home/SocialProof.tsx` returns at least 1
    - `npm run test -- --watch src/components/home` passes
  </acceptance_criteria>
  <done>SocialProof renders 3 named testimonials with headshots pulled statically from testimonials.json, with no anonymized initials remaining, and links to /enterprise for enterprise case studies.</done>
</task>

<task type="auto">
  <name>Task 2: Force /engage to read as enterprise-only intake</name>
  <files>src/app/engage/page.tsx</files>
  <read_first>
    src/app/engage/page.tsx
    src/app/engage/actions.ts
    .planning/phases/09-homepage-funnel-reposition/09-RESEARCH.md (Pitfall 1, Code Examples → engage/page.tsx mutations)
    .planning/phases/09-homepage-funnel-reposition/09-UI-SPEC.md (Copywriting Contract row "/engage reframing")
  </read_first>
  <action>
    In the `.eyebrow` p (currently `{isEnterprise ? "Enterprise Discovery Brief" : "Discovery Brief"}`), remove the ternary and render the static string "Enterprise Discovery Brief" — the page is enterprise-only now regardless of the `enterprise` query param.
    In the `.headline` h1 (currently `{isEnterprise ? "Discuss Your Enterprise Challenges" : "Tell Me About Your Problem"}`), remove the ternary and render the static string "Discuss Your Enterprise Challenges".
    Do NOT remove the `isEnterprise` variable itself or the `<input type="hidden" name="enterprise" value={isEnterprise ? "true" : "false"} />` — `actions.ts` still branches on this hidden field for its Calendly-vs-confirmation redirect, which is out of this phase's scope. Only the two visible copy ternaries above change.
    Leave the form fields, submit handler, and all styling untouched.
  </action>
  <verify>
    <automated>grep -c "Discuss Your Enterprise Challenges" src/app/engage/page.tsx (expect 1); grep -c "name=\"enterprise\"" src/app/engage/page.tsx (expect 1)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "enterprise" src/app/engage/page.tsx | wc -l` returns at least 1 (already true, now unconditional)
    - `grep -c "isEnterprise ? \"Enterprise Discovery Brief\" : \"Discovery Brief\"" src/app/engage/page.tsx` returns 0
    - `grep -c "isEnterprise ? \"Discuss Your Enterprise Challenges\" : \"Tell Me About Your Problem\"" src/app/engage/page.tsx` returns 0
    - `grep -c "Discuss Your Enterprise Challenges" src/app/engage/page.tsx` returns 1
    - `grep -c "name=\"enterprise\"" src/app/engage/page.tsx` returns 1 (hidden field preserved for actions.ts compatibility)
  </acceptance_criteria>
  <done>/engage always displays "Enterprise Discovery Brief" / "Discuss Your Enterprise Challenges" copy regardless of query params; the hidden enterprise field and actions.ts redirect logic are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the mismatched SMB CTA from /services and verify /enterprise's case-study position (HOME-17)</name>
  <files>src/app/services/page.tsx, src/app/services/services.module.css</files>
  <read_first>
    src/app/services/page.tsx
    src/app/services/services.module.css
    src/app/enterprise/page.tsx
  </read_first>
  <action>
    In services/page.tsx's `.heroActions` div, remove the second `<Link href="/assessment" className={styles.heroSecondary}>Start the Discovery</Link>` — this page is enterprise-tier pricing only (already $5,000–$30,000+), and linking out to the SMB /assessment discovery flow from an enterprise pricing page contradicts the "one entry price story" requirement (HOME-15). Keep the first `<Link href="/engage" className={styles.workWithMeBtn}>Work With Me</Link>` as the sole hero CTA.
    In services.module.css, remove the now-orphaned `.heroSecondary` rule (it was only used by the Link just deleted).
    No changes needed to src/app/enterprise/page.tsx — confirm by reading it that the `CASE_STUDY_SUMMARIES` section ("What We&apos;ve Built") renders after `ENTERPRISE_VERTICALS` and before `SERVICE_TIER_SUMMARIES`/the closing CTA section, which already satisfies HOME-17 (case studies as a credibility strip, not primary proof — the page opens with verticals, not case studies).
  </action>
  <verify>
    <automated>grep -c "href=\"/assessment\"" src/app/services/page.tsx (expect 0); grep -c "heroSecondary" src/app/services/page.tsx (expect 0)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "href=\"/assessment\"" src/app/services/page.tsx` returns 0
    - `grep -c "heroSecondary" src/app/services/page.tsx` returns 0
    - `grep -c "heroSecondary" src/app/services/services.module.css` returns 0
    - `grep -c "workWithMeBtn" src/app/services/page.tsx` returns 1
    - In src/app/enterprise/page.tsx, the `<section className={styles.caseStudiesSection}>` block appears after `<section className={styles.verticalsSection}>` and before `<section className={styles.tiersSection}>` in source order (manual read confirmation, no edit required)
  </acceptance_criteria>
  <done>/services shows one hero CTA (Work With Me → /engage), no stray link into the SMB /assessment flow; /enterprise's case-study section is confirmed positioned as a credibility strip below the verticals, satisfying HOME-17 without a code change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Old shared /engage links (social media, cached pages) → /engage form | Untrusted referrers may carry stale `?enterprise=false` or no param at all, expecting the old general-purpose form |
| testimonials.json → SocialProof render | Build-time data source; must not become a runtime-fetched/tamperable source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Elevation of privilege (SMB user reaches enterprise form expecting general contact) | src/app/engage/page.tsx | mitigate | Copy is now unconditionally "Enterprise Discovery Brief" / "Discuss Your Enterprise Challenges" regardless of query params, so any visitor landing on a stale link immediately sees enterprise framing instead of a generic form; full referrer-based redirect is Phase 10's assessment-gate responsibility (out of scope here, per 09-RESEARCH.md A6) |
| T-09-03 | Tampering (testimonial content altered at runtime) | src/components/home/SocialProof.tsx | mitigate | `testimonials.json` is statically imported (`import testimonials from "@/content/testimonials.json"`), compiled into the build — never fetched client-side or server-side at request time |
</threat_model>

<verification>
- `grep -c "T\.N\.\|R\.O\.\|P\.J\." src/components/home/SocialProof.tsx` returns 0
- `grep -c "import testimonials from" src/components/home/SocialProof.tsx` returns 1
- `grep -ci "enterprise" src/app/engage/page.tsx` returns at least 1, and both ternaries are gone
- `grep -c "href=\"/assessment\"" src/app/services/page.tsx` returns 0
- `npm run test` full suite green
</verification>

<success_criteria>
- SocialProof shows 3 named, headshot-bearing testimonials sourced from testimonials.json; no anonymized names remain.
- /engage always reads as enterprise intake.
- /services carries a single, enterprise-consistent CTA.
- /enterprise's case-study section position is confirmed as a credibility strip (HOME-17), no change required.
</success_criteria>

<output>
Create `.planning/phases/09-homepage-funnel-reposition/09-04-SUMMARY.md` when done
</output>
