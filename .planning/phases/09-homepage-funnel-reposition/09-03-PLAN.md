---
phase: 09-homepage-funnel-reposition
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/PainSection.tsx
  - src/components/home/PainSection.module.css
  - src/components/home/ServicesPreview.tsx
  - src/components/home/ServicesPreview.module.css
autonomous: true
requirements: [HOME-13, HOME-14, HOME-15]

must_haves:
  truths:
    - "PainSection speaks to creators/coaches (lead capture, manual DMs, income) with no regulated-vertical or regulation-name copy"
    - "PainSection offers an escape hatch to /enterprise for regulated-industry visitors"
    - "ServicesPreview reads as a two-rung ladder: Product #1 (Instagram Lead Automation) as the visible, real-priced entry rung, and a second rung showing the other four offerings as coming soon"
    - "No sector-track chips or /assessment sector-picker links remain on the homepage services section"
  artifacts:
    - path: "src/components/home/PainSection.tsx"
      provides: "Creator/coach pain cards + /enterprise escape hatch"
    - path: "src/components/home/ServicesPreview.tsx"
      provides: "Two-rung automation ladder with real Product #1 pricing"
  key_links:
    - from: "src/components/home/PainSection.tsx"
      to: "/enterprise"
      via: "escape-hatch Link after the pain-card grid"
      pattern: "href=\"/enterprise\""
    - from: "src/components/home/ServicesPreview.tsx"
      to: "/automate/instagram"
      via: "Rung 1 card Link"
      pattern: "href=\"/automate/instagram\""
    - from: "src/components/home/ServicesPreview.tsx"
      to: "/automate"
      via: "Rung 2 (waitlist) card Link"
      pattern: "href=\"/automate\""
---

<objective>
Rewrite PainSection to speak to creators/coaches instead of regulated verticals, and rewrite ServicesPreview so the homepage shows Product #1 as a real, visible entry rung on a product ladder instead of $5k+ enterprise engagements.

Purpose: HOME-13 (creator-language pain section, regulated verticals move to /enterprise), HOME-14 (ladder with Product #1 as the visible entry rung at its real price), HOME-15 (one consistent entry-price story — no $5,000/"sector-aware assessment" framing left on the homepage).
Output: Rewritten PainSection.tsx/.module.css (4 creator pain cards + escape hatch) and ServicesPreview.tsx/.module.css (2-rung ladder, sector strip removed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-homepage-funnel-reposition/09-UI-SPEC.md
@.planning/phases/09-homepage-funnel-reposition/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite PainSection for creator/coach pain points</name>
  <files>src/components/home/PainSection.tsx, src/components/home/PainSection.module.css</files>
  <read_first>
    src/components/home/PainSection.tsx
    src/components/home/PainSection.module.css
    .planning/phases/09-homepage-funnel-reposition/09-RESEARCH.md (Pattern 3 "PainSection" code example, Pitfall 3)
    .planning/phases/09-homepage-funnel-reposition/09-PATTERNS.md (PainSection.tsx section)
  </read_first>
  <action>
    Replace the `import { Scale, Landmark, Building2, HardHat } from "lucide-react"` line with `import { TrendingUp, Clock, Zap, DollarSign } from "lucide-react"` and add `import Link from "next/link";` above the styles import.
    Replace the `sectorCards` array (Law firms / Finance & RIAs / Real estate & property / Construction, with ABA Rule 1.6 / SR 11-7 / EU AI Act / Fair Housing copy) with a `creatorPains` array of exactly 4 objects using the `{ icon, title, body }` shape:
    1. icon `<TrendingUp size={24} />`, title "Growing followers, not income", body "You&apos;ve mastered ad spend and follower growth. But leads come in DMs and you&apos;re manually capturing them. That&apos;s not a business — that&apos;s a job."
    2. icon `<Clock size={24} />`, title "Your time is your bottleneck", body "Lead capture, qualification, follow-up — you&apos;re doing it all manually. Every lead that comes in while you&apos;re asleep is a lead you never reach."
    3. icon `<Zap size={24} />`, title "The systems exist, but they don&apos;t talk", body "Instagram, email, CRM, Calendly — they work. But building the flow between them yourself takes time you don&apos;t have."
    4. icon `<DollarSign size={24} />`, title "One automation = time + money back", body "Instagram Comment-to-DM costs $6/mo owned, or $17–$99/mo with ManyChat. At $500, it pays for itself in 3 months."
    Update `.subheading` text (currently "Different sectors, same pattern — exposure without architecture...") to: "Growing creators and coaches hit the same ceiling — manual lead capture while the rest of your business runs on autopilot." Keep the `.heading` "Sound familiar?" unchanged.
    Map over `creatorPains` instead of `sectorCards` (rename the `.map((card) => ...)` variable accordingly, same JSX shape: icon div, `.cardTitle` h3, `.cardBody` p).
    After the closing `</div>` of `.grid`, add an escape-hatch paragraph: `<p className={styles.cta}><Link href="/enterprise">Working in a regulated industry? →</Link></p>`.
    In PainSection.module.css, add a new `.cta` rule after the existing `.grid`/`.card` rules: text-align center, margin-top var(--spacing-5), containing an `a` (or apply directly if the Link renders as the only child) styled with color var(--ink-2), font-size var(--fontSize-2), font-weight var(--fontWeight-bold), text-decoration underline, and a `:hover` state with color var(--accent). Do not reuse `--accent` as the default (non-hover) color — this is a secondary path, not the primary CTA.
  </action>
  <verify>
    <automated>grep -ci "aba\|sr 11-7\|ai act\|fair housing" src/components/home/PainSection.tsx (expect 0); grep -c "href=\"/enterprise\"" src/components/home/PainSection.tsx (expect >=1)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "aba\|sr 11-7\|ai act\|fair housing" src/components/home/PainSection.tsx` returns 0
    - `grep -c "href=\"/enterprise\"" src/components/home/PainSection.tsx` returns at least 1
    - `grep -c "Growing followers, not income" src/components/home/PainSection.tsx` returns 1
    - `grep -c "Scale\|Landmark\|Building2\|HardHat" src/components/home/PainSection.tsx` returns 0
    - `npm run test -- --watch src/components/home` passes
  </acceptance_criteria>
  <done>PainSection shows 4 creator/coach pain cards with no regulated-vertical or regulation-name copy, and links to /enterprise as an escape hatch.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ServicesPreview as a 2-rung automation ladder</name>
  <files>src/components/home/ServicesPreview.tsx, src/components/home/ServicesPreview.module.css</files>
  <read_first>
    src/components/home/ServicesPreview.tsx
    src/components/home/ServicesPreview.module.css
    .planning/phases/09-homepage-funnel-reposition/09-RESEARCH.md (Pattern 2, Code Examples → ServicesPreview)
    .planning/phases/09-homepage-funnel-reposition/09-UI-SPEC.md (Copywriting Contract row "Empty/waitlist state", Component-Level Notes §3, Color section on accent restriction)
  </read_first>
  <action>
    Replace the `services` array (AI Opportunity Discovery $5,000 / Ops Automation Build $5,000–$15,000 / Systems Architecture $15,000–$30,000+, all hrefs "/engage") with a `ladder` array of exactly 2 objects using shape `{ tier, title, description, entry, cta, href }`:
    1. tier: 1, title: "Instagram Lead Automation", description: "Capture leads from Instagram comments, convert to DMs, nurture in email.", entry: "Free (DIY) or $500 (Done For You)", cta: "Get Started", href: "/automate/instagram"
    2. tier: 2, title: "The Next Four", description: "Email triage on Telegram, writing constitution + content, rate-aware invoice, client onboarding agent.", entry: "Coming soon.", cta: "Join Waitlist", href: "/automate"
    Delete the `sectorTracks` array entirely, and delete the entire `.sectorStrip` JSX block (the `<div className={styles.sectorStrip}>...</div>` including its sectorLabel span, sectorChips div, and the `/assessment` sectorLink) — this section is not part of the creator-ladder positioning.
    Change `.heading` text from "How I help" to "The Automation Ladder", and add a `<p className={styles.subheading}>Start with one. Earn with it. Scale to five.</p>` immediately after the heading (mirror the subheading pattern already used in PainSection.tsx — this class does not yet exist in ServicesPreview.module.css, add it per Task action below).
    Map over `ladder` instead of `services`. On each card div, add a tier badge: `<div className={styles.tier}>Rung {service.tier}</div>` as the first child inside `.card`, before `.cardTop`. Apply a conditional className on the outer card div: `` `${styles.card} ${service.tier === 1 ? styles.featured : styles.coming}` ``. Change `.cardBottom`'s `{service.price}` reference to `{service.entry}`, and the link label from the hardcoded "Learn More →" to `{service.cta} →`.
    In ServicesPreview.module.css: remove the `.sectorStrip`, `.sectorLabel`, `.sectorChips`, `.sectorChip`, `.sectorLink` rules entirely (orphaned by removing the sector strip). Add a `.subheading` rule (text-align center, color var(--ink-2), font-size var(--fontSize-2), margin-bottom var(--spacing-6), matching PainSection.module.css's `.subheading` pattern). Add a `.tier` rule (font-size var(--fontSize-1), font-weight var(--fontWeight-bold), text-transform uppercase, letter-spacing 0.1em, color var(--ink-2), margin-bottom var(--spacing-3)). Add a `.featured` rule that overrides `.card`'s border to `2px solid var(--accent)` (Rung 1 gets the sole accent-bordered treatment per UI-SPEC's accent-restriction rule). Add a `.coming` rule that overrides `.price` (now unused — rename `.price` usages accordingly if you keep the class name) to color var(--ink-2) instead of var(--accent), since Rung 2 is a waitlist state and must not carry accent.
  </action>
  <verify>
    <automated>grep -c "href=\"/automate/instagram\"" src/components/home/ServicesPreview.tsx (expect >=1); grep -c "sectorTracks\|sectorStrip\|sectorChip" src/components/home/ServicesPreview.tsx (expect 0)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "Free|\\$500|\\$800" src/components/home/ServicesPreview.tsx | wc -l` returns at least 1
    - `grep -c "href=\"/automate/instagram\"" src/components/home/ServicesPreview.tsx` returns at least 1
    - `grep -c "href=\"/automate\"" src/components/home/ServicesPreview.tsx` returns at least 1
    - `grep -c "sectorTracks\|sectorStrip\|sectorChip" src/components/home/ServicesPreview.tsx` returns 0
    - `grep -c "\\$5,000\|\\$15,000\|\\$30,000" src/components/home/ServicesPreview.tsx` returns 0
    - `npm run test -- --watch src/components/home` passes
  </acceptance_criteria>
  <done>ServicesPreview shows a 2-rung ladder with Instagram Lead Automation as the featured, real-priced Rung 1 linking to /automate/instagram, and a waitlist Rung 2 linking to /automate; no enterprise pricing or sector-track chips remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Homepage → user | Public, unauthenticated rendering; no user input processed in this plan's files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Information disclosure (mixed pricing signals) | ServicesPreview.tsx | mitigate | Removing $5k–$30k+ enterprise pricing from the homepage ladder and the /assessment sector strip ensures a cold SMB visitor only ever sees the real Product #1 entry price ($0/$500), closing the "free/$50-per-hour/$5,000" contradiction named in HOME-15 |
</threat_model>

<verification>
- `grep -ci "aba\|sr 11-7\|ai act\|fair housing" src/components/home/PainSection.tsx` returns 0
- `grep -c "\\$5,000\|\\$15,000\|\\$30,000" src/components/home/ServicesPreview.tsx` returns 0
- `npm run test` full suite green
</verification>

<success_criteria>
- PainSection speaks entirely in creator/coach language with an /enterprise escape hatch.
- ServicesPreview shows exactly 2 ladder rungs, Rung 1 real-priced and featured, Rung 2 waitlist-framed, no enterprise pricing.
</success_criteria>

<output>
Create `.planning/phases/09-homepage-funnel-reposition/09-03-SUMMARY.md` when done
</output>
