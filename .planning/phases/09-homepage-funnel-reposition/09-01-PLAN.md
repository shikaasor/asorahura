---
phase: 09-homepage-funnel-reposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/HeroSection.tsx
  - src/components/home/HeroSection.module.css
  - src/components/Navigation.tsx
autonomous: true
requirements: [HOME-12, HOME-16, HOME-20, HOME-21]

must_haves:
  truths:
    - "A cold reader sees exactly one primary CTA above the fold on the homepage (hero), labeled 'See Automations', linking to /automate"
    - "The site nav shows the same single CTA label and href, plus a 'Pricing' link to /services"
    - "The hero headline leads with income/money language, not hours-saved language"
    - "The hero eyebrow carries the sitewide positioning statement verbatim"
  artifacts:
    - path: "src/components/home/HeroSection.tsx"
      provides: "Income-framed hero with single CTA to /automate"
    - path: "src/components/Navigation.tsx"
      provides: "Single CTA nav + Pricing link"
  key_links:
    - from: "src/components/home/HeroSection.tsx"
      to: "/automate"
      via: "Link href in .actions"
      pattern: "href=\"/automate\""
    - from: "src/components/Navigation.tsx"
      to: "/automate"
      via: "styles.cta Link (desktop + mobile)"
      pattern: "className={styles.cta}"
---

<objective>
Reposition the homepage hero to lead with income growth and a single CTA, and update the global Navigation so the same CTA and label appear sitewide, with pricing surfaced in the nav.

Purpose: HOME-12 (income framing), HOME-16 (one primary CTA replacing four competing entry points), HOME-20 (pricing in nav), and the hero half of HOME-21 (positioning statement in the eyebrow).
Output: Rewritten HeroSection.tsx (+ CSS orphan cleanup) and Navigation.tsx with consolidated CTA and "Pricing" nav label.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-homepage-funnel-reposition/09-UI-SPEC.md
@.planning/phases/09-homepage-funnel-reposition/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite HeroSection for income framing and single CTA</name>
  <files>src/components/home/HeroSection.tsx, src/components/home/HeroSection.module.css</files>
  <read_first>
    src/components/home/HeroSection.tsx
    src/components/home/HeroSection.module.css
    .planning/phases/09-homepage-funnel-reposition/09-UI-SPEC.md (Copywriting Contract, Component-Level Notes §1)
  </read_first>
  <action>
    In HeroSection.tsx: change the eyebrow `<p className={styles.eyebrow}>` text (currently "AI Systems Consultant") to the exact positioning statement: "Automations that work like your best hire — reliable, consistent, and yours to keep." (this exact string must also land verbatim in Plan 09-02's Footer tagline and layout.tsx description — do not paraphrase).
    Change the h1 `.headline` text (currently "You&rsquo;re spending more time managing your business than growing it.") to "Small automations that make measurable money." — no "hours" wording anywhere in this file (verified by grep).
    Change the `.subheading` p text to: "Start with Instagram leads, scale to five offerings. Own the system. Own the profit."
    Replace the two links inside `.actions` (currently a primaryBtn to /assessment and a secondaryBtn to /engage) with exactly one Link: href="/automate", className={styles.primaryBtn}, label "See Automations" — remove the secondaryBtn Link entirely (per HOME-16, exactly one href to /automate, /assessment, or /engage may remain in this file).
    Leave TrustSignals and TestimonialCard (HERO_TESTIMONIAL = testimonials.hero) untouched — same imports, same JSX position.
    In HeroSection.module.css: remove the now-unused `.secondaryBtn` and `.secondaryBtn:hover` rules (orphaned by removing the secondary Link), and remove `.secondaryBtn` from the two responsive `.primaryBtn, .secondaryBtn { width: 100%; ... }` selector groups (mobile @media 768px and @media 640px) — keep `.primaryBtn` alone in those selectors so the mobile full-width button rule still applies to the remaining CTA.
  </action>
  <verify>
    <automated>grep -E "href=\"/(automate|assessment|engage)\"" src/components/home/HeroSection.tsx | wc -l (expect 1)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hours" src/components/home/HeroSection.tsx` returns 0
    - `grep -E "href=\"/(automate|assessment|engage)\"" src/components/home/HeroSection.tsx | wc -l` returns exactly 1
    - `grep -c "See Automations" src/components/home/HeroSection.tsx` returns at least 1
    - `grep -c "Automations that work like your best hire" src/components/home/HeroSection.tsx` returns at least 1
    - `grep -c "secondaryBtn" src/components/home/HeroSection.tsx` returns 0
    - `grep -c "secondaryBtn" src/components/home/HeroSection.module.css` returns 0
    - `npm run test -- --watch src/components/home` passes (no existing test regressions)
  </acceptance_criteria>
  <done>HeroSection renders income-framed copy with exactly one CTA to /automate labeled "See Automations"; eyebrow carries the exact positioning statement; no dead secondaryBtn CSS remains.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate Navigation CTA and rename Services link to Pricing</name>
  <files>src/components/Navigation.tsx</files>
  <read_first>
    src/components/Navigation.tsx
    .planning/phases/09-homepage-funnel-reposition/09-UI-SPEC.md (Copywriting Contract, Component-Level Notes §5)
  </read_first>
  <action>
    In both the desktop links block (around line 39) and the mobile menu block (around line 74), change the visible text of the `<Link href="/services" ...>` from "Services" to "Pricing". Keep the href ("/services") and the existing active-state className logic (`pathname === "/services" ? styles.active : ""` / `styles.mobileActive`) unchanged — this is a label-only rename, not a routing change.
    Change the desktop primary CTA Link (currently `href="/assessment"` with label "Start AI Opportunity Discovery", className={styles.cta}): set href="/automate" and label to "See Automations". Make the identical change to the mobile CTA Link (className={styles.mobileCta}).
    Leave Work, Discovery (/assessment), Blog, and Enterprise links untouched in both desktop and mobile blocks — this task only touches the Services→Pricing rename and the CTA href/label.
  </action>
  <verify>
    <automated>grep -c "className={styles.cta}" src/components/Navigation.tsx (expect 1)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "className={styles.cta}" src/components/Navigation.tsx` returns exactly 1
    - `grep -A2 -B2 "href=\"/services\"" src/components/Navigation.tsx | grep -ci "pricing"` returns at least 2 (desktop + mobile)
    - `grep -c "See Automations" src/components/Navigation.tsx` returns exactly 2 (desktop cta + mobile cta)
    - `grep -c "Start AI Opportunity Discovery" src/components/Navigation.tsx` returns 0
    - `npm run test -- --watch src/components` passes (no existing test regressions)
  </acceptance_criteria>
  <done>Navigation shows a single .cta-styled link (href /automate, label "See Automations") in both desktop and mobile menus, and the /services link is labeled "Pricing" in both.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Homepage/nav → user | Public, unauthenticated rendering; no user input processed in this plan's files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Elevation of privilege (funnel confusion) | HeroSection.tsx, Navigation.tsx | mitigate | Reducing CTA count to exactly one per surface (HOME-16) removes the ambiguous-path risk of a cold visitor landing on the enterprise-only /engage form; verified via grep link-count checks in acceptance criteria |
| T-09-06 | Tampering (accidental copy drift) | HeroSection.tsx | accept | Positioning-statement string duplicated in three files (Hero, Footer, layout.tsx) by design per HOME-21 — no shared constant introduced (single-use literal per CLAUDE.md simplicity rule); cross-file consistency verified by HOME-21 substring-match test in Plan 09-02 |
</threat_model>

<verification>
- `npm run test -- --watch src/components` — component-level Vitest suite green
- `grep -E "href=\"/(automate|assessment|engage)\"" src/components/home/HeroSection.tsx | wc -l` equals 1
- `grep -c "className={styles.cta}" src/components/Navigation.tsx` equals 1
</verification>

<success_criteria>
- HeroSection.tsx has exactly one entry-point CTA, income-framed copy, no "hours" language.
- Navigation.tsx has one .cta link (See Automations → /automate) desktop + mobile, and a Pricing label on /services desktop + mobile.
</success_criteria>

<output>
Create `.planning/phases/09-homepage-funnel-reposition/09-01-SUMMARY.md` when done
</output>
