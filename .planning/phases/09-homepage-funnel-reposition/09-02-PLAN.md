---
phase: 09-homepage-funnel-reposition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/components/home/Footer.tsx
  - src/components/home/Footer.module.css
autonomous: true
requirements: [HOME-21]

must_haves:
  truths:
    - "Page metadata (title/description/OG) is built from the one positioning statement and metadataBase points at the production domain, not vercel.app"
    - "The footer tagline is the same positioning statement (verbatim) that appears in the hero eyebrow"
    - "Footer navigation is grouped into Creator Path, Enterprise, and Legal so the two funnels read as visibly separate"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "metadataBase on production domain + positioning-statement metadata"
    - path: "src/components/home/Footer.tsx"
      provides: "Positioning-statement tagline + Creator Path / Enterprise / Legal nav groups"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/home/HeroSection.tsx"
      via: "shared positioning-statement string (metadata description == hero eyebrow text)"
      pattern: "Automations that work like your best hire"
    - from: "src/components/home/Footer.tsx"
      to: "src/components/home/HeroSection.tsx"
      via: "shared positioning-statement string (footer tagline == hero eyebrow text)"
      pattern: "Automations that work like your best hire"
---

<objective>
Point production metadata at the real domain and carry the single positioning statement into layout.tsx metadata and the Footer tagline; reorganize footer nav links into Creator Path / Enterprise / Legal so the two funnels don't blur together.

Purpose: HOME-21 — one positioning statement across metadata/hero/footer, metadataBase on asorahura.com instead of the vercel.app preview domain (closes T-09-01, OG-spoofing via wrong origin).
Output: layout.tsx with production metadataBase + positioning-statement metadata; Footer.tsx with matching tagline and three-column nav grouping; Footer.module.css updated for the added column.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-homepage-funnel-reposition/09-UI-SPEC.md
@.planning/phases/09-homepage-funnel-reposition/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Point metadataBase at production domain and carry the positioning statement into metadata</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    src/app/layout.tsx
    .planning/phases/09-homepage-funnel-reposition/09-UI-SPEC.md (Copywriting Contract row "Positioning statement", Component-Level Notes §7)
    .planning/phases/09-homepage-funnel-reposition/09-RESEARCH.md (Pitfall 4)
  </read_first>
  <action>
    Change `metadataBase: new URL("https://asorahura.vercel.app")` to `metadataBase: new URL("https://asorahura.com")`.
    Change `title` (currently "Asor Ahura | AI Automation Authority") to "Asor Ahura | Automations that work like your best hire".
    Change `description` (currently "Transforming Work into Flow. AI-powered automation systems that eliminate operational drag.") to the exact positioning statement: "Automations that work like your best hire — reliable, consistent, and yours to keep." — this string must be byte-identical to the HeroSection eyebrow text from Plan 09-01 and the Footer tagline from Task 2 below.
    Apply the same title/description values to the nested `openGraph.title` and `openGraph.description` fields.
    Leave `viewport`, font setup, `openGraph.images`, `twitter`, and `icons` untouched.
  </action>
  <verify>
    <automated>grep -c "https://asorahura.com" src/app/layout.tsx (expect >=1); grep -c "vercel.app" src/app/layout.tsx (expect 0)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "asorahura.vercel.app" src/app/layout.tsx` returns 0
    - `grep -c "https://asorahura.com" src/app/layout.tsx` returns at least 1
    - `grep -c "Automations that work like your best hire" src/app/layout.tsx` returns at least 2 (description + openGraph.description)
    - `npm run build` completes without a metadataBase/URL construction error (or `npm run test` if build is slow — confirm no TypeScript error in layout.tsx)
  </acceptance_criteria>
  <done>layout.tsx metadataBase resolves to https://asorahura.com; title/description/openGraph carry the positioning statement.</done>
</task>

<task type="auto">
  <name>Task 2: Update Footer tagline and reorganize nav into Creator Path / Enterprise / Legal</name>
  <files>src/components/home/Footer.tsx, src/components/home/Footer.module.css</files>
  <read_first>
    src/components/home/Footer.tsx
    src/components/home/Footer.module.css
    .planning/phases/09-homepage-funnel-reposition/09-RESEARCH.md (Code Examples → Footer)
    .planning/phases/09-homepage-funnel-reposition/09-PATTERNS.md (Footer.tsx section)
  </read_first>
  <action>
    In Footer.tsx: change the `.tagline` p text (currently "Scale your business without scaling your payroll.") to the exact positioning statement: "Automations that work like your best hire — reliable, consistent, and yours to keep." — must be byte-identical to the HeroSection eyebrow (Plan 09-01) and layout.tsx description (Task 1 above).
    Replace the single "Navigation" `<nav>` block (currently: Home, Discovery, Enterprise, Engage, Work) with two `<nav>` blocks:
    - First, labeled "Creator Path" (`<p className={styles.navLabel}>Creator Path</p>`): links to Home (/), Automations (/automate), Learn (/blog), Discovery (/assessment).
    - Second, labeled "Enterprise": links to Enterprise Solutions (/enterprise), Intake Form (/engage), Case Studies (/work).
    Keep the existing Legal column (Privacy, Terms, Refund) and the LinkedIn social link exactly as they are, just shifted right by the new column.
    In Footer.module.css: change `.grid` `grid-template-columns` from `2fr 1fr 1fr` to `2fr 1fr 1fr 1fr` (brand + Creator Path + Enterprise + Legal). In the `@media (max-width: 768px)` block, keep `grid-template-columns: 1fr 1fr` and keep `.brand { grid-column: 1 / -1; }` — this already spans the new column count correctly, no other change needed there.
  </action>
  <verify>
    <automated>grep -c "Creator Path" src/components/home/Footer.tsx (expect >=1); grep -c "2fr 1fr 1fr 1fr" src/components/home/Footer.module.css (expect >=1)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Automations that work like your best hire" src/components/home/Footer.tsx` returns at least 1
    - `grep -c "Creator Path" src/components/home/Footer.tsx` returns at least 1
    - `grep -c "Enterprise" src/components/home/Footer.tsx` returns at least 1
    - `grep -c "2fr 1fr 1fr 1fr" src/components/home/Footer.module.css` returns at least 1
    - `grep -c "Scale your business without scaling your payroll" src/components/home/Footer.tsx` returns 0
    - `npm run test -- --watch src/components/home` passes
  </acceptance_criteria>
  <done>Footer tagline matches the sitewide positioning statement; footer nav is split into Creator Path / Enterprise / Legal columns; grid CSS reflects the 4-column layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Social preview crawlers → OG metadata | Untrusted crawlers (Slack, Twitter, LinkedIn) read metadataBase-resolved OG image/URL from layout.tsx |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing (wrong origin in social preview) | src/app/layout.tsx metadataBase | mitigate | metadataBase changed from asorahura.vercel.app (preview) to https://asorahura.com (production) so OG/canonical URLs resolve to the real domain; verified via grep absence of "vercel.app" in layout.tsx |
</threat_model>

<verification>
- `grep -c "https://asorahura.com" src/app/layout.tsx` returns at least 1 and `grep -c "vercel.app" src/app/layout.tsx` returns 0
- `grep -c "Automations that work like your best hire" src/components/home/Footer.tsx` and the same grep against `src/app/layout.tsx` both return matches (cross-file consistency check ahead of Plan 09-01's Hero eyebrow)
- `npm run test` full suite green
</verification>

<success_criteria>
- metadataBase resolves to the production domain.
- The positioning statement is byte-identical across layout.tsx description, Footer tagline, and (per Plan 09-01) the Hero eyebrow.
- Footer nav visibly separates the Creator Path funnel from the Enterprise funnel.
</success_criteria>

<output>
Create `.planning/phases/09-homepage-funnel-reposition/09-02-SUMMARY.md` when done
</output>
