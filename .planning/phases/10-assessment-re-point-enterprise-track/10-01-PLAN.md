---
phase: 10-assessment-re-point-enterprise-track
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/revenueCalculation.ts
  - src/components/assessment/AssessmentSectorGate.tsx
  - src/components/assessment/AssessmentSectorGate.module.css
  - src/components/assessment/RevenueResultsScreen.tsx
  - src/components/assessment/RevenueResultsScreen.module.css
  - tests/test-revenue-calculation.test.ts
  - tests/test-assessment-sector-gate.test.ts
  - tests/test-revenue-results-screen.test.ts
autonomous: true
requirements: [ASSESS-11, ASSESS-12]

must_haves:
  truths:
    - "getRevenueOpportunities(sector, tier) returns 3 named automations and a total dollar range for every sector and every tier level"
    - "A sector gate component exists offering a Small Business path and an Enterprise path, with Enterprise routing to /enterprise"
    - "A single results component can render revenue-framed output for both quick (0-100) and deep (0-72) assessment scales"
  artifacts:
    - path: "src/lib/revenueCalculation.ts"
      provides: "getRevenueOpportunities pure function + AutomationOpportunity/RevenueOpportunitiesResult types"
    - path: "src/components/assessment/AssessmentSectorGate.tsx"
      provides: "SMB vs Enterprise entry gate"
    - path: "src/components/assessment/RevenueResultsScreen.tsx"
      provides: "unified revenue-framed results screen for quick + deep assessments"
  key_links:
    - from: "src/components/assessment/RevenueResultsScreen.tsx"
      to: "src/lib/revenueCalculation.ts"
      via: "import"
      pattern: "getRevenueOpportunities"
    - from: "src/components/assessment/RevenueResultsScreen.tsx"
      to: "/checkout"
      via: "Link href"
      pattern: "/checkout"
---

<objective>
Build the foundation layer for Phase 10's revenue-framed assessment: a pure revenue-calculation utility (ASSESS-12), a sector entry gate that routes regulated verticals to the enterprise track instead of the generic assessment (ASSESS-11), and a unified results component that will replace both `ResultsScreen` and `DeepResultsScreen` in Plan 02.

This plan creates net-new files only — nothing in the existing assessment flow is wired up yet (that happens in `10-02-PLAN.md`, which depends on this plan). All three deliverables are self-contained and consumable independently once created.

Purpose: give the executor of `10-02-PLAN.md` finished, tested contracts to import rather than having to design the revenue model and gate UI while also rewiring the live assessment shells.
Output: `src/lib/revenueCalculation.ts`, `src/components/assessment/AssessmentSectorGate.tsx` (+ CSS module), `src/components/assessment/RevenueResultsScreen.tsx` (+ CSS module), plus vitest coverage for all three.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-assessment-re-point-enterprise-track/10-RESEARCH.md
@.planning/phases/10-assessment-re-point-enterprise-track/10-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Revenue calculation utility</name>
  <files>src/lib/revenueCalculation.ts, tests/test-revenue-calculation.test.ts</files>
  <read_first>
    src/lib/sectorRecommendations.ts (Sector×TierLevel pattern, quickScoreToTierLevel/deepScoreToTierLevel, TierLevel type)
    src/lib/assessment.ts (Sector type, SECTORS array)
  </read_first>
  <behavior>
    - getRevenueOpportunities("Other / Cross-Industry", 2) returns exactly 3 opportunities named "Email triage automation", "Client intake system", "Reporting pipeline", each with lowMonthly/highMonthly scaled by the tier-2 multiplier (0.85), and totalLow/totalHigh equal to the sum of all 3 opportunities' low/high.
    - getRevenueOpportunities("Law", 4) returns opportunities named "Client intake & conflicts check automation", "Document review triage", "Billing reconciliation automation" scaled by the tier-4 multiplier (1.5).
    - For every one of the 5 Sector values × 4 TierLevel values, totalLow <= totalHigh and totalLow > 0.
    - lowMonthly/highMonthly/totalLow/totalHigh are all rounded to the nearest 10 (result % 10 === 0).
  </behavior>
  <action>
    Create src/lib/revenueCalculation.ts. Import `type Sector` from "./assessment" and `type TierLevel` from "./sectorRecommendations".

    Export interface AutomationOpportunity { name: string; lowMonthly: number; highMonthly: number; justification: string }.
    Export interface RevenueOpportunitiesResult { sector: Sector; tier: TierLevel; opportunities: AutomationOpportunity[]; totalLow: number; totalHigh: number }.

    Define a module-private const TIER_MULTIPLIER: Record&lt;TierLevel, number&gt; = { 1: 0.6, 2: 0.85, 3: 1.15, 4: 1.5 }.

    Define a module-private const SECTOR_OPPORTUNITY_BASE: Record&lt;Sector, { name: string; low: number; high: number; justification: string }[]&gt; with exactly these 5 entries (base values are pre-multiplier, USD/month):

    "Other / Cross-Industry": [
      { name: "Email triage automation", low: 40, high: 100, justification: "5 hrs/wk at $20/hr" },
      { name: "Client intake system", low: 40, high: 120, justification: "6 hrs/wk at $20/hr" },
      { name: "Reporting pipeline", low: 20, high: 80, justification: "3 hrs/wk at $20/hr" },
    ]

    "Law": [
      { name: "Client intake & conflicts check automation", low: 150, high: 600, justification: "at $300/hr partner time" },
      { name: "Document review triage", low: 200, high: 800, justification: "at $350/hr associate time" },
      { name: "Billing reconciliation automation", low: 150, high: 600, justification: "at $250/hr billing admin time" },
    ]

    "Finance": [
      { name: "KYC/AML automation", low: 300, high: 1000, justification: "at $200/hr ops time" },
      { name: "Portfolio reporting automation", low: 300, high: 1200, justification: "at $300/hr advisor time" },
      { name: "Comms surveillance automation", low: 200, high: 800, justification: "at $400/hr compliance time" },
    ]

    "Real Estate & Property": [
      { name: "Lease abstraction automation", low: 100, high: 500, justification: "at agent/PM hourly rate" },
      { name: "Tenant triage automation", low: 100, high: 500, justification: "at agent/PM hourly rate" },
      { name: "Listing generation automation", low: 100, high: 500, justification: "at agent/PM hourly rate" },
    ]

    "Construction": [
      { name: "Daily report automation", low: 150, high: 700, justification: "at PM/super hourly rate" },
      { name: "RFI triage automation", low: 150, high: 700, justification: "at PM/super hourly rate" },
      { name: "Estimating AI assist", low: 100, high: 600, justification: "at estimator hourly rate" },
    ]

    Export function getRevenueOpportunities(sector: Sector, tier: TierLevel): RevenueOpportunitiesResult. It multiplies each base opportunity's low/high by TIER_MULTIPLIER[tier], rounds each to the nearest 10 via Math.round(x / 10) * 10, and computes totalLow/totalHigh as the sum of the rounded per-opportunity low/high values. Do not round the total after summing already-rounded values (sum the rounded values directly).

    Write tests/test-revenue-calculation.test.ts using vitest (describe/it/expect), following the style in tests/test-offerings-catalog.test.ts (plain `import { describe, it, expect } from "vitest"` + `import { getRevenueOpportunities } from "../src/lib/revenueCalculation"`). Cover the four behavior cases listed above, plus a loop over all 5 sectors × 4 tiers asserting totalLow <= totalHigh and totalLow > 0 and totalLow % 10 === 0.
  </action>
  <verify>
    <automated>npx vitest run tests/test-revenue-calculation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/revenueCalculation.ts exports getRevenueOpportunities, AutomationOpportunity, RevenueOpportunitiesResult
    - `npx vitest run tests/test-revenue-calculation.test.ts` passes with 0 failures
    - `npx tsc --noEmit` reports no new errors introduced by this file
  </acceptance_criteria>
  <done>getRevenueOpportunities is implemented, exported, and covered by a passing vitest suite for all 5 sectors and all 4 tiers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assessment sector gate component</name>
  <files>src/components/assessment/AssessmentSectorGate.tsx, src/components/assessment/AssessmentSectorGate.module.css, tests/test-assessment-sector-gate.test.ts</files>
  <read_first>
    src/components/assessment/QuestionCard.tsx (existing "use client" component + CSS module pairing convention in this directory)
    src/app/globals.css (design tokens: --surface-1, --surface-2, --ink-1, --ink-2, --accent, --accent-hover, --fontSize-3, --fontSize-4, --fontSize-6, --spacing-2..6, --radius-2, --radius-3)
    .planning/phases/10-assessment-re-point-enterprise-track/10-UI-SPEC.md (Sector Gating copywriting contract, lines ~130-142)
  </read_first>
  <behavior>
    - Rendering AssessmentSectorGate with an onContinue prop shows two choice buttons labeled "Small Business" and "Enterprise".
    - Clicking the "Small Business" choice calls the onContinue callback exactly once; it does not navigate.
    - Clicking the "Enterprise" choice navigates to "/enterprise" (via next/navigation's useRouter().push) and does not call onContinue.
  </behavior>
  <action>
    Create src/components/assessment/AssessmentSectorGate.tsx as a "use client" component. Props: `{ onContinue: () => void }`. Import `useRouter` from "next/navigation" and `Link` from "next/link". Import styles from "./AssessmentSectorGate.module.css".

    Render heading "Which path describes you best?" (per UI-SPEC gate heading copy), subheading "Both paths are equally valid — pick the one that fits.", and two `<button type="button">` choice cards:
    - Small Business: heading "Small Business", examples line "Creators, Coaches, Local Services", description "Fast self-serve assessment, low-ticket products, straight to automation", footer label "Continue to Assessment →". onClick calls `onContinue()`.
    - Enterprise: heading "Enterprise", examples line "Law, Finance, Real Estate, Construction", description "Compliance-aware scoping, custom solutions, discovery call included", footer label "Explore Enterprise Path →". onClick calls `router.push("/enterprise")` (router from `useRouter()`).

    Below the two choices, render `<p>Not sure? <Link href="/enterprise">See differences →</Link></p>`.

    Create AssessmentSectorGate.module.css using the design tokens listed in read_first (not the legacy hardcoded hex used elsewhere in this directory — this is new Phase 10 UI per UI-SPEC's locked token mapping). Gate container background var(--surface-2); choice card background var(--surface-1) with border 1px solid var(--surface-3); choice heading uses var(--fontSize-4) weight 700 color var(--ink-1); description text var(--fontSize-2) color var(--ink-2); footer CTA label color var(--accent), hover var(--accent-hover); heading uses var(--fontSize-6).

    Write tests/test-assessment-sector-gate.test.ts as a source-content assertion test (readFileSync pattern from tests/test-automate-catalog-page.test.ts) verifying the file contains the strings "Small Business", "Enterprise", `router.push("/enterprise")`, and `onContinue()`.
  </action>
  <verify>
    <automated>npx vitest run tests/test-assessment-sector-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/assessment/AssessmentSectorGate.tsx exports AssessmentSectorGate accepting `{ onContinue: () => void }`
    - Source contains a literal `router.push("/enterprise")` call (hardcoded destination — no dynamic/user-controlled redirect target)
    - `npx vitest run tests/test-assessment-sector-gate.test.ts` passes
  </acceptance_criteria>
  <done>AssessmentSectorGate renders two choices; Small Business advances the caller via onContinue, Enterprise navigates to /enterprise.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unified revenue-framed results screen</name>
  <files>src/components/assessment/RevenueResultsScreen.tsx, src/components/assessment/RevenueResultsScreen.module.css, tests/test-revenue-results-screen.test.ts</files>
  <read_first>
    src/components/assessment/ResultsScreen.tsx (current props/JSX shape being replaced — do not delete this file in this plan, Plan 02 deletes it)
    src/components/assessment/DeepResultsScreen.tsx (dimension-breakdown rendering pattern being folded in)
    src/lib/assessment.ts (getTierName, getTierDescription, getPreviewBullets, DEFAULT_SECTOR, type Sector)
    src/lib/sectorRecommendations.ts (quickScoreToTierLevel, deepScoreToTierLevel, type TierLevel)
    src/lib/deepAssessment.ts (DIMENSIONS, type Dimension, getDeepTier, getDimensionInterpretation, DEEP_MAX_SCORE)
    src/lib/revenueCalculation.ts (getRevenueOpportunities — created in Task 1 of this plan)
    src/components/shared/TestimonialCard.tsx (props: quote, name, title, headshot)
    src/content/testimonials.json (testimonials.assessmentResults)
    .planning/phases/10-assessment-re-point-enterprise-track/10-UI-SPEC.md (Assessment Results Screen copywriting contract + Calendly Removal table, lines ~115-172)
  </read_first>
  <behavior>
    - assessmentType="quick": renders heading "Your Revenue Opportunities", a revenue amount string containing "~$" and "/mo", 3 automation bullets from getRevenueOpportunities, a CTA `<Link>` with href "/checkout" and text "See Your Purchase Options →", and no element contains the string "calendly".
    - assessmentType="deep" with byDimension provided: renders the same revenue block plus a dimension-breakdown section (6 dimension cards, one per DIMENSIONS key), CTA href "/checkout?assessment=deep" and text "View Your Roadmap →".
    - No "Book a discovery call" or /engage link is rendered in either mode.
  </behavior>
  <action>
    Create src/components/assessment/RevenueResultsScreen.tsx. Props interface: `{ assessmentType: "quick" | "deep"; score: number; byDimension?: Record&lt;Dimension, number&gt;; firstName: string; sector?: Sector }` with `sector = DEFAULT_SECTOR` default.

    Compute tier level: `assessmentType === "deep" ? deepScoreToTierLevel(score) : quickScoreToTierLevel(score)`. Compute revenue via `getRevenueOpportunities(sector, tierLevel)`.

    For tier name/description: quick mode uses `getTierName(score, sector)` / `getTierDescription(score, sector)` / `getPreviewBullets(score, sector)` (same functions ResultsScreen.tsx already calls). Deep mode uses `getDeepTier(score, sector)` which returns `{ name, description, action }`.

    JSX structure (top to bottom): revenue score block (label "Your Revenue Opportunities", amount `~$${totalLow}–$${totalHigh}<span>/mo</span>`, tier name below); description paragraph (tier description from the functions above); automations block (subheading "Your Top 3 Automations", one row per `revenue.opportunities` entry rendering `{name} → ~${lowMonthly}–${highMonthly}/mo ({justification})`); when assessmentType === "deep" and byDimension is provided, render a dimension-breakdown section reusing the DeepResultsScreen.tsx pattern (map `Object.entries(DIMENSIONS)`, per-dimension score/max bar, `getDimensionInterpretation(code, score, sector)`); divider; email banner (quick: title "Your Full Report Is On Its Way", sub referencing `{firstName}'s inbox`; deep: title `Full Scorecard Sent to {firstName}`, sub "Your section-by-section breakdown with recommendations has been sent to your inbox."); CTA block with a single `<Link>` — quick mode href="/checkout" text "See Your Purchase Options →"; deep mode href="/checkout?assessment=deep" text "View Your Roadmap →" — and no secondary link; TestimonialCard rendered only when assessmentType === "quick", using `testimonials.assessmentResults` from src/content/testimonials.json.

    Create RevenueResultsScreen.module.css using the same design-token set as AssessmentSectorGate.module.css (--surface-1/2, --ink-1/2, --accent, --accent-hover, --fontSize-2/3/4/6/7, --spacing-2..6, --radius-2/3) per UI-SPEC's Token Mapping table: revenue amount uses var(--fontSize-7) weight 700 color var(--accent); score block background var(--surface-2); automation rows background var(--surface-1) border var(--surface-3).

    Write tests/test-revenue-results-screen.test.ts as a source-content assertion test verifying the file contains: `getRevenueOpportunities`, `"/checkout"`, `"/checkout?assessment=deep"`, `"See Your Purchase Options"`, `"View Your Roadmap"`, and does NOT contain the substring `calendly`.
  </action>
  <verify>
    <automated>npx vitest run tests/test-revenue-results-screen.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/assessment/RevenueResultsScreen.tsx exports RevenueResultsScreen accepting assessmentType, score, byDimension?, firstName, sector?
    - Source contains no substring "calendly" (case-insensitive check in the test)
    - CTA hrefs are exactly "/checkout" (quick) and "/checkout?assessment=deep" (deep)
    - `npx vitest run tests/test-revenue-results-screen.test.ts` passes
  </acceptance_criteria>
  <done>RevenueResultsScreen renders revenue-framed output with a working /checkout CTA for both quick and deep assessment types, with no Calendly or /engage references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → client component state | AssessmentSectorGate and RevenueResultsScreen are pure client-rendered UI; no server input crosses these boundaries in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | src/lib/revenueCalculation.ts | accept | Pricing heuristic constants ship in the client JS bundle; this is a marketing estimate, not sensitive/PII data — no confidentiality requirement applies |
| T-10-02 | Spoofing | AssessmentSectorGate.tsx Enterprise choice | mitigate | `router.push("/enterprise")` uses a hardcoded literal destination, never a value derived from props or user input — rules out open redirect from this component |
</threat_model>

<verification>
Run `npx vitest run tests/test-revenue-calculation.test.ts tests/test-assessment-sector-gate.test.ts tests/test-revenue-results-screen.test.ts` — all pass. Run `npx tsc --noEmit` — no new type errors from the three new source files.
</verification>

<success_criteria>
- getRevenueOpportunities is a pure, tested function covering all 5 sectors × 4 tiers
- AssessmentSectorGate and RevenueResultsScreen exist, compile, and are ready for Plan 02 to wire into AssessmentShell/DeepAssessmentShell
- No existing file is modified in this plan (net-new files only)
</success_criteria>

<output>
Create `.planning/phases/10-assessment-re-point-enterprise-track/10-01-SUMMARY.md` when done
</output>
