---
phase: 10-assessment-re-point-enterprise-track
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/enterprise/page.tsx
  - src/app/enterprise/enterprise.module.css
  - src/components/home/Footer.tsx
  - src/components/Navigation.tsx
  - tests/test-enterprise-page.test.ts
  - tests/test-enterprise-reachability.test.ts
autonomous: true
requirements: [ENT-01, ENT-02]

must_haves:
  truths:
    - "A visitor can reach the enterprise track page and see all four regulated verticals (Law, Finance, Real Estate & Property, Construction), a case-study proof section, a service-tiers section, and an intake CTA"
    - "The enterprise track is reachable from the footer and from a secondary (non-primary) nav slot, and does not appear as the homepage's primary CTA"
  artifacts:
    - path: "src/app/enterprise/page.tsx"
      provides: "enterprise track landing page"
      exports: ["default", "metadata"]
    - path: "src/app/enterprise/enterprise.module.css"
      provides: "enterprise page styling using Phase 6 design tokens"
  key_links:
    - from: "src/components/home/Footer.tsx"
      to: "/enterprise"
      via: "Link href"
      pattern: "href=\"/enterprise\""
    - from: "src/components/Navigation.tsx"
      to: "/enterprise"
      via: "Link href"
      pattern: "href=\"/enterprise\""
---

<objective>
Give the four regulated verticals (Law, Finance, Real Estate & Property, Construction), the existing case studies, and the `/services` enterprise tiers a home outside the primary SMB path: a new `/enterprise` page (ENT-01), reachable only from the footer and a secondary nav slot — never the primary nav CTA or homepage (ENT-02).

Purpose: preserve the existing high-ticket enterprise relationships and content without cluttering the SMB-first primary path re-pointed by `10-02-PLAN.md`.
Output: `src/app/enterprise/page.tsx` (+ CSS module), and one-line additions to `Footer.tsx` and `Navigation.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-assessment-re-point-enterprise-track/10-RESEARCH.md
@.planning/phases/10-assessment-re-point-enterprise-track/10-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enterprise track page</name>
  <files>src/app/enterprise/page.tsx, src/app/enterprise/enterprise.module.css, tests/test-enterprise-page.test.ts</files>
  <read_first>
    src/app/services/page.tsx (serviceTiers data shape, page structure being summarized here — read-only, not modified)
    src/app/work/page.tsx (caseStudies data shape — read-only, not modified)
    src/lib/sectorRecommendations.ts (Law/Finance/Real Estate & Property/Construction paragraph content — source material for vertical card descriptions, read-only)
    src/app/globals.css (design tokens: --surface-1/2/3, --ink-1/2, --accent, --accent-hover, --fontSize-3/4/6/7, --spacing-2..6, --radius-2/3, .container, .section-padding classes)
    .planning/phases/10-assessment-re-point-enterprise-track/10-UI-SPEC.md (Enterprise Track Page copywriting contract + layout, lines ~144-240)
  </read_first>
  <behavior>
    - Page renders 4 vertical cards, one each for Law, Finance, Real Estate & Property, Construction, each with a regulation label and a CTA link.
    - Page renders a case-studies section summarizing at least 2 of the 4 entries from src/app/work/page.tsx's caseStudies array, each linking to /work for full detail.
    - Page renders a services-tiers section summarizing all 4 entries from src/app/services/page.tsx's serviceTiers array (name + price only), each linking to /services for full detail.
    - Page renders one engagement CTA linking to /engage?enterprise=true.
  </behavior>
  <action>
    Create src/app/enterprise/page.tsx as a server component. Export `metadata = { title: "Enterprise AI Automation Solutions | Asor Ahura", description: "Custom scoping for regulated industries with compliance-aware implementation." }`.

    Define a module-private const ENTERPRISE_VERTICALS array with exactly 4 entries, each `{ id, name, regulation, description, ctaHref }`:
    - `{ id: "law", name: "Law", regulation: "ABA Rule 1.6 Compliance", description: "AI-assisted legal workflows with privilege protection and malpractice safeguards.", ctaHref: "/engage?enterprise=true&sector=law" }`
    - `{ id: "finance", name: "Finance", regulation: "SR 11-7 / EU AI Act", description: "Model risk management, governance-first AI integration, regulatory-ready deployment.", ctaHref: "/engage?enterprise=true&sector=finance" }`
    - `{ id: "realestate", name: "Real Estate & Property", regulation: "Fair Housing / HUD 2024", description: "Bias-aware automation, tenant compliance, protected-class safeguards.", ctaHref: "/engage?enterprise=true&sector=realestate" }`
    - `{ id: "construction", name: "Construction", regulation: "Industry Standards (Procore/ACC)", description: "Project automation, compliance-aware reporting, crew coordination.", ctaHref: "/engage?enterprise=true&sector=construction" }`

    Page sections, top to bottom: (1) hero — heading "Enterprise AI Automation Solutions", tagline "Custom scoping for regulated industries with compliance-aware implementation"; (2) verticals section — heading "Industry Expertise", grid of 4 cards from ENTERPRISE_VERTICALS, each rendering name + `(regulation)`, description, and a `<Link href={vertical.ctaHref}>Discuss Your Challenges →</Link>`; (3) case studies section — heading "What We've Built", render the `headline` field from the first 2 entries of a locally-defined summary (do not import from src/app/work/page.tsx — its caseStudies const is not exported; instead transcribe the 2 headline strings for "2,000+ resumes screened..." and "16,454 chatbot conversations analyzed..." as a local array of `{ headline: string }`), each followed by a single shared `<Link href="/work">See all case studies →</Link>` below the two summaries (not per-card); (4) services tiers section — heading "Our Enterprise Tiers", render a local array transcribing the 4 tier names/prices from src/app/services/page.tsx ("Starter Automation" $5,000 / "Operational Automation" $5,000–$15,000 / "Systems Integration" $15,000–$30,000 / "Enterprise / Complex Build" $30,000+) as compact cards, each linking to `/services`; (5) engagement CTA section — heading "Schedule a Compliance-Aware Discovery Call", subtext "Tell us about your challenges. We'll map out a custom scoping and timeline.", `<Link href="/engage?enterprise=true">Discuss Your Needs →</Link>`.

    Create src/app/enterprise/enterprise.module.css using design tokens per UI-SPEC: page background var(--surface-1); alternating sections use var(--surface-2); vertical/tier/case-study cards use var(--surface-1) background with 1px solid var(--surface-3) border, var(--radius-2); headings use var(--fontSize-6) for section headings, var(--fontSize-7) for hero; CTA links use var(--accent) color with var(--accent-hover) on hover. Reuse the global `.container` class from src/app/globals.css for horizontal centering (do not redefine `.container` inside this module).

    Write tests/test-enterprise-page.test.ts (readFileSync source-assertion style, matching tests/test-automate-catalog-page.test.ts): assert metadata.title equals "Enterprise AI Automation Solutions | Asor Ahura"; assert the page source contains all 4 strings "Law", "Finance", "Real Estate & Property", "Construction"; assert it contains "/engage?enterprise=true"; assert it contains "/services" and "/work".
  </action>
  <verify>
    <automated>npx vitest run tests/test-enterprise-page.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/app/enterprise/page.tsx default-exports a server component and exports `metadata`
    - Page source contains all 4 regulated vertical names and links to /services, /work, and /engage?enterprise=true
    - `npx vitest run tests/test-enterprise-page.test.ts` passes
    - `npx tsc --noEmit` reports no new errors from this file
  </acceptance_criteria>
  <done>/enterprise renders the four verticals, case-study proof, service tiers, and an intake CTA, per ENT-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Footer + secondary nav reachability</name>
  <files>src/components/home/Footer.tsx, src/components/Navigation.tsx, tests/test-enterprise-reachability.test.ts</files>
  <read_first>
    src/components/home/Footer.tsx (current 3-column nav list — Home, Discovery, Engage, Work)
    src/components/Navigation.module.css (.links, .cta, .active class names — existing dark hex palette, matched not replaced)
    src/components/Navigation.tsx (current primary links: Services, Work, Discovery, Blog + one .cta button "Start AI Opportunity Discovery")
  </read_first>
  <behavior>
    - Footer's Navigation column includes a link to /enterprise labeled "Enterprise", positioned after "Discovery" and before "Engage".
    - Navigation.tsx renders a link to /enterprise labeled "Enterprise", visually distinct from (smaller/less prominent than) the primary "Start AI Opportunity Discovery" CTA button — it must not use the `.cta` class or replace the primary CTA.
  </behavior>
  <action>
    In src/components/home/Footer.tsx, inside the `<nav className={styles.nav}>` `<ul className={styles.navList}>`, add `<li><Link href="/enterprise" className={styles.navLink}>Enterprise</Link></li>` immediately after the existing `<li><Link href="/assessment" ...>Discovery</Link></li>` entry and before the `<li><Link href="/engage" ...>Engage</Link></li>` entry. Match the existing `styles.navLink` class — do not introduce a new CSS class or modify Footer.module.css.

    In src/components/Navigation.tsx, add a secondary link to /enterprise in both the desktop `<div className={styles.links}>` block and the `<div className={styles.mobileMenu}>` block. Desktop: place it as a plain `<Link href="/enterprise" className={pathname.startsWith("/enterprise") ? styles.active : ""}>Enterprise</Link>` immediately before the existing primary CTA link (`Start AI Opportunity Discovery`) — do NOT give it the `.cta` class, so it renders as a normal nav link, visually secondary to the CTA button. Mobile: add the matching `<Link href="/enterprise" className={pathname.startsWith("/enterprise") ? styles.mobileActive : ""} onClick={() => setMenuOpen(false)}>Enterprise</Link>` immediately before the existing mobile CTA link. Do not modify Navigation.module.css — the existing `.links a` / `.mobileMenu a` styles already render plain links as visually secondary to `.cta`.

    Write tests/test-enterprise-reachability.test.ts (readFileSync source-assertion style): assert src/components/home/Footer.tsx contains `href="/enterprise"`; assert src/components/Navigation.tsx contains `href="/enterprise"` exactly twice (desktop + mobile) and does not attach the `.cta`/`styles.cta` class to either enterprise link (assert the substring `styles.cta}>Enterprise` and `mobileCta}>Enterprise` are both absent).
  </action>
  <verify>
    <automated>npx vitest run tests/test-enterprise-reachability.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Footer.tsx contains exactly one `href="/enterprise"` link inside the Navigation column
    - Navigation.tsx contains exactly two `href="/enterprise"` links (desktop + mobile), neither styled with the `.cta`/`.mobileCta` class
    - `npx vitest run tests/test-enterprise-reachability.test.ts` passes
  </acceptance_criteria>
  <done>/enterprise is reachable from the footer nav column and from a secondary (non-CTA) slot in the primary site navigation, per ENT-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → static marketing content | /enterprise, Footer, and Navigation render no user input; all links are hardcoded route literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Spoofing | src/app/enterprise/page.tsx outbound links (/engage, /services, /work) | mitigate | Every href is a hardcoded string literal defined in ENTERPRISE_VERTICALS or inline JSX — none are constructed from query params, props, or any user-controlled value, ruling out link/redirect tampering |
| T-10-07 | Information Disclosure | /enterprise page content | accept | Public marketing content only (case study summaries already public at /work, service tier pricing already public at /services) — no new sensitive data introduced |
</threat_model>

<verification>
Run `npx vitest run tests/test-enterprise-page.test.ts tests/test-enterprise-reachability.test.ts` — all pass. Manually verify in dev: visiting `/enterprise` renders all four verticals; the Enterprise link is visible in the footer and in the top nav (not as the gold CTA button); the homepage itself does not link to `/enterprise` from its primary hero CTA.
</verification>

<success_criteria>
- ENT-01: /enterprise carries case studies, all 4 regulated verticals, an /engage intake CTA, and /services tier summaries
- ENT-02: /enterprise is reachable only from the footer and a secondary nav link — never the primary CTA
</success_criteria>

<output>
Create `.planning/phases/10-assessment-re-point-enterprise-track/10-03-SUMMARY.md` when done
</output>
