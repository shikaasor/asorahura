---
phase: 10-assessment-re-point-enterprise-track
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/assessment/deep/actions.ts
  - src/lib/email.ts
  - src/lib/prompts.ts
  - src/lib/pdf.ts
  - src/emails/AssessmentReport.tsx
  - tests/test-calendly-removal-emails.test.ts
autonomous: true
requirements: [ASSESS-15]

must_haves:
  truths:
    - "The deep assessment confirmation email links to the checkout purchase page, not Calendly"
    - "The initial assessment follow-up email links to the checkout purchase page, not Calendly"
    - "The assessment PDF's personalized next-step text for warm and hot segments points at the checkout purchase page, not Calendly"
    - "The LLM email-sequence prompt instructions direct the model toward a checkout CTA instead of a Calendly booking CTA for warm/hot segments"
    - "The AssessmentReport email template's CTA button links to the checkout page, not Calendly"
  artifacts:
    - path: "src/lib/prompts.ts"
      provides: "SEGMENT_CTA pointing at /checkout for warm/hot segments"
  key_links:
    - from: "src/lib/email.ts"
      to: "/checkout"
      via: "template string"
      pattern: "/checkout"
    - from: "src/emails/AssessmentReport.tsx"
      to: "/checkout"
      via: "Button href"
      pattern: "/checkout"
---

<objective>
Complete ASSESS-15's Calendly removal in the parts of the codebase RESEARCH.md flags as "supporting references" — the deep-assessment confirmation email, the primary assessment email sequence, the PDF report's next-step copy, the LLM prompt instructions that generate nurture emails, and the React-Email results template. These are the last hardcoded/instructed Calendly touchpoints in the assessment funnel not covered by `10-02-PLAN.md` (the live results screen) or `10-04-PLAN.md` (services/checkout/engage pages).

Per RESEARCH.md Pitfall 3, grepping for the literal `calendly.com` URL alone is insufficient here — the LLM-facing prompt instructions in `src/lib/prompts.ts` reference Calendly as *guidance text fed to the model*, not as a rendered link, so they must be edited even though they never appear in a browser.

Purpose: make ASSESS-15 fully true across every email/PDF artifact the assessment funnel produces, not just the live web pages.
Output: updated assessment/deep/actions.ts, email.ts, prompts.ts, pdf.ts, AssessmentReport.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-assessment-re-point-enterprise-track/10-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deep-assessment email + initial follow-up email CTA</name>
  <files>src/app/assessment/deep/actions.ts, src/lib/email.ts, tests/test-calendly-removal-emails.test.ts</files>
  <read_first>
    src/app/assessment/deep/actions.ts (inline HTML email, CTA `&lt;a href="https://calendly.com/asorahura"&gt;` around line 54)
    src/lib/email.ts (BASE_URL constant, CALENDLY_URL constant at line 11 — keep this export in place, it is still used by src/app/automate/instagram/success/page.tsx outside this phase's scope; only change its usage inside sendAssessmentEmailSequence's initial email at line 223)
  </read_first>
  <behavior>
    - The deep assessment confirmation email HTML no longer contains an anchor to calendly.com; it contains an anchor to `${BASE_URL}/checkout?assessment=deep` with the label "View Your Roadmap".
    - sendAssessmentEmailSequence's initial email body no longer appends "Book a call: ${CALENDLY_URL}"; it appends "See your purchase options: ${BASE_URL}/checkout" instead.
  </behavior>
  <action>
    In src/app/assessment/deep/actions.ts: replace the anchor tag currently reading `href="https://calendly.com/asorahura"` with label "Book a Discovery Call" so it instead reads `href="${BASE_URL}/checkout?assessment=deep"` with label "View Your Roadmap" (keep the existing inline style attributes unchanged). Add `const BASE_URL = process.env.NEXT_PUBLIC_BASE_URL ?? 'https://asorahura.vercel.app';` near the top of this file (it does not currently define one) and use it in the template literal.

    In src/lib/email.ts: inside `sendAssessmentEmailSequence`, change the appended body line from `Book a call: ${CALENDLY_URL}` to `See your purchase options: ${BASE_URL}/checkout` (BASE_URL is already defined at the top of this file). Do not remove the `CALENDLY_URL` export itself or its other usages elsewhere in the file — only this one call site inside `sendAssessmentEmailSequence` changes.

    Write tests/test-calendly-removal-emails.test.ts (readFileSync source-assertion style): assert src/app/assessment/deep/actions.ts does NOT contain "calendly" and contains "/checkout?assessment=deep"; assert that within src/lib/email.ts, the specific substring "Book a call:" does not exist and the file contains "See your purchase options:".
  </action>
  <verify>
    <automated>npx vitest run tests/test-calendly-removal-emails.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - assessment/deep/actions.ts contains zero occurrences of "calendly"
    - email.ts's sendAssessmentEmailSequence initial-email body no longer contains "Book a call:"
    - `npx vitest run tests/test-calendly-removal-emails.test.ts` passes
  </acceptance_criteria>
  <done>Both the deep-assessment confirmation email and the primary assessment's initial follow-up email route recipients to /checkout instead of Calendly.</done>
</task>

<task type="auto">
  <name>Task 2: LLM prompt instructions + PDF next-step copy</name>
  <files>src/lib/prompts.ts, src/lib/pdf.ts</files>
  <read_first>
    src/lib/prompts.ts (SEGMENT_CTA record, segmentAngle function, nurtureSegmentAngle function — all three currently instruct the LLM to push a Calendly/"strategy session" CTA for warm/hot segments)
    src/lib/pdf.ts (lines ~140-164 — the `nextStep` string built per segment inside the PDF generator, "warm" and "hot" branches currently reference booking a call)
  </read_first>
  <action>
    In src/lib/prompts.ts:
    - Change `SEGMENT_CTA.warm` from `"https://calendly.com/asorahura"` to `"https://asorahura.com/checkout"`.
    - Change `SEGMENT_CTA.hot` from `"https://calendly.com/asorahura"` to `"https://asorahura.com/checkout"`.
    - In `segmentAngle("hot")`'s returned string, replace the "Primary CTA is direct: https://calendly.com/asorahura." sentence (through "Frame the strategy session as the cheapest, fastest path to a working system.") with "Primary CTA is direct: https://asorahura.com/checkout. No free options, no soft asks, no 'learn more'. Frame the purchase as the cheapest, fastest path to a working system."
    - In `nurtureSegmentAngle("warm")`'s returned string, replace the phrase "CTA to book a discovery call at https://calendly.com/asorahura — frame it as 'the conversation that costs nothing but reveals everything'." with "CTA to see purchase options at https://asorahura.com/checkout — frame it as 'the fastest path from where you are to a working system'."
    - In `nurtureSegmentAngle("hot")`'s returned string, replace "Direct CTA to a strategy session at https://calendly.com/asorahura." with "Direct CTA to purchase at https://asorahura.com/checkout."
    - In the Day 14 email-arc instructions inside `nurtureSequencePrompt`'s user text, the 3-step walkthrough currently reads "discovery call → fixed-scope build → handoff" — leave this specific 3-step description unchanged (it describes the DFY/DWY engagement process generically, not a literal Calendly link) since `${offerCta}` (now pointing at `/checkout`) is the only actual link injected.

    In src/lib/pdf.ts: change the `warm` branch's `nextStep` string from "Book a Discovery Call — see how others at your readiness level achieved 40–60% reduction in manual effort. Visit asorahura.com to schedule." to "See Your Purchase Options — see how others at your readiness level achieved 40–60% reduction in manual effort. Visit asorahura.com/checkout to get started." Change the `else` (hot) branch's `nextStep` string from "You're ready to build. Book a Strategy Session to define your exact build scope: calendly.com/asorahura" to "You're ready to build. See your purchase options and get started today: asorahura.com/checkout"
  </action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const files=['src/lib/prompts.ts','src/lib/pdf.ts'];const bad=files.filter(f=>fs.readFileSync(f,'utf8').toLowerCase().includes('calendly.com/asorahura'));if(bad.length){console.error('still references calendly:',bad);process.exit(1);}console.log('clean');"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/prompts.ts's SEGMENT_CTA.warm and SEGMENT_CTA.hot both equal "https://asorahura.com/checkout"
    - src/lib/prompts.ts contains zero occurrences of "calendly.com/asorahura"
    - src/lib/pdf.ts's warm and hot nextStep strings both reference "asorahura.com/checkout" and contain zero occurrences of "calendly.com/asorahura"
    - The Task 2 verify node command exits 0
  </acceptance_criteria>
  <done>Every LLM prompt instruction and PDF next-step string that previously pointed the reader at Calendly now points at /checkout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AssessmentReport email template CTA</name>
  <files>src/emails/AssessmentReport.tsx</files>
  <read_first>
    src/emails/AssessmentReport.tsx (BASE_URL constant at top of file, second Button element at lines ~97-109 currently hrefs calendly.com)
  </read_first>
  <behavior>
    - The second Button in AssessmentReport hrefs `${BASE_URL}/checkout` and its label text is "See Your Purchase Options" (not "Book a Discovery Call").
  </behavior>
  <action>
    In src/emails/AssessmentReport.tsx, change the second `Button` element's `href="https://calendly.com/asorahura"` to `href={`${BASE_URL}/checkout`}` and its label from "Book a Discovery Call" to "See Your Purchase Options", keeping the existing style object (`backgroundColor: "#111"`, etc.) unchanged — only the `href` value and the button label text change.
  </action>
  <verify>
    <automated>npx vitest run tests/test-calendly-removal-emails.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - AssessmentReport.tsx contains zero occurrences of "calendly"
    - AssessmentReport.tsx's second Button hrefs `${BASE_URL}/checkout`
  </acceptance_criteria>
  <done>The React-Email assessment report template's CTA routes recipients to /checkout, matching every other primary-path touchpoint updated in this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Server (LLM output) → outbound email | LLM-generated email bodies are sent via Resend without being parsed back into server-side redirect logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Tampering | src/lib/prompts.ts CTA URLs embedded in LLM prompt instructions | mitigate | CTA URL strings remain hardcoded literals (`https://asorahura.com/checkout`) inside the prompt text sent to the model; the model's free-text output is only ever used as an email body/subject via Resend — no server-side code parses or acts on LLM output as a redirect target or executable instruction, so prompt injection here cannot achieve an open redirect |
| T-10-11 | Information Disclosure | PDF/email CTA URLs | accept | All URLs are public marketing pages; no sensitive data is newly exposed by this change |
</threat_model>

<verification>
Run `npx vitest run tests/test-calendly-removal-emails.test.ts` and the Task 2 node one-liner — all pass. Grep the full `src/` tree for `calendly` and confirm the only remaining hit is `src/lib/email.ts`'s `CALENDLY_URL` constant definition and its use in `src/app/automate/instagram/success/page.tsx` (explicitly out of scope for this phase per RESEARCH.md).
</verification>

<success_criteria>
- ASSESS-15: no assessment-funnel email, PDF, or LLM-prompt instruction references Calendly; every one now points at /checkout
</success_criteria>

<output>
Create `.planning/phases/10-assessment-re-point-enterprise-track/10-05-SUMMARY.md` when done
</output>
