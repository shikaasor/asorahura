---
phase: 04-navigation-content-polish-site-wide-cta-discipline-finish-content-trust-signals
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/articles.ts
autonomous: true
requirements:
  - CONTENT-04
  - CONTENT-06
  - CONV-05

must_haves:
  truths:
    - "No EU HORIZON grant references appear anywhere in src/lib/articles.ts"
    - "The metrics string for the health facilities article no longer contains 'EU HORIZON €2.25M'"
    - "The EU HORIZON grant paragraph is removed from the article body array"
    - "No social media exit links (LinkedIn, YouTube, Twitter) appear in any conversion page (assessment, results, engage, checkout)"
    - "Articles content reads in executive language — outcomes and business impact lead, not technical implementation"
  artifacts:
    - path: "src/lib/articles.ts"
      provides: "Case study articles with EU Horizon removed and executive-language copy"
      contains: "39+ Facilities"
  key_links:
    - from: "src/lib/articles.ts"
      to: "blog MDX content"
      via: "articles data used by blog pages"
      pattern: "articles"
---

<objective>
Remove EU Horizon grant references from articles.ts (CONTENT-04), audit conversion pages for social media exit links (CONV-05), and verify/improve executive language in article content (CONTENT-06).

Purpose: EU Horizon references undermine the solo expert positioning by associating Asor with grant-seeking rather than direct business outcomes. This is a pure content surgery — no component changes needed.

Output: articles.ts with EU Horizon strings deleted. Conversion pages verified clean of social media links. Article copy reviewed for executive-language compliance.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/lib/articles.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove EU Horizon references from articles.ts and verify executive language</name>
  <files>src/lib/articles.ts</files>
  <action>
Read articles.ts in full first, then make these targeted changes:

**EU Horizon removal (CONTENT-04):**

Line 22 — metrics string fix:
- BEFORE: `metrics: "39+ Facilities | EU HORIZON €2.25M | Zero Cloud Dependency"`
- AFTER: `metrics: "39+ Facilities | Zero Cloud Dependency"`

Line 66 — body array element removal:
- Find the paragraph: `"This project is currently being positioned for an EU HORIZON grant of up to €2.25 million under the EDCTP3 Global Health partnership. The call is specifically for AI-driven health innovation in sub-Saharan Africa."`
- Delete this string from the body array entirely. Ensure the surrounding array elements remain valid (check for trailing comma issues after deletion).

**Executive language review (CONTENT-06):**

After the EU Horizon removal, read through the other article body arrays. The goal is outcome-first, executive language — business results lead, not technical architecture. Check specifically:

1. Any sentence starting with "We used..." or "The technical approach was..." — reframe to lead with the business outcome the approach enabled.
2. Any mention of specific model version numbers (GPT-4o, Claude 3.5, etc.) in body text — keep in stack fields but remove from narrative body where they distract from outcome.
3. The overall tone should read as a case study a COO would find compelling, not a technical blog post a developer would write.

Make targeted copy improvements to article body paragraphs where needed. The headlines (metric-first) are already strong per Phase 02/03 work — focus on body paragraphs.

Specific check: verify the `/blog` slug versions of articles match the executive-language standard (these come from the MDX files in `/content/blog/`, not articles.ts — but note any inconsistencies to flag in the summary).

Do NOT change: article IDs, slugs, metrics fields (except the EU Horizon removal), stack fields, or headline format.
  </action>
  <verify>Run `grep -r "EU HORIZON" src/lib/articles.ts` — must return no results. Run `grep -r "EDCTP" src/lib/articles.ts` — must return no results. Run `npx tsc --noEmit` — no errors. Read through the article body copy to confirm outcome-first language.</verify>
  <done>articles.ts contains no EU Horizon or EDCTP references. All body paragraphs lead with business outcomes. `npx tsc --noEmit` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Audit conversion pages for social media exit links (CONV-05)</name>
  <files>src/lib/articles.ts</files>
  <action>
This task is a verification audit, not a build task. Run grep searches across conversion page files to confirm CONV-05 compliance (no social media exit links on conversion pages).

Conversion pages to check: assessment, results (ResultsScreen), engage, checkout.

Run these checks using Bash:
```bash
# Check for social media links in conversion pages
grep -r "linkedin\|youtube\|twitter\|instagram\|tiktok" \
  src/app/assessment/ \
  src/app/engage/ \
  src/app/checkout/ \
  src/components/assessment/ResultsScreen.tsx \
  --include="*.tsx" --include="*.ts" -i
```

**If any social media links are found in these files:** Remove them. Social links should only appear in Footer.tsx.

**If no social media links are found:** Document in the summary that CONV-05 is confirmed compliant — social links verified absent from all conversion pages.

Also check the HomePage (src/app/page.tsx) and its child components for social media feeds:
```bash
grep -r "linkedin\|youtube\|LinkedInFeed\|YouTubeFeed" \
  src/app/page.tsx \
  src/components/home/ \
  --include="*.tsx" -i
```

Per research: LinkedInFeed and YouTubeFeed are NOT imported on any conversion page — verify this is still true and note the result.

Note: This task lists `src/lib/articles.ts` in files_modified as a placeholder to satisfy plan structure, but the file may not need modification if the audit finds no violations. If removals ARE needed in conversion pages, add those files to the actual change set and note in the SUMMARY.
  </action>
  <verify>Grep returns zero results for social media links in src/app/assessment/, src/app/engage/, src/app/checkout/, and src/components/assessment/ResultsScreen.tsx. Document result in summary.</verify>
  <done>CONV-05 audit complete. Either: (a) no social media exit links found in conversion pages — fully compliant, or (b) found links were removed and files were updated. Result documented in phase summary.</done>
</task>

</tasks>

<verification>
1. `grep -r "EU HORIZON\|EDCTP\|Horizon grant" src/lib/articles.ts` — returns empty
2. `grep -r "EU HORIZON\|EDCTP" src/` — returns empty across the entire src directory
3. `npx tsc --noEmit` — zero errors
4. Conversion pages audit: zero social media links in assessment/, engage/, checkout/, ResultsScreen.tsx
5. Article body paragraphs reviewed for executive language (outcome-first, business impact leads)
</verification>

<success_criteria>
- Zero EU Horizon or EDCTP mentions in any source file
- articles.ts metrics string for health facilities article: "39+ Facilities | Zero Cloud Dependency"
- All article body paragraphs lead with business outcomes (no technical-first narrative)
- Conversion pages contain no social media exit links
- `npx tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-navigation-content-polish-site-wide-cta-discipline-finish-content-trust-signals/04-05-SUMMARY.md` using the summary template.
</output>
