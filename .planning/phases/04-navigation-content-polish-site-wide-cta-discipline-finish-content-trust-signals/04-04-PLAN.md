---
phase: 04-navigation-content-polish-site-wide-cta-discipline-finish-content-trust-signals
plan: "04"
type: execute
wave: 2
depends_on:
  - "04-02"
files_modified:
  - src/app/work/page.tsx
  - src/app/engage/page.tsx
  - src/components/assessment/ResultsScreen.tsx
autonomous: true
requirements:
  - NAV-03
  - NAV-05
  - NAV-06
  - CONTENT-05
  - CONV-05

must_haves:
  truths:
    - "Work page has at most 2 page-level CTAs: 'Work With Me' primary button and a secondary (Assessment or none)"
    - "Work page per-card 'See if we're a fit →' standalone links are removed (they count as page-level CTAs per the locked definition)"
    - "Engage page has no Testimonials component rendered"
    - "Assessment Results page has 1 TestimonialCard below the opportunities block and before the CTA"
    - "No social media exit links appear on Work, Engage, or Assessment Results pages"
  artifacts:
    - path: "src/app/work/page.tsx"
      provides: "Work page with 2-CTA discipline: bottom CTA button primary, per-card links removed"
      contains: "Work With Me"
    - path: "src/app/engage/page.tsx"
      provides: "Engage page with Testimonials component removed"
    - path: "src/components/assessment/ResultsScreen.tsx"
      provides: "Results screen with 1 TestimonialCard above the CTA block"
      contains: "TestimonialCard"
  key_links:
    - from: "ResultsScreen.tsx"
      to: "TestimonialCard"
      via: "import"
      pattern: "TestimonialCard"
    - from: "work/page.tsx bottom CTA"
      to: "/engage"
      via: "Link href"
      pattern: "href=\"/engage\""
---

<objective>
Apply 2-CTA discipline to the Work page (remove per-card links, keep bottom primary CTA), remove the Testimonials component from the Engage page (CONTENT-05), and add 1 TestimonialCard to the Assessment Results screen below the opportunities block.

Purpose: Work page violates the 2-CTA rule because each case study card has a standalone "See if we're a fit →" link (4 cards = 4 standalone links per the locked definition). Engage page has the old white Testimonials component that was supposed to be moved (CONTENT-05). Assessment Results needs 1 testimonial to build trust at the highest-intent moment in the funnel.

Output: Work page with 1 bottom CTA button only. Engage page clean of Testimonials. ResultsScreen with 1 dark TestimonialCard placed above the CTA.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/app/work/page.tsx
@src/app/engage/page.tsx
@src/components/assessment/ResultsScreen.tsx
@src/components/assessment/ResultsScreen.module.css
@src/components/shared/TestimonialCard.tsx
@.planning/phases/04-navigation-content-polish-site-wide-cta-discipline-finish-content-trust-signals/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Work page CTA surgery — remove per-card links, rename bottom CTA to "Work With Me"</name>
  <files>src/app/work/page.tsx</files>
  <action>
Work page currently has:
- Per-card: `<div className={styles.caseCta}><Link href="/engage">See if we're a fit &rarr;</Link></div>` on all 4 case study cards
- Bottom section: `<Link href="/engage" className={styles.bottomCtaBtn}>Tell Me About Your Problem →</Link>`

Per the locked CTA definition (standalone text links count), the 4 per-card links are 4 separate page-level CTAs. Remove them.

Changes:
1. Delete the entire `<div className={styles.caseCta}>...</div>` block from inside the caseStudies map. The case study cards should show headline, clientContext, whatWeBuilt, businessImpact, and stack — no CTA link.

2. Rename the bottom CTA button text from "Tell Me About Your Problem →" to "Work With Me →" to match the locked decision (NAV-03: "Work With Me" CTA on Work page).
   - BEFORE: `Tell Me About Your Problem →`
   - AFTER: `Work With Me →`
   - Keep the same href (`/engage`) and className.

3. Optionally add a second CTA in the bottom section — "Take the Assessment" as a secondary plain text link — to comply with NAV-06 (every page has a primary CTA routing to next step, plus a secondary):
   ```tsx
   <Link href="/engage" className={styles.bottomCtaBtn}>Work With Me →</Link>
   <Link href="/assessment" className={styles.bottomCtaSecondary}>Take the Assessment</Link>
   ```
   Add `.bottomCtaSecondary` as a CSS class inline in the page (or in work.module.css — but since work.module.css is not in files_modified for this task, keep it simple with a style prop):
   ```tsx
   <Link href="/assessment" style={{ color: '#a3a3a3', fontSize: '0.875rem', textDecoration: 'underline', marginTop: '0.75rem', display: 'block', textAlign: 'center' }}>
     Or take the AI Readiness Assessment first
   </Link>
   ```
   This secondary link is understated (small, muted) — not competing with the primary. It serves as the NAV-06 secondary route.

Verify the `src/app/work/work.module.css` still has `.caseCta` if used elsewhere (it won't be — safe to leave it as dead CSS, do not delete).
  </action>
  <verify>Run `npx tsc --noEmit`. Open /work — verify: no "See if we're a fit" links on any case study card. Bottom section shows "Work With Me →" button. Secondary assessment link visible below in muted text.</verify>
  <done>Work page: 4 case study cards with no per-card CTA links. Bottom section: 1 "Work With Me →" primary button + 1 "Or take the AI Readiness Assessment first" secondary link. Exactly 2 page-level CTAs.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Testimonials from Engage page + add TestimonialCard to Assessment Results</name>
  <files>
    src/app/engage/page.tsx
    src/components/assessment/ResultsScreen.tsx
  </files>
  <action>
**In engage/page.tsx:**

1. Remove `import Testimonials from "@/components/Testimonials"` from the import list.
2. Remove the `<Testimonials />` JSX element from inside `EngageFormInner` (it appears just before the form wrapper div). Delete that line.
3. Also remove the `style={{ marginTop: "4rem" }}` inline style on the formWrapper div if it was only there to create spacing after the Testimonials component — or keep it, as some spacing is still appropriate. Keep it for spacing.

The form itself (Submit button) remains as the single functional CTA on the page. No other CTAs should be present. The page is compliant after Testimonials is removed.

**In ResultsScreen.tsx:**

1. Add import at the top:
   ```typescript
   import { TestimonialCard } from '@/components/shared/TestimonialCard';
   ```

2. Add a constant just above the component function (or inside — prefer top-of-file):
   ```typescript
   // Placeholder — Asor replaces before launch
   const RESULTS_TESTIMONIAL = {
     quote: "Placeholder quote — Asor to supply.",
     name: "Client Name",
     title: "Title, Company",
     headshot: "/images/testimonials/placeholder.jpg",
   };
   ```

3. Place `<TestimonialCard {...RESULTS_TESTIMONIAL} />` between the `.opportunities` section and the `.divider`:
   ```tsx
   </div> {/* end opportunities */}

   <TestimonialCard {...RESULTS_TESTIMONIAL} />

   <div className={styles.divider} />
   ```

   This places the testimonial at the highest-trust moment: after the user sees their score and opportunities, before they decide to act. It creates social proof exactly when hesitation might peak.

Note: ResultsScreen is rendered inside AssessmentShell which is a client component. Importing a Server Component (TestimonialCard has no "use client") into a client component is fine in Next.js App Router — the client boundary is the `"use client"` directive on the importing component, not the imported one.
  </action>
  <verify>Run `npx tsc --noEmit`. Open /engage — no Testimonials section visible. Open /assessment, complete to results screen — verify 1 dark TestimonialCard appears between the opportunities list and the divider above the CTA.</verify>
  <done>Engage page: Testimonials component removed, form is the only CTA. ResultsScreen: 1 TestimonialCard with placeholder data renders between opportunities and the divider. Zero TypeScript errors.</done>
</task>

</tasks>

<verification>
1. Work page: zero "See if we're a fit" links (grep `fit →` in work/page.tsx should return nothing)
2. Work page: bottom section shows "Work With Me →" button + secondary assessment link
3. Engage page: no `<Testimonials />` in JSX, no `import Testimonials` in engage/page.tsx
4. ResultsScreen: `<TestimonialCard` present in JSX between `.opportunities` and `.divider`
5. `npx tsc --noEmit` — zero errors
6. No social media links in work/page.tsx, engage/page.tsx, or ResultsScreen.tsx (grep for linkedin, youtube, twitter — should return nothing in these files)
</verification>

<success_criteria>
- Work page: 2 CTAs exactly (Work With Me primary button + assessment secondary link)
- Engage page: Testimonials component fully removed (import + JSX)
- ResultsScreen: 1 TestimonialCard between opportunities block and divider
- Zero social media exit links in these 3 files
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-navigation-content-polish-site-wide-cta-discipline-finish-content-trust-signals/04-04-SUMMARY.md` using the summary template.
</output>
