---
phase: 04-navigation-content-polish-site-wide-cta-discipline-finish-content-trust-signals
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-02"
files_modified:
  - src/components/home/HeroSection.tsx
  - src/app/page.tsx
  - src/app/services/page.tsx
  - src/app/services/services.module.css
  - src/components/home/AboutSection.tsx
autonomous: true
requirements:
  - NAV-03
  - NAV-05
  - NAV-06
  - CONV-03
  - CONV-04
  - CONTENT-05

must_haves:
  truths:
    - "Homepage secondary CTA reads 'Work With Me' and links to /engage (not /checkout)"
    - "Homepage has exactly 2 page-level CTAs: 'Take the Free AI Readiness Assessment' (primary) and 'Work With Me' (secondary)"
    - "Services page has exactly 2 page-level CTAs: a 'Work With Me' primary button and the existing 'Take the Assessment' tier-card links"
    - "The assessment callout block at the bottom of the Services page is removed"
    - "Services page shows pricing transparently (all 4 tier prices visible — already done, no regression)"
    - "BookingUrgency inline text appears near the tier grid on the Services page"
    - "Services page shows 2 TestimonialCard components replacing the old white Testimonials component"
    - "1 TestimonialCard appears in or after the HeroSection on the Homepage"
    - "Old Testimonials import is removed from services/page.tsx"
    - "AboutSection has a 'Work With Me' Link CTA at the bottom, matching the style of HeroSection.tsx secondary CTA"
  artifacts:
    - path: "src/components/home/HeroSection.tsx"
      provides: "Updated hero CTAs + 1 TestimonialCard"
      contains: "Work With Me"
    - path: "src/app/services/page.tsx"
      provides: "Services page with BookingUrgency, 2 TestimonialCards, no assessment callout, 'Work With Me' CTA"
      contains: "BookingUrgency"
    - path: "src/app/services/services.module.css"
      provides: "Styles for new Work With Me CTA button and testimonials layout"
      contains: ".workWithMe"
    - path: "src/components/home/AboutSection.tsx"
      provides: "About section with 'Work With Me' CTA at the bottom"
      contains: "Work With Me"
  key_links:
    - from: "HeroSection.tsx"
      to: "/engage"
      via: "Link href"
      pattern: "href=\"/engage\""
    - from: "services/page.tsx"
      to: "TestimonialCard"
      via: "import"
      pattern: "TestimonialCard"
    - from: "services/page.tsx"
      to: "BookingUrgency"
      via: "import"
      pattern: "BookingUrgency"
    - from: "AboutSection.tsx"
      to: "/engage"
      via: "Link href"
      pattern: "href=\"/engage\""
---

<objective>
Apply CTA discipline to the Homepage and Services page, add TestimonialCard placements, add BookingUrgency to Services, remove the old white Testimonials component from Services, and add a "Work With Me" CTA to AboutSection.

Purpose: Homepage currently has "Book a Discovery Call" pointing to /checkout as secondary CTA — this is wrong per the locked decision (secondary should be "Work With Me" → /engage). Services page has a third CTA (assessment callout block) that violates the 2-CTA rule, and uses white Testimonials cards that break dark-theme consistency. AboutSection has no CTA at all — NAV-03 requires "Work With Me" CTA in hero, Services, Work page, AND About section.

Output: Homepage with correct 2 CTAs + 1 testimonial. Services page with Work With Me CTA, BookingUrgency, 2 dark testimonial cards, no assessment callout, transparent pricing. AboutSection with "Work With Me" secondary CTA at bottom.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/components/home/HeroSection.tsx
@src/components/home/HeroSection.module.css
@src/app/page.tsx
@src/app/services/page.tsx
@src/app/services/services.module.css
@src/components/home/AboutSection.tsx
@src/components/home/AboutSection.module.css
@src/components/shared/TestimonialCard.tsx
@src/components/services/BookingUrgency.tsx
@.planning/phases/04-navigation-content-polish-site-wide-cta-discipline-finish-content-trust-signals/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Homepage CTA pair — replace "Book a Discovery Call" with "Work With Me" + add TestimonialCard</name>
  <files>
    src/components/home/HeroSection.tsx
    src/app/page.tsx
  </files>
  <action>
**In HeroSection.tsx:**

1. Change the secondary CTA button:
   - BEFORE: `<Link href="/checkout" className={styles.secondaryBtn}>Book a Discovery Call</Link>`
   - AFTER: `<Link href="/engage" className={styles.secondaryBtn}>Work With Me</Link>`
   (Exact class name `styles.secondaryBtn` — keep as-is, just change href and label.)

2. Add a TestimonialCard below TrustSignals, still inside the `.copy` div, after `<TrustSignals />`:
   ```tsx
   import { TestimonialCard } from '@/components/shared/TestimonialCard';

   // Placeholder — Asor replaces quote, name, title, and headshot before launch
   const HERO_TESTIMONIAL = {
     quote: "Placeholder quote — Asor to supply before launch.",
     name: "Client Name",
     title: "Title, Company",
     headshot: "/images/testimonials/placeholder.jpg",
   };
   ```
   Render it: `<TestimonialCard {...HERO_TESTIMONIAL} />`

   Place the constant at the top of the file (outside the component function). Import TestimonialCard at the top.

**In src/app/page.tsx:**

The existing `<SocialProof />` section renders 3 testimonial quotes (dark cards, no headshots). It stays. The new TestimonialCard (1 card, with headshot) is placed inside HeroSection, so there is no redundancy concern at the page level — both serve different purposes. No change to page.tsx is needed unless SocialProof must be removed. Per planning decision: keep SocialProof (it shows 3 result-focused quotes) and add 1 TestimonialCard in HeroSection (it shows a personal referral with headshot). This is consistent with the locked decision of "1 testimonial on homepage hero" — the TestimonialCard placement in HeroSection satisfies this. Do NOT remove SocialProof.
  </action>
  <verify>Run `npm run build` or `npx tsc --noEmit`. Open homepage — secondary CTA reads "Work With Me" and links to /engage. A dark testimonial card appears below TrustSignals in the hero with the placeholder text.</verify>
  <done>Homepage has exactly 2 page-level CTAs: "Take the Free AI Readiness Assessment" (primary) and "Work With Me" (secondary, → /engage). 1 TestimonialCard with placeholder data renders below TrustSignals in the hero.</done>
</task>

<task type="auto">
  <name>Task 2: Overhaul Services page — Work With Me CTA, BookingUrgency, dark testimonials, remove assessment callout</name>
  <files>
    src/app/services/page.tsx
    src/app/services/services.module.css
  </files>
  <action>
**In services/page.tsx:**

1. Remove the `import Testimonials from "@/components/Testimonials"` line.

2. Add new imports at the top:
   ```typescript
   import { TestimonialCard } from '@/components/shared/TestimonialCard';
   import { BookingUrgency } from '@/components/services/BookingUrgency';
   ```

3. Add a placeholder testimonials array above the `serviceTiers` array:
   ```typescript
   // Placeholder — Asor replaces before launch
   const SERVICE_TESTIMONIALS = [
     {
       quote: "Placeholder quote 1 — Asor to supply.",
       name: "Client Name A",
       title: "Title, Company A",
       headshot: "/images/testimonials/placeholder.jpg",
     },
     {
       quote: "Placeholder quote 2 — Asor to supply.",
       name: "Client Name B",
       title: "Title, Company B",
       headshot: "/images/testimonials/placeholder.jpg",
     },
   ];
   ```

4. In the hero section (`<section className={styles.hero}>`), add a "Work With Me" primary CTA button after the `<p className={styles.heroSub}>` paragraph:
   ```tsx
   <div className={styles.heroActions}>
     <Link href="/engage" className={styles.workWithMeBtn}>Work With Me</Link>
     <Link href="/assessment" className={styles.heroSecondary}>Take the Assessment</Link>
   </div>
   ```

5. Remove the entire `{/* Assessment callout */}` block:
   ```tsx
   // DELETE this entire block:
   <div className={styles.assessmentCallout}>
     <p className={styles.calloutText}>...</p>
     <Link href="/assessment" className={styles.calloutLink}>Take the Assessment →</Link>
   </div>
   ```

6. Add `<BookingUrgency />` immediately after the closing `</div>` of the tiersGrid:
   ```tsx
   </div> {/* end tiersGrid */}
   <BookingUrgency />
   ```

7. Replace `<Testimonials />` with the two testimonial cards:
   ```tsx
   {/* Testimonials */}
   <section className={styles.testimonialsSection}>
     <div className={styles.testimonialsInner}>
       {SERVICE_TESTIMONIALS.map((t) => (
         <TestimonialCard key={t.name} {...t} />
       ))}
     </div>
   </section>
   ```

**In services/services.module.css:**

Add new CSS classes. Do not change existing tier-related styles:
```css
.heroActions {
  display: flex;
  flex-direction: row;
  gap: 1rem;
  margin-top: 1.5rem;
  flex-wrap: wrap;
  align-items: center;
}

.workWithMeBtn {
  background: #ffffff;
  color: #0a0a0a;
  padding: 0.875rem 2rem;
  border-radius: 8px;
  font-weight: 600;
  font-size: 0.9375rem;
  text-decoration: none;
  transition: opacity 0.15s ease;
}

.workWithMeBtn:hover {
  opacity: 0.85;
}

.heroSecondary {
  color: #ffffff;
  font-size: 0.9375rem;
  text-decoration: underline;
  text-underline-offset: 3px;
  font-weight: 500;
}

.testimonialsSection {
  padding: 4rem 2rem;
  max-width: 1200px;
  margin: 0 auto;
}

.testimonialsInner {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 1.5rem;
}
```

Also remove or comment out the `.assessmentCallout` and `.calloutLink` CSS classes since the block is removed (prevents dead CSS). Do not remove classes used by other elements.
  </action>
  <verify>Run `npx tsc --noEmit`. Open /services — verify: (1) "Work With Me" white button appears in hero, (2) "Take the Assessment" secondary link is present, (3) the old assessment callout block at the bottom is gone, (4) BookingUrgency shows "Currently booking for [Month] — 2 slots remaining" near the tier grid, (5) two dark TestimonialCard cards appear where Testimonials used to be, (6) no white card testimonials remain.</verify>
  <done>Services page: 2 CTAs (Work With Me primary button, Take the Assessment secondary link). BookingUrgency inline text visible near tiers. 2 dark TestimonialCards rendered. Old assessment callout block removed. Old Testimonials import removed. Zero TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 3: Add "Work With Me" CTA to AboutSection</name>
  <files>src/components/home/AboutSection.tsx</files>
  <action>
Read AboutSection.tsx in full first to understand its current structure and the last element in the component's return value.

NAV-03 requires a "Work With Me" CTA in the About section. AboutSection currently has no CTA.

Add a "Work With Me" Link at the very bottom of the AboutSection JSX, just before the closing tag of the outermost container. Style it to match the secondary CTA pattern used in HeroSection.tsx (underlined white text link, not a filled button — it is a secondary CTA in context of a page that already has a primary CTA elsewhere):

```tsx
import Link from 'next/link';

// At the bottom of the component's JSX, append:
<Link
  href="/engage"
  style={{
    display: 'inline-block',
    marginTop: '2rem',
    color: '#ffffff',
    fontSize: '0.9375rem',
    fontWeight: 500,
    textDecoration: 'underline',
    textUnderlineOffset: '3px',
  }}
>
  Work With Me
</Link>
```

If `Link` from `next/link` is already imported in AboutSection.tsx, do not add a duplicate import.

If AboutSection.tsx already uses a CSS module (AboutSection.module.css), prefer adding a class there instead of using inline styles — only use inline styles if there is no CSS module or adding to the module would require understanding class names that aren't visible without reading the CSS file. Use whichever approach is simpler given the file's existing patterns.

Do NOT add a wrapper div or section — just append the Link as the last child of whatever the current outermost container is. Keep the change minimal.
  </action>
  <verify>Run `npx tsc --noEmit` — zero errors. Open the homepage and scroll to the About section — a "Work With Me" link appears at the bottom of the section, links to /engage, and matches the secondary CTA style (underlined white text).</verify>
  <done>AboutSection.tsx has a "Work With Me" Link → /engage at the bottom of its JSX. It renders on the homepage below the about section content. Zero TypeScript errors.</done>
</task>

</tasks>

<verification>
1. Homepage: secondary CTA = "Work With Me" → /engage (not "Book a Discovery Call" → /checkout)
2. Homepage: 2 page-level CTAs only (primary Assessment + secondary Work With Me)
3. Homepage: 1 TestimonialCard visible in hero below TrustSignals
4. Services: "Work With Me" white button in hero section
5. Services: "Take the Assessment" secondary link visible
6. Services: assessment callout block completely absent from page
7. Services: BookingUrgency text renders "Currently booking for [Month] — 2 slots remaining"
8. Services: 2 dark TestimonialCard cards in testimonials section
9. Services: old white Testimonials component not imported
10. About section: "Work With Me" link visible at bottom, links to /engage
11. `npx tsc --noEmit` — zero errors
</verification>

<success_criteria>
- Homepage: exactly 2 page-level CTAs, secondary links to /engage
- Services: exactly 2 page-level CTAs (Work With Me + Take the Assessment)
- Services: BookingUrgency shows current month and BOOKING_SLOTS value
- Services: 2 dark TestimonialCards, no white card Testimonials
- Pricing all 4 tiers still visible (no regression on CONV-03)
- AboutSection: "Work With Me" → /engage link at bottom
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-navigation-content-polish-site-wide-cta-discipline-finish-content-trust-signals/04-03-SUMMARY.md` using the summary template.
</output>
