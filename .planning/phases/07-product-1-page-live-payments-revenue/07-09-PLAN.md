---
phase: 07-product-1-page-live-payments-revenue
plan: 09
type: execute
wave: 3
depends_on: ["07-02", "07-04"]
files_modified: [src/components/automate/ComparisonStrip.tsx, src/components/automate/ComparisonStrip.module.css, src/components/automate/PricingSection.tsx, src/components/automate/PricingSection.module.css]
autonomous: true
requirements: [PROD-03, PROD-04, PROD-05, PROD-06, PROD-10]

must_haves:
  truths:
    - "The Care Plan price reads $9.99/mo on the rendered page, never $99/mo"
    - "The page states $15.99/mo all-in against ManyChat's cheapest 250-contact tier as a headline fact"
    - "All four ManyChat tiers are shown as a path with arrows, contact counts leading and price secondary, with a flat $6/mo line as visual counterpoint"
    - "A visitor can click Purchase DFY / Purchase DWY / Subscribe Now and the correct Paddle checkout opens with the correct price ID"
  artifacts:
    - path: "src/components/automate/ComparisonStrip.tsx"
      provides: "ManyChat tier comparison + trajectory heading (PROD-05, PROD-06)"
      min_lines: 30
    - path: "src/components/automate/PricingSection.tsx"
      provides: "Build Map / DFY / DWY / Care Plan pricing cards wired to Paddle checkout"
      min_lines: 60
  key_links:
    - from: "src/components/automate/PricingSection.tsx"
      to: "src/lib/checkout.ts"
      via: "getAutomateTierById(\"dfy\" | \"dwy\" | \"care-plan\")"
      pattern: "getAutomateTierById"
    - from: "src/components/automate/PricingSection.tsx"
      to: "src/components/checkout/PaddleCheckout.tsx"
      via: "<PaddleCheckout priceId={tier.paddlePriceId} customData={{ product: tier.id }} />"
      pattern: "PaddleCheckout"
---

<objective>
Build the two pricing-truth sections: the ManyChat comparison strip (PROD-05: four tiers as a path with arrows, contact counts leading; PROD-06: "the bill climbs as your ads work" trajectory heading with the flat $6/mo line beneath) and the pricing cards (PROD-01/03/04/10: Build Map, DFY, DWY with speed/certainty-framed bullets, Care Plan at the corrected $9.99/mo, and the $15.99/mo all-in headline fact).

Purpose: This is the §H4-R typo fix (Care Plan $99→$9.99/mo) rendered on the actual page, and the section that opens real Paddle checkouts using Plan 04's automateTiers.
Output: ComparisonStrip + PricingSection components, composed into the page in Plan 12 at `id="pricing"`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ComparisonStrip component</name>
  <files>src/components/automate/ComparisonStrip.tsx, src/components/automate/ComparisonStrip.module.css</files>
  <read_first>
    .planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md (§ Section 4: Product Comparison Strip — exact tier data, arrow layout, mobile horizontal-scroll spec)
  </read_first>
  <action>
    Create component `ComparisonStrip`. Section heading (Large, --fontSize-6, 700): "Your bill climbs as your ads work. Ours doesn't." Render a horizontally-scrollable (mobile) row of 4 cards with "→" separators: "50 contacts" $17/mo, "250 contacts" $39/mo, "1,000 contacts" $99/mo, "5,000+ contacts" $199/mo (contact count in Lead --fontSize-3, price in Heading --fontSize-5, per UI-SPEC card text spec). Below the row, render a flat baseline visual: "$6/mo forever" spanning the same width, styled distinctly (e.g. dashed border-top using var(--border-2)) as the flat counterpoint. Card styling: `background: var(--surface-2)`, `border: 1px solid var(--border-2)`, `border-radius: var(--radius-2)`. Use CSS `overflow-x: auto` for the mobile scroll container (not CSS grid reflow) per UI-SPEC responsive rule.
  </action>
  <acceptance_criteria>
    <criterion type="source">ComparisonStrip.tsx contains all four contact-count strings: "50 contacts", "250 contacts", "1,000 contacts", "5,000+ contacts"</criterion>
    <criterion type="source">ComparisonStrip.tsx contains the exact heading "Your bill climbs as your ads work. Ours doesn't."</criterion>
    <criterion type="source">ComparisonStrip.tsx contains "$6/mo"</criterion>
  </acceptance_criteria>
  <verify>
    <automated>grep -c "5,000+ contacts" src/components/automate/ComparisonStrip.tsx</automated>
  </verify>
  <done>ComparisonStrip renders all four ManyChat tiers with arrows, the trajectory heading, and the flat $6/mo counterpoint line.</done>
</task>

<task type="auto">
  <name>Task 2: PricingSection component (Build Map / DFY / DWY / Care Plan)</name>
  <files>src/components/automate/PricingSection.tsx, src/components/automate/PricingSection.module.css</files>
  <read_first>
    src/lib/checkout.ts (automateTiers, getAutomateTierById — from Plan 07-04)
    src/components/checkout/PaddleCheckout.tsx (Props including new customData, from Plan 07-04)
    src/components/automate/BuildMapForm.tsx (from Plan 07-05 — the Build Map card's CTA opens this form, do not duplicate its logic)
    src/lib/analytics.ts (trackAnalyticsEvent, from Plan 07-02)
    .planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md (§ Section 5: Pricing Section — full card copy, feature lists, badge text, $15.99/mo all-in headline placement)
  </read_first>
  <action>
    Create `"use client"` component `PricingSection` with `id="pricing"` on the root element (Hero's CTA in Plan 06 links to `#pricing`). Render a headline fact above the cards per PROD-04: "$15.99/mo all-in ($6 droplet + $9.99 care) — against ManyChat's cheapest 250-contact tier at $39/mo" in Large (--fontSize-6) type. Render 4 cards using `getAutomateTierById()` from `@/lib/checkout` for `dfy`, `dwy`, `care-plan` (price/description sourced from config, not hardcoded strings, so the $9.99/mo fix propagates automatically) and a static Build Map card (free, not from automateTiers pricing since it's not purchasable): Build Map card CTA renders `<BuildMapForm />` inline on click (toggle local `showForm` state) with label "Download Build Map"; DFY card CTA renders `<PaddleCheckout priceId={getAutomateTierById("dfy").paddlePriceId} customData={{ product: "dfy" }} />` with feature bullets emphasizing speed/certainty per PROD-10 ("We handle all technical setup", "Live and tested on your account", "Training call included") — NOT bullets about capability already in the free tier; DWY card same pattern with `customData={{ product: "dwy" }}`; Care Plan card CTA renders `<PaddleCheckout priceId={getAutomateTierById("care-plan").paddlePriceId} customData={{ product: "care-plan" }} />` labeled "Subscribe Now", price line reading exactly `getAutomateTierById("care-plan").price` (never a hardcoded "$9.99/mo" string — always read from config so there is a single source of truth). On each Purchase/Subscribe button click (before the PaddleCheckout iframe mounts), call `trackAnalyticsEvent("Checkout Opened", { product_type: tier.id })`. Card layout: 3-col desktop / 2-col tablet / 1-col mobile, `background: var(--surface-2)`, `border: 1px solid var(--border-2)`, `box-shadow: var(--shadow-2)`.
  </action>
  <acceptance_criteria>
    <criterion type="source">PricingSection.tsx has a root element with id="pricing"</criterion>
    <criterion type="source">PricingSection.tsx contains no hardcoded literal "$9.99" string — Care Plan price is read via getAutomateTierById("care-plan").price</criterion>
    <criterion type="source">PricingSection.tsx contains no hardcoded literal "$99/mo" anywhere (regression guard against the original typo)</criterion>
    <criterion type="behavior">trackAnalyticsEvent is called with "Checkout Opened" and a product_type prop for each of dfy/dwy/care-plan</criterion>
  </acceptance_criteria>
  <verify>
    <automated>grep -c "\\$99/mo" src/components/automate/PricingSection.tsx | grep -qx 0 && echo "no $99/mo typo present"</automated>
  </verify>
  <done>PricingSection renders 4 tier cards sourced from automateTiers config, opens correct Paddle checkouts with customData, fires Checkout Opened events, and never hardcodes the Care Plan price string.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → Paddle checkout iframe | User-initiated; priceId is sourced from server-side config (automateTiers), not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-07-14 | Tampering | Price displayed to user vs. price charged by Paddle | mitigate | Displayed price string and the priceId passed to PaddleCheckout both originate from the same `automateTiers` config entry — no separate display-only price literal that could drift from the actual charge |
</threat_model>

<verification>
Both components compile under `npm run build` once composed into a page (Plan 12).
</verification>

<success_criteria>
Comparison strip and pricing cards render correctly, with the Care Plan typo fixed at the source (config-driven, not hardcoded), and purchase buttons open the correct Paddle checkout with tracked analytics.
</success_criteria>

<output>
Create `.planning/phases/07-product-1-page-live-payments-revenue/07-09-SUMMARY.md` when done.
</output>
