---
phase: 07-product-1-page-live-payments-revenue
plan: 10
type: execute
wave: 3
depends_on: ["07-03", "07-04", "07-05"]
files_modified: [src/app/(automate)/success/page.tsx, src/app/(automate)/success/success.module.css, src/app/api/automate/onboarding/route.ts, src/lib/email.ts]
autonomous: true
requirements: [PAY-07, PAY-08, PAY-09]

must_haves:
  truths:
    - "After purchasing DFY, the buyer lands on a success page with a 4-field onboarding form (IG handle, keyword, lead magnet link, voice/tone) and no Calendly embed"
    - "After purchasing DWY, the buyer lands on the same onboarding form PLUS a scheduling link for the screen-to-screen build session"
    - "The success page names the next rung of the ladder in copy, tagged automate-buyer, with no link into the assessment (Phase 10/ASSESS-11 not shipped yet)"
  artifacts:
    - path: "src/app/(automate)/success/page.tsx"
      provides: "Product-differentiated post-purchase onboarding page (PAY-07, PAY-08, PAY-09)"
      min_lines: 60
    - path: "src/app/api/automate/onboarding/route.ts"
      provides: "POST endpoint receiving onboarding form submissions, emails owner"
      exports: ["POST"]
  key_links:
    - from: "src/app/(automate)/success/page.tsx"
      to: "src/app/api/automate/onboarding/route.ts"
      via: "fetch POST with { product, igHandle, keyword, leadMagnetLink, voiceTone }"
      pattern: "api/automate/onboarding"
    - from: "src/app/(automate)/success/page.tsx"
      to: "src/lib/schemas.ts"
      via: "automateSuccessSchema + zodResolver"
      pattern: "automateSuccessSchema"
---

<objective>
Build the differentiated post-purchase success page (`/automate/success?product=dfy|dwy|care-plan`) with the 4-field onboarding form (PAY-07), a DWY-only scheduling link (PAY-08), and next-rung ladder copy (PAY-09) — explicitly NOT the existing `/checkout/success` page's Calendly-for-everyone pattern (RESEARCH.md Pitfall 6 names this exact anti-pattern).

Purpose: The webhook (Plan 08/11) confirms the purchase server-side; this page collects onboarding details client-side immediately after checkout, per the architecture split in RESEARCH.md (client callback → redirect + form; webhook → email/tagging).
Output: `/automate/success` page + `/api/automate/onboarding` route that emails the owner with the buyer's onboarding details.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-product-1-page-live-payments-revenue/07-RESEARCH.md
@.planning/phases/07-product-1-page-live-payments-revenue/07-PATTERNS.md
@.planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Onboarding API route</name>
  <files>src/app/api/automate/onboarding/route.ts, src/lib/email.ts</files>
  <read_first>
    src/app/api/subscribe/route.ts (NextRequest/NextResponse pattern)
    src/lib/schemas.ts (automateSuccessSchema, from Plan 07-05)
    src/lib/email.ts (FROM constant, unsubscribeFooter helper, resend.emails.send call shape)
  </read_first>
  <behavior>
    - POST with a valid { product: "dfy", igHandle, keyword, leadMagnetLink, voiceTone } body returns 200 { ok: true }
    - POST with a body that fails automateSuccessSchema validation (e.g. igHandle: "") returns 400 with a field-level error message
    - POST with product: "dwy" and a schedulingConfirmed: true field still returns 200 { ok: true }
  </behavior>
  <action>
    Create `export async function POST(req: NextRequest)` in `src/app/api/automate/onboarding/route.ts`. Parse the body, validate `{ igHandle, keyword, leadMagnetLink, voiceTone }` against `automateSuccessSchema` (imported from `@/lib/schemas`) using `.safeParse()`; on failure return `NextResponse.json({ error: result.error.issues[0].message }, { status: 400 })`. Accept an additional `product: "dfy" | "dwy"` field alongside the schema fields (not part of automateSuccessSchema itself — validate it's one of the two literal strings inline). On success, call a new `sendOnboardingNotification()` helper added to `src/lib/email.ts` — `export async function sendOnboardingNotification(params: { product: "dfy" | "dwy"; igHandle: string; keyword: string; leadMagnetLink: string; voiceTone: string }): Promise<{ success: boolean; error?: string }>` — that sends via `resend.emails.send({ from: FROM, to: process.env.OWNER_EMAIL ?? "hello@asorahura.com", subject: \`New ${params.product.toUpperCase()} onboarding: ${params.igHandle}\`, text: ... })` listing all four fields in the body. Log (not throw) on email failure; still return `{ ok: true }` to the client if validation passed (the form submission itself succeeded from the buyer's perspective even if the owner notification email has a transient failure — log clearly for manual follow-up).
  </action>
  <acceptance_criteria>
    <criterion type="test-command">npm test -- tests/test-onboarding-route.test.ts passes all `<behavior>` cases</criterion>
    <criterion type="source">src/lib/email.ts exports sendOnboardingNotification</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/test-onboarding-route.test.ts</automated>
  </verify>
  <done>Onboarding route validates input with the shared schema, emails the owner with all 4 fields plus product type, and never blocks the client response on email delivery failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Success page (DFY/DWY differentiated)</name>
  <files>src/app/(automate)/success/page.tsx, src/app/(automate)/success/success.module.css, tests/test-onboarding-route.test.ts</files>
  <read_first>
    src/app/checkout/success/page.tsx (existing pattern — DO NOT reuse the Calendly-for-everyone iframe; this is the anti-pattern being explicitly avoided per RESEARCH.md Pitfall 6)
    src/components/assessment/EmailGate.tsx (React Hook Form + Zod resolver pattern)
    src/lib/email.ts (CALENDLY_URL constant — reuse for DWY scheduling link per REQUIREMENTS.md Out of Scope table's explicit exception: "post-purchase DWY scheduling ... are the exceptions")
    .planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md (§ Success Page Form — full field spec, DWY scheduling CTA, error copy)
  </read_first>
  <behavior>
    - Rendering the success page with ?product=dfy shows the 4-field form and no scheduling link
    - Rendering the success page with ?product=dwy shows the 4-field form PLUS a "Schedule Build Session" link to CALENDLY_URL
    - Submitting the form with valid data calls fetch("/api/automate/onboarding", ...) with the product param included
  </behavior>
  <action>
    Create `src/app/(automate)/success/page.tsx` following the `Suspense` + `useSearchParams()` pattern from `src/app/checkout/page.tsx` (wrap the client component reading `useSearchParams` in `<Suspense fallback={null}>`). Read `product` param, defaulting to `"dfy"` if absent/invalid. Render: confirmation header ("Payment confirmed." + product-specific subcopy), then a form using `useForm<AutomateSuccessFormData>({ resolver: zodResolver(automateSuccessSchema) })` from `@/lib/schemas` (Plan 07-05) with the 4 fields per UI-SPEC (igHandle, keyword, leadMagnetLink, voiceTone — labels/placeholders/error copy verbatim from UI-SPEC § Success Page Form). On submit, POST to `/api/automate/onboarding` with `{ product, ...data }`; on success show "✓ We'll be in touch soon. Check your email for next steps." and call `trackAnalyticsEvent("Success Page Submit")`. If `product === "dwy"`, additionally render a "Schedule Build Session" outlined-accent button linking to `CALENDLY_URL` (import from `@/lib/email` — export it if not already exported) opening in a new tab — this is the one explicit exception to the "no Calendly in the primary path" rule, confirmed by REQUIREMENTS.md's Out of Scope table. Below the form, render next-rung copy per PAY-09: "Your Care Plan keeps this running — token renewals, uptime, small copy changes, $9.99/mo" (reads price from `getAutomateTierById("care-plan").price`, not a hardcoded string) with a link to `#pricing` on the main `/automate` page (`href="/automate#pricing"`) — no link to `/assessment` (PROD-12/ASSESS-11 constraint: assessment re-point ships in Phase 10, not this phase).
  </action>
  <acceptance_criteria>
    <criterion type="source">success/page.tsx contains no `<iframe src="https://calendly.com` for the DFY case (Calendly only renders when product === "dwy")</criterion>
    <criterion type="source">success/page.tsx contains no literal string "/assessment" (PROD-12 constraint — no link into assessment yet)</criterion>
    <criterion type="source">success/page.tsx imports automateSuccessSchema from "@/lib/schemas"</criterion>
    <criterion type="behavior">Rendering with product=dwy includes a link whose href matches CALENDLY_URL; rendering with product=dfy does not</criterion>
  </acceptance_criteria>
  <verify>
    <automated>grep -L "/assessment" "src/app/(automate)/success/page.tsx"</automated>
  </verify>
  <done>Success page differentiates DFY (form only) from DWY (form + scheduling link), names the Care Plan next rung without linking to the assessment, and submits onboarding data to the API route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → /api/automate/onboarding | Untrusted client submits IG handle / links / free-text voice notes; no purchase verification tied to this submission (any visitor could POST directly) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-07-15 | Tampering | Onboarding form fields (igHandle, leadMagnetLink, voiceTone) | mitigate | automateSuccessSchema (Zod) validates format/length server-side before the email is sent — igHandle regex, leadMagnetLink URL check, voiceTone length bounds |
| T-07-16 | Spoofing | Anyone (not just a real buyer) can POST to /api/automate/onboarding since it is not gated on a Paddle transaction ID | accept | No database exists to cross-check a real purchase against this submission in this phase (same constraint as T-07-12). Worst case is a spurious onboarding email to the owner inbox, not a financial or data-integrity impact. Documented for a future phase to gate this route on a transaction ID once order persistence exists. |
</threat_model>

<verification>
`npm test -- tests/test-onboarding-route.test.ts` passes.
</verification>

<success_criteria>
Success page correctly differentiates DFY/DWY, collects the 4 onboarding fields, emails the owner, and names the Care Plan next rung without linking into the assessment.
</success_criteria>

<output>
Create `.planning/phases/07-product-1-page-live-payments-revenue/07-10-SUMMARY.md` when done.
</output>
