---
phase: 07-product-1-page-live-payments-revenue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/api/paddle/config-status/route.ts, tests/test-paddle-config-status.test.ts]
autonomous: false
requirements: [PAY-01]

must_haves:
  truths:
    - "A developer can hit one endpoint and immediately know whether Paddle is in sandbox or production mode, and whether all required price IDs and the webhook secret are set"
    - "No further checkout work (Plan 04+) proceeds until a human has confirmed Paddle production status via this checkpoint"
  artifacts:
    - path: "src/app/api/paddle/config-status/route.ts"
      provides: "GET endpoint reporting Paddle mode + price ID + webhook secret presence"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/paddle/config-status/route.ts"
      to: "process.env.NEXT_PUBLIC_PADDLE_TOKEN"
      via: "token?.startsWith('test_') check"
      pattern: "startsWith\\('test_'\\)"
---

<objective>
Verify Paddle's production readiness before any other Phase 7 checkout work begins. This is the first task of the phase (PAY-01) and the largest launch risk called out in ROADMAP.md and STATE.md: the codebase currently only has a sandbox Paddle token and two price IDs (Discovery Call, Strategy Session) — none of the new DFY/DWY/Care Plan price IDs exist yet, and production Paddle status is unverified.

Purpose: Give the developer a single source of truth for Paddle environment status, then gate all subsequent checkout work (Plans 04, 07, 08, 09, 11) behind explicit human confirmation that production is ready.
Output: `/api/paddle/config-status` diagnostic route + a blocking checkpoint that pauses execution until the human confirms Paddle production account, price IDs, and webhook secret are set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-product-1-page-live-payments-revenue/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Paddle config-status diagnostic route</name>
  <files>src/app/api/paddle/config-status/route.ts, tests/test-paddle-config-status.test.ts</files>
  <read_first>
    src/app/api/subscribe/route.ts (NextRequest/NextResponse route pattern, env var guard style)
    .planning/phases/07-product-1-page-live-payments-revenue/07-RESEARCH.md (§ Code Examples — "Production Paddle Environment Check")
    tests/test-palette-review-route.test.ts (existing vitest route-import test pattern — no jsdom, pure node)
  </read_first>
  <action>
    Create `GET` handler at src/app/api/paddle/config-status/route.ts. Read env vars: NEXT_PUBLIC_PADDLE_TOKEN, NEXT_PUBLIC_PADDLE_PRICE_ID_DFY, NEXT_PUBLIC_PADDLE_PRICE_ID_DWY, NEXT_PUBLIC_PADDLE_PRICE_ID_CARE_PLAN, PADDLE_WEBHOOK_SECRET. Compute `paddle_mode: token?.startsWith('test_') ? 'sandbox' : 'production'`. Build a status object with `paddle_mode`, `paddle_token_set`, `dfy_price_id_set`, `dwy_price_id_set`, `care_plan_price_id_set`, `webhook_secret_set` (all booleans except paddle_mode). Do NOT include the actual token or price ID values in the response (secret-safety — booleans only). Return 503 with `{ warning: "Paddle is in SANDBOX mode...", ...status }` if paddle_mode is 'sandbox'. Return 500 with `{ error: "Missing price IDs", ...status }` if any of the three price ID booleans is false. Otherwise return 200 with `{ ok: true, ...status }`. This route has no side effects and requires no auth (matches existing `/api/subscribe` pattern of unauthenticated diagnostic-style routes) — it never echoes secret values, only presence booleans, so it is safe to leave unauthenticated for a solo-operator diagnostic endpoint.
  </action>
  <acceptance_criteria>
    <criterion type="source">Route file exports a named `GET` function, not `POST`</criterion>
    <criterion type="source">Response body never includes raw values of NEXT_PUBLIC_PADDLE_TOKEN or any PRICE_ID env var — only booleans/derived strings</criterion>
    <criterion type="behavior">With current .env.local (sandbox token, no DFY/DWY/CARE_PLAN price IDs, no PADDLE_WEBHOOK_SECRET), calling the route returns 503 (sandbox mode takes precedence over missing price IDs)</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/test-paddle-config-status.test.ts</automated>
  </verify>
  <done>Route compiles, returns correct status code and shape for sandbox/missing-price-ID/fully-configured states; test file covers all three states by mocking process.env.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm Paddle production readiness</name>
  <files>(none — configuration/verification only, no repo files)</files>
  <action>No automated action — this is a human sign-off gate confirming Paddle production account, price IDs, and webhook secret are set before Plan 04 (checkout tier config) and downstream checkout/webhook plans proceed.</action>
  <verify>Human confirms `/api/paddle/config-status` returns `{ ok: true, paddle_mode: "production" }` per the how-to-verify checklist below</verify>
  <done>User types "approved" once config-status returns ok:true in a production-configured environment — no downstream checkout plan proceeds until this is resolved</done>
  <what-built>A diagnostic endpoint at `/api/paddle/config-status` that reports whether Paddle is live and whether DFY/DWY/Care Plan price IDs and the webhook secret are configured.</what-built>
  <how-to-verify>
    1. Log into the Paddle dashboard (paddle.com) and confirm a **production** (non-sandbox) account exists for asorahura.
    2. In the Paddle dashboard, create three new production prices if they do not exist: DFY ($500 one-time), DWY ($800 one-time), Care Plan ($9.99/mo recurring).
    3. Generate a production client-side token (Developer Tools → Authentication) and a webhook signing secret (Developer Tools → Notifications → set up a destination for `transaction.completed`, copy the secret).
    4. Set the following in the production environment (e.g. Vercel project settings): `NEXT_PUBLIC_PADDLE_TOKEN` (production token, does NOT start with `test_`), `NEXT_PUBLIC_PADDLE_PRICE_ID_DFY`, `NEXT_PUBLIC_PADDLE_PRICE_ID_DWY`, `NEXT_PUBLIC_PADDLE_PRICE_ID_CARE_PLAN`, `PADDLE_WEBHOOK_SECRET`.
    5. Run `npm run dev` locally with these values in `.env.local` (or deploy to a preview environment) and visit `http://localhost:3000/api/paddle/config-status`. Confirm the JSON response is `{ "ok": true, "paddle_mode": "production", ... }`.
  </how-to-verify>
  <resume-signal>Type "approved" once config-status returns ok:true in a production-configured environment, or describe what's still missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Anonymous client → `/api/paddle/config-status` | Unauthenticated GET; public internet can call this route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-07-01 | Information Disclosure | `/api/paddle/config-status` | mitigate | Response contains only booleans/derived strings (`paddle_mode`, `*_set` flags) — never raw token or price ID values, preventing credential leak to any caller |
| T-07-02 | Denial of Service | `/api/paddle/config-status` | accept | No DB or third-party call in this route; Vercel's default rate limiting is sufficient for a low-traffic diagnostic endpoint |
</threat_model>

<verification>
`npm test -- tests/test-paddle-config-status.test.ts` passes. Human checkpoint confirms production Paddle status.
</verification>

<success_criteria>
Route returns accurate config status for sandbox and production states. Human has confirmed Paddle production account, all three new price IDs, and webhook secret are set before Plan 04 proceeds.
</success_criteria>

<output>
Create `.planning/phases/07-product-1-page-live-payments-revenue/07-01-SUMMARY.md` when done
</output>
