---
phase: 07-product-1-page-live-payments-revenue
plan: 12
type: execute
wave: 4
depends_on: ["07-03", "07-05", "07-06", "07-07", "07-09"]
files_modified: [src/app/(automate)/page.tsx, src/app/(automate)/error.tsx]
autonomous: true
requirements: [PROD-01, PROD-12, TRACK-02]

must_haves:
  truths:
    - "Visiting /automate renders Hero, Try-It-Now, Comparison Strip, Pricing (with Build Map/DFY/DWY/Care Plan), Phone Mockup, FAQ, and a closing section, inside the self-contained (automate) layout, in that order"
    - "A Land analytics event fires once on page mount, carrying UTM params from the URL"
    - "The closing section names the next rung of the ladder in copy and does not link to /assessment"
  artifacts:
    - path: "src/app/(automate)/page.tsx"
      provides: "Composed /automate landing page (PROD-01)"
      min_lines: 40
  key_links:
    - from: "src/app/(automate)/page.tsx"
      to: "src/lib/analytics.ts"
      via: "useTrackLandEvent() call on mount"
      pattern: "useTrackLandEvent"
    - from: "src/app/(automate)/page.tsx"
      to: "src/components/automate/HeroSection.tsx"
      via: "component composition"
      pattern: "HeroSection"
---

<objective>
Compose the full `/automate` page from every section component built in Plans 05–07 and 09, plus a closing section naming the next rung of the ladder with `automate-buyer` segmentation copy (PROD-12) and the Land analytics event (TRACK-02). This is the final "wire" step — no new business logic, only composition — per Interface-First ordering: all contracts (components, schemas, tier config) were defined in earlier waves, so this task only imports and arranges them.

**Content note:** PROD-01 originally specified rebuilding the page from `ai_learnt/assets/automate-landing-page.html`. That file does not exist anywhere in the repo (confirmed absent — RESEARCH.md, STATE.md blockers). Per planning instructions, this page's copy and structure are instead derived from PROJECT.md's product definitions, REQUIREMENTS.md's PROD-01..13 requirement text, and UI-SPEC.md's Page Structure & Sections contract (which already encodes the intended structure section-by-section). This satisfies PROD-01's intent (a live, on-brand `/automate` page) without a source HTML file to port from.

Purpose: PROD-01 (working page), PROD-12 (closing section + ladder framing), TRACK-02 (Land event wiring).
Output: `src/app/(automate)/page.tsx` — the live product page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose the /automate page + closing section</name>
  <files>src/app/(automate)/page.tsx, src/app/(automate)/error.tsx</files>
  <read_first>
    src/components/automate/HeroSection.tsx, TryItNowSection.tsx (Plan 07-06)
    src/components/automate/PhoneMockup.tsx, FAQSection.tsx (Plan 07-07)
    src/components/automate/ComparisonStrip.tsx, PricingSection.tsx (Plan 07-09)
    src/components/automate/BuildMapForm.tsx (Plan 07-05 — likely already composed inside PricingSection's Build Map card; do not duplicate rendering it a second time at page level unless PricingSection expects it as a child prop — check PricingSection.tsx's actual implementation before deciding)
    src/lib/analytics.ts (useTrackLandEvent, from Plan 07-02)
    .planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md (§ Page Structure & Sections — full 9-section order and § PROD-12 closing-section intent from REQUIREMENTS.md)
    src/app/checkout/page.tsx (Suspense + useSearchParams wrapping pattern, since useTrackLandEvent uses useSearchParams internally)
  </read_first>
  <action>
    Create `src/app/(automate)/page.tsx` exporting `export const metadata = { title: "Instagram Lead Automation for Creators | Asor Ahura" }` and a default export page component. Wrap the interactive body in `<Suspense fallback={null}>` around an inner client component that calls `useTrackLandEvent()` once on mount (this hook internally uses `useSearchParams`, which requires the Suspense boundary in the App Router, matching the existing `/checkout` page pattern). Render sections in this order: `<HeroSection />`, `<TryItNowSection />`, `<ComparisonStrip />`, `<PricingSection />`, `<PhoneMockup />`, `<FAQSection />`, then a new inline closing section (no separate file needed — a `<section>` directly in page.tsx) with heading "What's next" and body copy per PROD-12: "This is the first rung. Once your automation is live and generating leads, the next step is [Product #2 — coming soon]." (LADDER-01/02 are explicitly deferred per REQUIREMENTS.md Future Requirements — do not name a specific Product #2, only gesture at "the next rung" per PROD-12's literal wording) — and explicitly do NOT include any link or button pointing to `/assessment` (PROD-12 constraint: "it does not link into the assessment until ASSESS-11 lands"). Create a minimal `src/app/(automate)/error.tsx` client error boundary (`"use client"`, standard Next.js `error.tsx` shape: `{ error, reset }: { error: Error & { digest?: string }; reset: () => void }`) showing a simple "Something went wrong. Please refresh." message with a reset button, scoped to this route group only.
  </action>
  <acceptance_criteria>
    <criterion type="source">page.tsx imports and renders HeroSection, TryItNowSection, ComparisonStrip, PricingSection, PhoneMockup, and FAQSection, each exactly once</criterion>
    <criterion type="source">page.tsx contains no literal string "/assessment"</criterion>
    <criterion type="source">page.tsx calls useTrackLandEvent inside a component wrapped by Suspense</criterion>
    <criterion type="source">error.tsx exists at src/app/(automate)/error.tsx and starts with "use client"</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>`/automate` builds successfully, renders all sections in spec order inside the self-contained layout, fires the Land event once, and the closing section names the next rung without linking to the assessment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Page composition only; no new data flow beyond what child components already establish |
</threat_model>

<verification>
`npm run build` succeeds; `/automate` route appears in the build output.
</verification>

<success_criteria>
`/automate` is live, composed of all Phase 7 sections in UI-SPEC order, fires the Land event, and correctly stops short of linking into the (not-yet-shipped) assessment re-point.
</success_criteria>

<output>
Create `.planning/phases/07-product-1-page-live-payments-revenue/07-12-SUMMARY.md` when done.
</output>
