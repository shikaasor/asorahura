---
phase: 07-product-1-page-live-payments-revenue
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/analytics.ts, src/app/layout.tsx, tests/test-analytics.test.ts]
autonomous: true
requirements: [TRACK-01, TRACK-02]

must_haves:
  truths:
    - "Every page on the site (including /automate once built) loads the Plausible script, so events fired anywhere are captured"
    - "A UTM-carrying Land event fires when a visitor arrives from the Reel link"
  artifacts:
    - path: "src/lib/analytics.ts"
      provides: "trackAnalyticsEvent() helper + useTrackLandEvent() hook for UTM capture"
      exports: ["trackAnalyticsEvent", "useTrackLandEvent"]
    - path: "src/app/layout.tsx"
      provides: "Plausible script tag in <head>, loaded site-wide"
      contains: "plausible.io/js/script.js"
  key_links:
    - from: "src/app/layout.tsx"
      to: "plausible.io/js/script.js"
      via: "script tag in <head>"
      pattern: "data-domain"
---

<objective>
Install lightweight, privacy-first analytics (Plausible, TRACK-01) site-wide via a script tag (no npm package — Plausible's official Next.js integration is a `<script>` tag, not an SDK import, so no new dependency is added and no package-legitimacy checkpoint is required). Provide a small typed helper other Phase 7 plans (05, 06, 07, 08, 09, 11) import to fire the five funnel events, plus a reusable hook that captures UTM params on page mount for the Land event (TRACK-02).

Purpose: Every other component/route plan in this phase needs `trackAnalyticsEvent` to exist before it can fire funnel events — this plan is a wave-1 foundation plan other plans depend on.
Output: `src/lib/analytics.ts` (event helper + UTM hook), Plausible script tag added to the root layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-product-1-page-live-payments-revenue/07-RESEARCH.md
@.planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Plausible script + analytics helper</name>
  <files>src/lib/analytics.ts, src/app/layout.tsx, tests/test-analytics.test.ts</files>
  <read_first>
    src/app/layout.tsx (current RootLayout structure — html/head/body)
    .planning/phases/07-product-1-page-live-payments-revenue/07-RESEARCH.md (§ Pattern 5 — Analytics Event Tracking with Plausible)
    .planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md (§ Analytics Events — exact event names and prop shapes for all 5 events)
  </read_first>
  <action>
    In `src/app/layout.tsx`, add a `<head>` element inside the existing `<html lang="en">` (there is currently no explicit `<head>` — Next.js injects one; add an explicit `<head>` containing a `<script defer data-domain={process.env.NEXT_PUBLIC_PLAUSIBLE_DOMAIN ?? "asorahura.com"} src="https://plausible.io/js/script.js" />` immediately before the existing `<body>`). Do not remove or reorder any existing metadata/viewport exports.
    Create `src/lib/analytics.ts` exporting: (1) a `Window.plausible` type augmentation (`(eventName: string, options?: { props?: Record<string, string | number> }) => void`); (2) `export function trackAnalyticsEvent(eventName: "Land" | "Demo Interaction" | "Build Map Submit" | "Checkout Opened" | "Purchase" | "Success Page Submit", props?: Record<string, string | number>): void` that no-ops safely if `window === undefined` or `window.plausible` is not yet loaded (wrap in `typeof window !== "undefined" && window.plausible`); (3) `export function useTrackLandEvent(): void`, a client-only React hook (`"use client"` at top of file) using `useSearchParams()` from `next/navigation` inside a `useEffect` that reads `utm_source`, `utm_campaign`, `utm_medium`, `utm_content` from the query string (defaulting missing values to `"direct"` for utm_source and `"organic"` for utm_campaign per RESEARCH.md Pattern 5) and calls `trackAnalyticsEvent("Land", { utm_source, utm_campaign, utm_medium, utm_content })` exactly once per mount.
  </action>
  <acceptance_criteria>
    <criterion type="source">src/lib/analytics.ts exports both `trackAnalyticsEvent` and `useTrackLandEvent`</criterion>
    <criterion type="source">trackAnalyticsEvent never throws when `window.plausible` is undefined (guarded with typeof/optional chaining)</criterion>
    <criterion type="source">src/app/layout.tsx contains the literal string "plausible.io/js/script.js"</criterion>
    <criterion type="behavior">Calling trackAnalyticsEvent("Purchase", { product_type: "dfy", amount: 500, currency: "usd" }) in a test with a mocked window.plausible calls the mock with exactly those args</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/test-analytics.test.ts</automated>
  </verify>
  <done>trackAnalyticsEvent is safely callable with and without window.plausible present; layout.tsx renders the Plausible script tag; test file covers the no-op path and the event-firing path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → plausible.io | Client-side script sends event data to a third-party analytics collector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-07-03 | Information Disclosure | trackAnalyticsEvent props | mitigate | Event props are restricted to non-PII values (utm_*, product_type, amount, currency) by convention documented in UI-SPEC.md § Analytics Events — never pass email or IG handle as a prop |
</threat_model>

<verification>
`npm test -- tests/test-analytics.test.ts` passes. `npm run build` succeeds with the layout change.
</verification>

<success_criteria>
Plausible script loads on every route. trackAnalyticsEvent and useTrackLandEvent are ready for import by Plans 05, 06, 07, 08, 09, 11.
</success_criteria>

<output>
Create `.planning/phases/07-product-1-page-live-payments-revenue/07-02-SUMMARY.md` when done
</output>
