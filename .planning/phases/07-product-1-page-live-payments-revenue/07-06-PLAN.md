---
phase: 07-product-1-page-live-payments-revenue
plan: 06
type: execute
wave: 2
depends_on: ["07-02"]
files_modified: [src/components/automate/HeroSection.tsx, src/components/automate/HeroSection.module.css, src/components/automate/TryItNowSection.tsx, src/components/automate/TryItNowSection.module.css]
autonomous: true
requirements: [PROD-07, PROD-08, PROD-11]

must_haves:
  truths:
    - "The hero headline leads with revenue/income framing, not hours-saved framing"
    - "The acquisition Reel is embedded and playable directly on the page"
    - "A visitor can read instructions to comment the keyword on the live post and understand they'll receive the DM themselves"
  artifacts:
    - path: "src/components/automate/HeroSection.tsx"
      provides: "Hero with headline, subheading, embedded Reel, primary CTA"
      min_lines: 30
    - path: "src/components/automate/TryItNowSection.tsx"
      provides: "Try-it-now interactive block (PROD-08)"
      min_lines: 20
  key_links:
    - from: "src/components/automate/TryItNowSection.tsx"
      to: "src/lib/analytics.ts"
      via: "trackAnalyticsEvent(\"Demo Interaction\") on click"
      pattern: "Demo Interaction"
---

<objective>
Build the two lead-in sections of `/automate`: the Hero (revenue-first headline, embedded Reel, primary CTA — PROD-07, PROD-11) and the "Try It Right Now" block (PROD-08 — invites the visitor to comment the keyword on the live post and receive the DM themselves).

Purpose: These are the first two sections a cold visitor from the Reel sees; they set the revenue framing (PROD-11: "the follow-gate grows followers and lead list together from ad spend already being made") before any pricing is shown.
Output: Two standalone client components, composed into the page in Plan 12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HeroSection component</name>
  <files>src/components/automate/HeroSection.tsx, src/components/automate/HeroSection.module.css</files>
  <read_first>
    src/components/home/HeroSection.tsx (closest existing analog — component structure, image/CTA layout conventions)
    src/app/globals.css (Phase 6 tokens — --fontSize-7 Display 48px, --spacing-6 2xl, --accent)
    .planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md (§ Section 2: Hero Section — exact copy, layout, spacing)
    src/lib/analytics.ts (trackAnalyticsEvent, from Plan 07-02)
  </read_first>
  <action>
    Create `"use client"` component `HeroSection`. Headline (Display, --fontSize-7, weight 700): "Instagram Lead Automation for Creators". Subheading (Lead, --fontSize-3): "Stop paying by the contact. Own your list. The bill stays flat at $6/mo." Embed the acquisition Reel via an `<iframe>` (9:16 aspect on mobile, 4:3 on desktop, per UI-SPEC) — since no real Reel URL exists in any source artifact, use a `NEXT_PUBLIC_AUTOMATE_REEL_URL` env var for the iframe `src` with a documented fallback: if the env var is unset, render a placeholder card with text "Reel embed pending — set NEXT_PUBLIC_AUTOMATE_REEL_URL" instead of a broken iframe (never render an iframe with an empty src). Primary CTA button (Accent, label "Get Yours Now" per UI-SPEC Copywriting Contract) using an anchor link `href="#pricing"` (scrolls to Plan 09's PricingSection, which must render with `id="pricing"`). Section padding `var(--spacing-6)` top/bottom, `var(--spacing-4)` left/right (responsive: `var(--spacing-4) var(--spacing-3)` under 768px). Background `var(--surface-1)`.
  </action>
  <acceptance_criteria>
    <criterion type="source">HeroSection.tsx never renders an &lt;iframe&gt; with an empty or undefined src attribute</criterion>
    <criterion type="source">Headline text contains no occurrence of "hours" or "save time" (revenue framing, not time-saved framing per PROD-11/HOME-12 pattern)</criterion>
    <criterion type="source">Primary CTA anchor href is "#pricing"</criterion>
  </acceptance_criteria>
  <verify>
    <automated>grep -L "hours saved\|save time" src/components/automate/HeroSection.tsx</automated>
  </verify>
  <done>HeroSection renders headline, subheading, guarded Reel embed, and a CTA scrolling to pricing.</done>
</task>

<task type="auto">
  <name>Task 2: TryItNowSection component</name>
  <files>src/components/automate/TryItNowSection.tsx, src/components/automate/TryItNowSection.module.css</files>
  <read_first>
    .planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md (§ Section 3: "Try It Right Now" Block — exact copy and layout)
    src/lib/analytics.ts (trackAnalyticsEvent, from Plan 07-02)
  </read_first>
  <action>
    Create `"use client"` component `TryItNowSection`. Heading (Subtitle, --fontSize-4): "Try it right now". Body copy: "Comment the keyword on the live post below and receive the DM yourself." Reference/embed the same Reel (reuse the `NEXT_PUBLIC_AUTOMATE_REEL_URL` guard pattern from Task 1, or link to it with a "Watch the Reel" anchor if embedding twice is undesirable — pick the link approach to avoid double-loading the same iframe, per UI-SPEC's "Embedded Reel (same as Hero, or reference/link)" allowance). Wrap the block in a `<div onClick={...}>` (or attach the handler to the primary interactive element, e.g. the link) that calls `trackAnalyticsEvent("Demo Interaction")` exactly once per click. Background `var(--surface-2)` (card-like container per UI-SPEC), padding `var(--spacing-4)`.
  </action>
  <acceptance_criteria>
    <criterion type="source">TryItNowSection.tsx imports trackAnalyticsEvent from "@/lib/analytics"</criterion>
    <criterion type="source">trackAnalyticsEvent is called with the literal string "Demo Interaction"</criterion>
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Demo Interaction" src/components/automate/TryItNowSection.tsx</automated>
  </verify>
  <done>TryItNowSection renders the invite-to-comment copy and fires the Demo Interaction analytics event on interaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser ↔ embedded Reel iframe | Third-party (Instagram/Meta) embedded content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-07-09 | Tampering | Reel iframe src sourced from env var | mitigate | src is read only from a build-time env var (NEXT_PUBLIC_AUTOMATE_REEL_URL), never from user input or query params, eliminating iframe-injection risk |
</threat_model>

<verification>
Both components compile under `npm run build` once composed into a page (Plan 12).
</verification>

<success_criteria>
Hero and Try-It-Now sections exist, ready for composition, with revenue-first copy and a guarded Reel embed.
</success_criteria>

<output>
Create `.planning/phases/07-product-1-page-live-payments-revenue/07-06-SUMMARY.md` when done. **Flag the missing real Reel URL** (no source artifact provides one) as a pre-launch follow-up.
</output>
