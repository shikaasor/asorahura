---
phase: 07-product-1-page-live-payments-revenue
plan: 07
type: execute
wave: 2
depends_on: ["07-02"]
files_modified: [src/components/automate/PhoneMockup.tsx, src/components/automate/PhoneMockup.module.css, src/components/automate/FAQSection.tsx, src/components/automate/FAQSection.module.css]
autonomous: true
requirements: [PROD-09, PROD-13]

must_haves:
  truths:
    - "The phone mockup image is explicitly labelled as a real screenshot of a live account, not described as an illustration"
    - "Refund policy text and at least 4 FAQ entries are visible as page text (not hidden behind an external link)"
  artifacts:
    - path: "src/components/automate/PhoneMockup.tsx"
      provides: "Labelled phone mockup section (PROD-09)"
      min_lines: 15
    - path: "src/components/automate/FAQSection.tsx"
      provides: "Expandable FAQ accordion including refund policy (PROD-13)"
      min_lines: 40
  key_links:
    - from: "src/components/automate/FAQSection.tsx"
      to: "lucide-react"
      via: "ChevronDown icon import"
      pattern: "lucide-react"
---

<objective>
Build the phone mockup section (PROD-09 — labelled as a real screenshot, not an illustration) and the FAQ accordion (PROD-13 — refund policy + common objections visible as page text).

Purpose: PROD-09 and PROD-13 are trust-building sections near the bottom of the page, both static/self-contained with no cross-plan data dependency beyond the analytics helper.
Output: Two standalone components composed into the page in Plan 12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PhoneMockup component</name>
  <files>src/components/automate/PhoneMockup.tsx, src/components/automate/PhoneMockup.module.css</files>
  <read_first>
    .planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md (§ Section 7: Phone Mockup — exact copy, alt text, layout)
    public/ (existing image assets — confirm no existing DM-screenshot asset to reuse; none found as of this plan's research, so a placeholder is required)
  </read_first>
  <action>
    Create component `PhoneMockup`. Since no real screenshot of a live DM conversation exists in any source artifact (repo `public/images/` has no automate-related screenshots), use `next/image` with a placeholder source `/images/automate/dm-screenshot-placeholder.png` — create this placeholder as a simple solid-background PNG is out of scope for a code-only executor; instead, render an `<div>` styled to look like a phone frame containing a text placeholder ("Screenshot pending") wrapped in the exact same layout/positioning the real image will occupy (max-width 300px desktop, full width mobile, right-aligned on desktop per UI-SPEC), so swapping in the real screenshot later requires no layout changes — only replacing the placeholder div with an `<Image>` tag. Label text below (Small, --fontSize-1, Ink 2): "Real screenshot. Live account. Working now." — this label copy must ship even though the image itself is a placeholder, since PROD-09's requirement is about the *labelling claim*, and the label is only valid once the real screenshot replaces the placeholder (flag this explicitly in the plan's summary).
  </action>
  <acceptance_criteria>
    <criterion type="source">PhoneMockup.tsx contains the exact label text "Real screenshot. Live account. Working now."</criterion>
    <criterion type="source">No hardcoded hex colors in PhoneMockup.module.css</criterion>
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Real screenshot" src/components/automate/PhoneMockup.tsx</automated>
  </verify>
  <done>PhoneMockup renders in the exact final layout position with a placeholder graphic and the correct label copy, ready for the real screenshot to be dropped in later.</done>
</task>

<task type="auto">
  <name>Task 2: FAQSection component (accordion, includes refund policy)</name>
  <files>src/components/automate/FAQSection.tsx, src/components/automate/FAQSection.module.css</files>
  <read_first>
    .planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md (§ Section 8: FAQ Section — 4 sample Q&As including the refund policy Q&A, icon spec)
    src/lib/checkout.ts (import if needed to reference DFY/DWY prices inside FAQ copy — but do not create a new dependency if UI-SPEC copy is self-contained)
  </read_first>
  <action>
    Create `"use client"` component `FAQSection` with `id="faq"` on the root element (Plan 03's layout footer links to `/automate#faq` for the refund policy anchor — this id must exist for that link to resolve). Implement as a `useState<number | null>` controlled accordion (no external library — PATTERNS.md confirms no accordion analog exists in the codebase). Render the 4 Q&As from UI-SPEC § Section 8 verbatim, including the refund policy answer ("Yes. 30-day money-back guarantee on DFY and DWY. Care Plan is billed monthly and can be cancelled anytime."). Each item: question (Subtitle, --fontSize-4, weight 700, clickable), answer (Body, --fontSize-2, Ink 2) shown when expanded. Use `ChevronDown` from `lucide-react` (already installed), rotated 180deg via CSS when expanded (no "+"/"−" text glyphs — lucide-react is the mandated icon library per UI-SPEC § Design System and POLISH-03's icon-set direction).
  </action>
  <acceptance_criteria>
    <criterion type="source">FAQSection.tsx has a root element with id="faq"</criterion>
    <criterion type="source">FAQSection.tsx imports ChevronDown from "lucide-react"</criterion>
    <criterion type="source">FAQSection.tsx contains the substring "30-day money-back guarantee"</criterion>
  </acceptance_criteria>
  <verify>
    <automated>grep -c "id=\"faq\"" src/components/automate/FAQSection.tsx</automated>
  </verify>
  <done>FAQSection renders an accordion with 4 entries including the refund policy, addressable via #faq anchor, using lucide-react icons only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Static content components, no user input, no external calls |
</threat_model>

<verification>
Both components compile under `npm run build` once composed into a page (Plan 12).
</verification>

<success_criteria>
Phone mockup section ships in final layout position with correct (currently placeholder-backed) labelling; FAQ accordion covers refund policy and 3 other objections, addressable via #faq.
</success_criteria>

<output>
Create `.planning/phases/07-product-1-page-live-payments-revenue/07-07-SUMMARY.md` when done. **Flag the placeholder phone-mockup screenshot** as a pre-launch follow-up — PROD-09's "real screenshot" claim is only true once the founder supplies the real image.
</output>
