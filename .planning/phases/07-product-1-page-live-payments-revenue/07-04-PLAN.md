---
phase: 07-product-1-page-live-payments-revenue
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: [src/lib/checkout.ts, src/components/checkout/PaddleCheckout.tsx, tests/test-automate-checkout.test.ts]
autonomous: true
requirements: [PAY-02, PAY-03, PAY-04, PROD-03]

must_haves:
  truths:
    - "The Care Plan price reads $9.99/mo everywhere it is defined in code, never $99/mo"
    - "DFY, DWY, and Care Plan each have a distinct, environment-variable-sourced Paddle price ID, separate from the existing /checkout page's discovery/strategy tiers"
    - "PaddleCheckout automatically targets Paddle sandbox or production based on the token's prefix, with no hardcoded environment string"
    - "The existing /checkout page (discovery/strategy tiers) is completely unaffected by this change"
  artifacts:
    - path: "src/lib/checkout.ts"
      provides: "automateTiers array + AutomateTierId type + getAutomateTierById() + getPaddleEnvironment(), additive to existing tiers/TierId"
      exports: ["automateTiers", "AutomateTierId", "getAutomateTierById", "getPaddleEnvironment"]
  key_links:
    - from: "src/components/checkout/PaddleCheckout.tsx"
      to: "src/lib/checkout.ts"
      via: "getPaddleEnvironment() import replacing hardcoded 'sandbox'"
      pattern: "getPaddleEnvironment"
---

<objective>
Extend the checkout tier configuration with the three new Phase 7 products (DFY $500 one-time, DWY $800 one-time, Care Plan $9.99/mo recurring, plus a non-purchasable Build Map entry for display consistency) as a NEW, additive export — the existing `/checkout` page's `tiers`/`TierId`/`getTierById` (discovery/strategy) must remain byte-for-byte unchanged, because `/checkout/page.tsx` iterates over `tiers` directly and would otherwise leak the new automate tiers into the enterprise checkout flow. Also fix `PaddleCheckout.tsx`'s hardcoded `Environment.set("sandbox")` to derive sandbox/production from the token prefix (PAY-01's config-status route already established this exact `startsWith('test_')` logic — reuse it), and add optional `customData` support so the upcoming webhook (Plan 08) can identify which product was purchased.

Purpose: This is the single source of truth for DFY/DWY/Care Plan pricing and Paddle price IDs (PAY-02/03/04) that Plans 08 (webhook), 09 (pricing UI), 10 (success page), and 12 (page compose) all depend on.
Output: `automateTiers` config + Paddle env auto-detection + `customData` passthrough in the checkout component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-product-1-page-live-payments-revenue/07-RESEARCH.md
@.planning/phases/07-product-1-page-live-payments-revenue/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add automateTiers config + getPaddleEnvironment helper</name>
  <files>src/lib/checkout.ts, tests/test-automate-checkout.test.ts</files>
  <read_first>
    src/lib/checkout.ts (current TierId/Tier/tiers/getTierById — DO NOT modify these, only append below them)
    src/app/api/paddle/config-status/route.ts (Plan 01 — same `startsWith('test_')` sandbox-detection logic to reuse/extract)
    .planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md (§ Section 5: Pricing Section — exact price strings and descriptions per tier)
  </read_first>
  <behavior>
    - getPaddleEnvironment() returns "sandbox" when NEXT_PUBLIC_PADDLE_TOKEN starts with "test_"
    - getPaddleEnvironment() returns "production" when NEXT_PUBLIC_PADDLE_TOKEN does not start with "test_" (and is non-empty)
    - getAutomateTierById("care-plan") returns a tier whose price field is exactly "$9.99/mo" (never "$99/mo")
    - getAutomateTierById("dfy") returns paddlePriceId sourced from process.env.NEXT_PUBLIC_PADDLE_PRICE_ID_DFY
    - existing tiers array (discovery, strategy) is unchanged in length and content after this change
  </behavior>
  <action>
    Append (do not replace) to src/lib/checkout.ts: `export type AutomateTierId = "build-map" | "dfy" | "dwy" | "care-plan";` and `export interface AutomateTier { id: AutomateTierId; name: string; price: string; priceDetail: string; billingType: "one-time" | "recurring" | "free"; paddlePriceId: string; description: string; }`. Define `export const automateTiers: AutomateTier[]` with four entries: build-map (price "Free", billingType "free", paddlePriceId ""), dfy (price "$500", priceDetail "one-time", billingType "one-time", paddlePriceId from `process.env.NEXT_PUBLIC_PADDLE_PRICE_ID_DFY || ""`), dwy (price "$800", priceDetail "one-time", billingType "one-time", paddlePriceId from `process.env.NEXT_PUBLIC_PADDLE_PRICE_ID_DWY || ""`), care-plan (price "$9.99/mo" — exact string, priceDetail "monthly", billingType "recurring", paddlePriceId from `process.env.NEXT_PUBLIC_PADDLE_PRICE_ID_CARE_PLAN || ""`). Descriptions per UI-SPEC.md § Section 5. Add `export function getAutomateTierById(id: AutomateTierId): AutomateTier` mirroring the existing `getTierById` pattern. Add `export function getPaddleEnvironment(): "sandbox" | "production"` returning `"sandbox"` if `process.env.NEXT_PUBLIC_PADDLE_TOKEN?.startsWith("test_")`, else `"production"`.
  </action>
  <acceptance_criteria>
    <criterion type="source">Existing `export const tiers` array in src/lib/checkout.ts is unchanged (still exactly 2 entries: discovery, strategy)</criterion>
    <criterion type="test-command">npm test -- tests/test-automate-checkout.test.ts passes all `<behavior>` cases above</criterion>
    <criterion type="source">automateTiers[care-plan].price is the literal string "$9.99/mo"</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/test-automate-checkout.test.ts</automated>
  </verify>
  <done>automateTiers, getAutomateTierById, getPaddleEnvironment exported and tested; existing tiers/TierId/getTierById untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PaddleCheckout to dynamic environment + custom data</name>
  <files>src/components/checkout/PaddleCheckout.tsx</files>
  <read_first>
    src/components/checkout/PaddleCheckout.tsx (full file — hardcoded `Environment.set("sandbox")` at line ~63, `Checkout.open` config at line ~74)
  </read_first>
  <action>
    Import `getPaddleEnvironment` from `@/lib/checkout`. Replace the hardcoded `window.Paddle.Environment.set("sandbox")` call with `window.Paddle.Environment.set(getPaddleEnvironment())`. Add an optional prop `customData?: Record<string, string>` to the `Props` interface. When present, pass it into the `Checkout.open` config as a top-level `customData` field alongside the existing `items` and `settings` keys (Paddle's checkout config accepts `customData` as an object echoed back verbatim in webhook payloads). Keep the prop optional and default to `undefined` so the existing `/checkout` page (which does not pass `customData`) is unaffected.
  </action>
  <acceptance_criteria>
    <criterion type="source">No literal string "sandbox" remains as an argument to Environment.set in PaddleCheckout.tsx</criterion>
    <criterion type="source">Props interface includes `customData?: Record<string, string>`</criterion>
    <criterion type="behavior">Calling `<PaddleCheckout priceId="x" />` without customData compiles and behaves identically to before this change (no customData key sent)</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>PaddleCheckout dynamically targets sandbox/production and can pass custom_data for webhook product identification; /checkout page continues to work with zero behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → Paddle SDK | Client controls which priceId/customData is sent to Paddle's checkout iframe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-07-04 | Tampering | priceId passed to PaddleCheckout | mitigate | Price IDs are always sourced server-side from `automateTiers`/`tiers` env-var-backed config, never accepted as raw client input from a form field or URL param without validation against the known tier ID enum |
| T-07-05 | Tampering | customData passed to Paddle | accept | customData only carries a product-type string for webhook bookkeeping; it is not trusted for pricing (Paddle charges based on priceId, not customData), so a manipulated customData value cannot cause a wrong charge amount |
</threat_model>

<verification>
`npm test -- tests/test-automate-checkout.test.ts` and `npm run build` both pass.
</verification>

<success_criteria>
automateTiers config exists with correct $9.99/mo Care Plan pricing and env-var-sourced price IDs. PaddleCheckout auto-detects sandbox/production and supports customData. /checkout page unaffected.
</success_criteria>

<output>
Create `.planning/phases/07-product-1-page-live-payments-revenue/07-04-SUMMARY.md` when done
</output>
