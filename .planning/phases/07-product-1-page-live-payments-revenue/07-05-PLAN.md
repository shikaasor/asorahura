---
phase: 07-product-1-page-live-payments-revenue
plan: 05
type: execute
wave: 2
depends_on: ["07-02"]
files_modified: [src/lib/schemas.ts, src/app/api/subscribe/route.ts, src/lib/email.ts, src/components/automate/BuildMapForm.tsx, src/components/automate/BuildMapForm.module.css, public/downloads/build-map-guide.txt, tests/test-schemas.test.ts, tests/test-subscribe-build-map.test.ts]
autonomous: true
requirements: [LEAD-01, LEAD-02, LEAD-03, LEAD-04]

must_haves:
  truths:
    - "A visitor can submit their email on /automate and receive a Build Map download link by email within seconds"
    - "The submitted email lands in Resend contacts with a build-map-downloader marker, mirrored to Google Sheets"
    - "The existing blog email capture widget (unrelated to Build Map) is completely unaffected by this change"
  artifacts:
    - path: "src/lib/schemas.ts"
      provides: "buildMapSchema + automateSuccessSchema Zod validation"
      exports: ["buildMapSchema", "automateSuccessSchema", "BuildMapFormData", "AutomateSuccessFormData"]
    - path: "src/components/automate/BuildMapForm.tsx"
      provides: "Email capture form with real-time validation, wired to /api/subscribe"
      min_lines: 40
    - path: "public/downloads/build-map-guide.txt"
      provides: "Placeholder Build Map deliverable (real n8n workflow files do not exist in repo — flagged for human replacement before launch)"
  key_links:
    - from: "src/components/automate/BuildMapForm.tsx"
      to: "src/app/api/subscribe/route.ts"
      via: "fetch POST /api/subscribe with { email, source: 'build-map' }"
      pattern: "source.*build-map"
    - from: "src/app/api/subscribe/route.ts"
      to: "src/lib/email.ts"
      via: "sendBuildMapEmail() call when source === 'build-map'"
      pattern: "sendBuildMapEmail"
---

<objective>
Build the Build Map free lead-capture flow: email form → `/api/subscribe` (existing route, extended) → Resend contact creation + `build-map-downloader` segmentation marker + Google Sheets mirror + transactional email with the download link, sent from `hello@asorahura.com` via `src/lib/email.ts`.

**Content gap flagged per planning instructions:** The real Build Map deliverable (4 n8n workflow files + env template + deployment guide, per PROJECT.md) does not exist anywhere in the repo (confirmed absent in RESEARCH.md Assumption A4 and STATE.md blockers). This plan creates a clearly-labeled placeholder text file so the phase is executable end-to-end; the founder must replace `public/downloads/build-map-guide.txt` with the real deliverable before the page goes live. This is called out explicitly in this plan's task and must be surfaced again in the phase's final SUMMARY.

Purpose: LEAD-01..04 require a working, tagged, emailed lead-capture loop — this is the full loop, reusing `/api/subscribe` non-destructively (existing blog widget caller is unaffected).
Output: Zod schemas (shared with Plan 10's success-page form), extended subscribe route, BuildMapForm component, placeholder deliverable asset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-product-1-page-live-payments-revenue/07-RESEARCH.md
@.planning/phases/07-product-1-page-live-payments-revenue/07-PATTERNS.md
@.planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zod schemas + placeholder deliverable</name>
  <files>src/lib/schemas.ts, public/downloads/build-map-guide.txt, tests/test-schemas.test.ts</files>
  <read_first>
    src/lib/validation.ts (existing Zod schema pattern — emailGateSchema, exported inferred type convention)
    .planning/phases/07-product-1-page-live-payments-revenue/07-RESEARCH.md (§ Code Examples — Zod Schema for Success Page Form)
  </read_first>
  <behavior>
    - buildMapSchema.safeParse({ email: "valid@example.com" }).success === true
    - buildMapSchema.safeParse({ email: "not-an-email" }).success === false
    - automateSuccessSchema.safeParse({ igHandle: "@creator", keyword: "AUTOMATE", leadMagnetLink: "https://bit.ly/x", voiceTone: "Casual and funny, Gen Z audience" }).success === true
    - automateSuccessSchema.safeParse({ igHandle: "", keyword: "x", leadMagnetLink: "not-a-url", voiceTone: "short" }).success === false
  </behavior>
  <action>
    Create `src/lib/schemas.ts` with `export const buildMapSchema = z.object({ email: z.string().email("Invalid email address") })` and `export const automateSuccessSchema = z.object({ igHandle: z.string().min(1, "Instagram handle required").regex(/^@?[\w.]+$/, "Invalid Instagram handle format").max(30, "Handle too long"), keyword: z.string().min(1, "Keyword required").max(50, "Keyword too long"), leadMagnetLink: z.string().url("Must be a valid URL"), voiceTone: z.string().min(10, "Provide voice/tone guidance").max(200, "Keep it concise") })`. Export `BuildMapFormData` and `AutomateSuccessFormData` as `z.infer<>` of each. Create `public/downloads/build-map-guide.txt` containing: a header "PLACEHOLDER — Instagram Lead Automation Build Map" followed by a note "This is a placeholder. Replace this file with the real deliverable (4 n8n workflow JSON exports, .env.example template, and a step-by-step deployment guide) before this page is used to acquire real leads.", then a short description per tier restating the free/DFY/DWY value proposition so the file is not literally empty.
  </action>
  <acceptance_criteria>
    <criterion type="test-command">npm test -- tests/test-schemas.test.ts passes all `<behavior>` cases</criterion>
    <criterion type="source">public/downloads/build-map-guide.txt contains the exact substring "PLACEHOLDER"</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/test-schemas.test.ts</automated>
  </verify>
  <done>Both schemas validate correctly per behavior cases; placeholder deliverable exists and is clearly labeled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend /api/subscribe with Build Map segmentation + email send</name>
  <files>src/app/api/subscribe/route.ts, src/lib/email.ts, tests/test-subscribe-build-map.test.ts</files>
  <read_first>
    src/app/api/subscribe/route.ts (full current file — the only other caller is src/components/blog/EmailCaptureWidget.tsx, which never sends a `source` field, so it must retain today's untagged behavior)
    src/lib/email.ts (FROM constant, BASE_URL constant, resend.emails.send call shape)
    node_modules/resend/dist/index.d.mts (search "UpdateContactOptions" and "CreateContactOptions" — Resend v6.12.3 has NO `tags` field on contacts. Use `properties?: { [key: string]: string | number | null }` instead — this is the actual segmentation mechanism available on this SDK version, not the `tags` field described in RESEARCH.md's illustrative pseudocode)
    .planning/phases/07-product-1-page-live-payments-revenue/07-RESEARCH.md (§ Pitfall 5 — segmentation on email capture; intent only, field name superseded by the actual SDK types above)
  </read_first>
  <action>
    In `src/app/api/subscribe/route.ts`, accept an optional `source` field from the request body (`const { email, source } = await req.json();`). Pass `properties: source === "build-map" ? { segment: "build-map-downloader" } : undefined` directly into the existing `resend.contacts.create({ email, audienceId, unsubscribed: false, properties })` call (Resend's `CreateContactOptions.properties` accepts `{ [key: string]: string | number | null }` — no separate update call needed since this is set at creation time; if the contact already exists, `contacts.create` for an existing email will error, so wrap the create call's error branch to also attempt `resend.contacts.update({ email: email.trim().toLowerCase(), audienceId, properties })` when `source === "build-map"` and the create call failed with a "contact exists"-shaped error, so returning downloaders still get segmented). After the contact is created/updated successfully, if `source === "build-map"`: (a) call the new `sendBuildMapEmail(email)` helper from `src/lib/email.ts` and log (not throw) on failure so the HTTP response is not blocked by an email hiccup; (b) change the Google Sheets mirror `formType` from `"newsletter"` to `"build-map"` only for this branch. When `source` is absent or any other value, preserve today's exact behavior (no properties field sent, no email send, `formType: "newsletter"`) — this keeps `EmailCaptureWidget.tsx` (blog) working unchanged. In `src/lib/email.ts`, add `export async function sendBuildMapEmail(email: string): Promise<{ success: boolean; error?: string }>` that sends via `resend.emails.send({ from: FROM, to: email, subject: "Your Build Map is ready", text: ... })` with a plain-text body linking to `${BASE_URL}/downloads/build-map-guide.txt` and including the standard unsubscribe footer via the existing `unsubscribeFooter(email)` helper.
  </action>
  <acceptance_criteria>
    <criterion type="behavior">POST /api/subscribe with { email, source: "build-map" } results in contacts.create called with properties.segment === "build-map-downloader", and a sendBuildMapEmail call (mocked in test)</criterion>
    <criterion type="behavior">POST /api/subscribe with { email } (no source) results in contacts.create called with no properties field and zero sendBuildMapEmail calls — matches pre-change behavior exactly</criterion>
    <criterion type="source">src/lib/email.ts exports sendBuildMapEmail</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/test-subscribe-build-map.test.ts</automated>
  </verify>
  <done>Build Map submissions are segmented via properties, mirrored with the right formType, and emailed; all other /api/subscribe callers (blog widget) behave identically to before.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BuildMapForm component</name>
  <files>src/components/automate/BuildMapForm.tsx, src/components/automate/BuildMapForm.module.css</files>
  <read_first>
    src/components/assessment/EmailGate.tsx (React Hook Form + Zod resolver pattern to replicate — register/handleSubmit/formState.errors)
    .planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md (§ Section 6 — Build Map Email Capture Form, full interaction state table)
    src/lib/schemas.ts (buildMapSchema created in Task 1 of this plan)
    src/lib/analytics.ts (trackAnalyticsEvent, created in Plan 07-02)
  </read_first>
  <action>
    Create `"use client"` component `BuildMapForm` using `useForm<BuildMapFormData>({ resolver: zodResolver(buildMapSchema) })`. Render a single email input with real-time validation feedback per UI-SPEC.md § Section 6 state table (empty/focused/invalid/valid/submitting/success/error/already-signed-up states — use React state `status: "idle" | "loading" | "success" | "error" | "already-subscribed"`). On submit, POST to `/api/subscribe` with `{ email: data.email, source: "build-map" }`; on `res.ok` call `trackAnalyticsEvent("Build Map Submit")` and set status to `"success"` showing "✓ Check your inbox for your download link"; on non-ok response with a body message containing "already" (case-insensitive) treat as `"already-subscribed"` (friendly, not error) per UI-SPEC; otherwise set `"error"` with the UI-SPEC error copy "Couldn't send email. Please try again." CTA button label "Download Build Map" (UI-SPEC § Copywriting Contract — never "Buy now"/"Submit"). Style via `BuildMapForm.module.css` referencing globals.css tokens (--surface-2, --border-1/2, --success, --error, --accent) — no hardcoded hex values.
  </action>
  <acceptance_criteria>
    <criterion type="source">BuildMapForm.tsx imports buildMapSchema from "@/lib/schemas" and zodResolver from "@hookform/resolvers/zod"</criterion>
    <criterion type="source">BuildMapForm.module.css contains no raw hex color literals (grep -c "#[0-9a-fA-F]\{3,8\}" returns 0)</criterion>
    <criterion type="behavior">Rendering BuildMapForm() does not throw</criterion>
  </acceptance_criteria>
  <verify>
    <automated>grep -c "#[0-9a-fA-F]\{3,8\}" src/components/automate/BuildMapForm.module.css | grep -qx 0 && echo "no hardcoded hex"</automated>
  </verify>
  <done>BuildMapForm renders, validates in real time, submits to /api/subscribe with source=build-map, fires the Build Map Submit analytics event, and matches all UI-SPEC interaction states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → /api/subscribe | Untrusted client submits an email address; no auth required (public lead-capture form) |
| /api/subscribe → Resend / Google Sheets | Server-side outbound calls carrying PII (email address) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-07-06 | Tampering | Email input validation | mitigate | Zod `.email()` (RFC-compliant) rejects malformed input client-side; server-side route already guards with `!email.includes('@')` as a defense-in-depth check |
| T-07-07 | Information Disclosure | List harvesting via repeated /api/subscribe submissions | accept | No rate limiting added in this plan (out of scope — RESEARCH.md flags 5 req/IP/min as a recommended follow-up); accepted risk for launch given solo-operator scale, documented here for Phase 11 polish follow-up |
| T-07-08 | Tampering | Header injection via email field | mitigate | Zod .email() rejects newlines/control characters; Resend API additionally escapes headers server-side |
</threat_model>

<verification>
`npm test -- tests/test-schemas.test.ts tests/test-subscribe-build-map.test.ts` passes.
</verification>

<success_criteria>
Build Map email capture is fully wired: validated form → segmented Resend contact → Sheets mirror → transactional email with a (placeholder, flagged) download link. Blog widget unaffected.
</success_criteria>

<output>
Create `.planning/phases/07-product-1-page-live-payments-revenue/07-05-SUMMARY.md` when done. **Explicitly note the placeholder Build Map deliverable in the summary's blockers/follow-ups section** so it surfaces in STATE.md.
</output>
