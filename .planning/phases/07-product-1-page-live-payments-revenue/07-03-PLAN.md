---
phase: 07-product-1-page-live-payments-revenue
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/app/(automate)/layout.tsx, src/app/(automate)/layout.module.css]
autonomous: true
requirements: [PROD-02]

must_haves:
  truths:
    - "Visiting any route under /automate renders a logo-only header and a self-contained footer, never the site-wide Navigation or ParticleWave components"
    - "The /automate footer copyright year is computed at render time, not hardcoded"
  artifacts:
    - path: "src/app/(automate)/layout.tsx"
      provides: "Route-group layout isolating /automate from site nav"
      contains: "AutomateLayout"
  key_links:
    - from: "src/app/(automate)/layout.tsx"
      to: "src/components/Navigation.tsx"
      via: "absence of import"
      pattern: "^(?!.*Navigation).*$"
---

<objective>
Build the self-contained `(automate)` route group layout: logo-only header (no site nav), own footer (Privacy/Terms/Refund links, dynamic copyright year), no `ParticleWave`/`RouteChrome`. This isolates `/automate` from the rest of the site until Phase 9 lands (per ROADMAP.md Phase 7 Note and PROD-02).

Purpose: Every page under `(automate)/` (Plan 12's page.tsx, Plan 10's success/page.tsx) renders inside this layout. Building it first (wave 1) lets those plans assume it exists.
Output: `src/app/(automate)/layout.tsx` + scoped CSS module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-product-1-page-live-payments-revenue/07-PATTERNS.md
@.planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the (automate) route group layout</name>
  <files>src/app/(automate)/layout.tsx, src/app/(automate)/layout.module.css</files>
  <read_first>
    src/app/layout.tsx (RootLayout — html/body structure, this route group layout nests INSIDE it, so do not duplicate html/body tags)
    src/components/home/Footer.tsx (existing footer structure — do not import this component; build a new isolated one)
    src/app/globals.css (Phase 6 tokens: --surface-1, --surface-2, --ink-1, --ink-2, --border-1, --spacing-*, --radius-*)
    .planning/phases/07-product-1-page-live-payments-revenue/07-UI-SPEC.md (§ Section 1: Header, § Section 9: Footer — exact spec)
  </read_first>
  <action>
    Create `src/app/(automate)/layout.tsx` exporting `default function AutomateLayout({ children }: { children: React.ReactNode })`. Do NOT wrap in `<html>`/`<body>` — this nests inside the existing RootLayout in `src/app/layout.tsx`, which already provides those. Render: (1) a `<header>` with `background: var(--surface-1)`, `border-bottom: var(--border-1)` — containing only a `<Link href="/">Asor Ahura</Link>` logo/wordmark (44px min height, no nav links, no hamburger); (2) `<main>{children}</main>`; (3) a `<footer>` with `background: var(--surface-1)`, `border-top: var(--border-1)`, `padding: var(--spacing-4)` containing: copyright line using `` `© ${new Date().getFullYear()} Asor Ahura. All rights reserved.` `` (computed, not a literal year), and links to `/privacy`, `/terms`, and `/refund` (new anchor text "Refund Policy" — link to `/automate#faq` since there is no standalone `/refund` route; PROD-13 requires refund policy visible as page text within the FAQ, not a separate route). Use `layout.module.css` for all styling (CSS Modules, matching sitewide convention) referencing the CSS custom properties from `globals.css` — do not hardcode hex colors or px spacing values that have a token equivalent. Explicitly do NOT import `Navigation`, `ParticleWave`, or `RouteChrome` from `src/components/`.
  </action>
  <acceptance_criteria>
    <criterion type="source">src/app/(automate)/layout.tsx contains no import of "@/components/Navigation", "@/components/ParticleWave", or "@/components/RouteChrome"</criterion>
    <criterion type="source">Footer copyright text uses `new Date().getFullYear()`, not a numeric literal like 2026</criterion>
    <criterion type="source">layout.module.css references at least one `var(--surface-` and one `var(--border-` custom property</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | grep -i "automate" || echo "no build errors referencing automate route"</automated>
  </verify>
  <done>Layout compiles and builds; visually renders logo-only header + footer with computed year when a child page is placed under the route group (verified once Plan 12 adds page.tsx).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Layout is static server-rendered markup, no user input, no data fetching |
</threat_model>

<verification>
`npm run build` succeeds with the new route group present (even with no page.tsx yet, Next.js will not error on an empty route group layout).
</verification>

<success_criteria>
`(automate)/layout.tsx` exists, isolates the route group from site nav, and is ready for Plans 10 and 12 to build pages inside it.
</success_criteria>

<output>
Create `.planning/phases/07-product-1-page-live-payments-revenue/07-03-SUMMARY.md` when done
</output>
