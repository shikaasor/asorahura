---
phase: 09.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/checkout.ts
  - src/lib/analytics.ts
  - tests/test-offerings-catalog.test.ts
  - tests/test-analytics.test.ts
autonomous: true
requirements: []  # No REQ-IDs exist for Phase 09.1 yet — phase was inserted urgently, REQUIREMENTS.md not updated (see STATE.md). Flagged for roadmap owner; not invented here.

must_haves:
  truths:
    - "All five automation offerings (instagram, email-triage, writing-constitution, rate-aware-invoice, client-onboarding) exist as typed, importable data with exact locked pricing"
    - "Offering #2 copy never states or implies instant multi-client Gmail onboarding"
    - "Offering #4 copy never frames the product as AI price research or AI-invented rates"
    - "Analytics can track catalog-specific interactions (card click, waitlist CTA click) using the existing trackAnalyticsEvent utility"
  artifacts:
    - path: "src/lib/checkout.ts"
      provides: "OfferingSlug type, OfferingMetadata interface, offerings record, getOfferingBySlug, getAllOfferingsSlugs, getWaitlistOfferingSlugs"
      contains: "export const offerings: Record<OfferingSlug"
    - path: "src/lib/analytics.ts"
      provides: "Offering Card Click and Waitlist CTA Click added to trackAnalyticsEvent's eventName union"
      contains: "Offering Card Click"
  key_links:
    - from: "src/lib/checkout.ts"
      to: "src/components/automate/OfferingCard.tsx (plan 09.1-02)"
      via: "import { offerings, type OfferingMetadata } from '@/lib/checkout'"
      pattern: "offerings\\["
---

<objective>
Build the single source-of-truth data layer for the five-offering automations catalog: a typed `offerings` record in `src/lib/checkout.ts` (mirroring the existing `automateTiers` pattern) carrying locked pricing, copy, and per-offering scope constraints from `09.1-CONTEXT.md`, plus two new analytics event names in `src/lib/analytics.ts` that later UI plans will fire.

This plan is the interface contract every other 09.1 plan depends on — no page or component work happens until this data shape exists and is tested.

Purpose: Centralize the five offerings' pricing/copy so no component ever hardcodes a price or misstates scope, and so the exact-figure and no-overpromise locks from CONTEXT.md are enforced by an automated test, not just doc review.
Output: `offerings` data module + extended analytics event union, both test-covered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-automations-page-catalog-of-automations-with-detail-pages-pe/09.1-CONTEXT.md
@.planning/phases/09.1-automations-page-catalog-of-automations-with-detail-pages-pe/09.1-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the offerings data module to checkout.ts</name>
  <files>src/lib/checkout.ts, tests/test-offerings-catalog.test.ts</files>
  <read_first>
    src/lib/checkout.ts (existing automateTiers/AutomateTier pattern — match its style: plain exported consts, string prices, no cents conversion)
    .planning/phases/09.1-automations-page-catalog-of-automations-with-detail-pages-pe/09.1-CONTEXT.md (locked pricing per offering, scope constraints for offering #2 and #4 — do not deviate from these figures or framing)
    .planning/phases/09.1-automations-page-catalog-of-automations-with-detail-pages-pe/09.1-UI-SPEC.md (§Card Titles & Positioning Labels, §Card Descriptions — exact catalog copy strings)
  </read_first>
  <behavior>
    - getAllOfferingsSlugs() returns exactly ["instagram","email-triage","writing-constitution","rate-aware-invoice","client-onboarding"] in that order
    - getWaitlistOfferingSlugs() returns exactly the four non-instagram slugs (excludes "instagram")
    - offerings.instagram.hasLiveCheckout === true; all four other offerings have hasLiveCheckout === false
    - offerings.instagram.setupPrice === "$500", offerings.instagram.ctaHref === "/automate/instagram", offerings.instagram.ctaLabel === "View Offering"
    - offerings["email-triage"].setupPrice === "$1,500" and offerings["email-triage"].monthlyPrice === "$250/mo"
    - offerings["writing-constitution"].setupPrice === "$600" and offerings["writing-constitution"].monthlyPrice === "$400/mo"
    - offerings["rate-aware-invoice"].setupPrice === "$1,000" and offerings["rate-aware-invoice"].monthlyPrice === "$150/mo"
    - offerings["client-onboarding"].setupPrice === "$2,000" and offerings["client-onboarding"].monthlyPrice === null
    - every waitlist offering (the four non-instagram) has ctaHref === "/engage" and ctaLabel === "Join Waitlist"
    - every waitlist offering has a non-null scopeConstraint string and faqs.length >= 3
    - offerings["rate-aware-invoice"].scopeConstraint does NOT contain the substring "AI price research" and does NOT contain "AI knows the market" (enforces the no-overpromise lock)
    - offerings["email-triage"].scopeConstraint contains the substring "Gmail" (enforces the matter-of-fact OAuth disclosure lock)
    - getOfferingBySlug("not-a-real-slug") returns null
    - getOfferingBySlug("email-triage") returns the same object as offerings["email-triage"]
  </behavior>
  <action>
    In src/lib/checkout.ts, append (do not touch the existing tiers/automateTiers exports above it):

    Export a type OfferingSlug as the union of exactly these five string literals: "instagram", "email-triage", "writing-constitution", "rate-aware-invoice", "client-onboarding".

    Export an interface OfferingFAQItem with fields question: string and answer: string.

    Export an interface OfferingMetadata with fields: id (OfferingSlug), name (string), tagline (string), description (string), heroHeadline (string), heroSubheading (string), setupPrice (string | null), setupLabel (string | null), monthlyPrice (string | null), monthlyLabel (string | null), hasLiveCheckout (boolean), ctaLabel (string), ctaHref (string), scopeConstraint (string | null), faqs (OfferingFAQItem[]).

    Export a const offerings: Record<OfferingSlug, OfferingMetadata> with exactly these five entries, using these exact field values (copy the strings verbatim, do not paraphrase or round any price):

    1. instagram: name "Instagram Comment-to-DM", tagline "The front door", description "Auto-DMs Reel commenters with a lead magnet link. Start here — it's the cheapest, fastest automation to set up.", heroHeadline "Instagram Lead Automation for Creators", heroSubheading "Stop paying by the contact. Own your list. The bill stays flat at $6/mo.", setupPrice "$500", setupLabel "DFY — one-time", monthlyPrice null, monthlyLabel null, hasLiveCheckout true, ctaLabel "View Offering", ctaHref "/automate/instagram", scopeConstraint null, faqs [] (this offering's detail page is unchanged from Phase 7 and owns its own hardcoded FAQ — this array is unused by the live page and exists only for catalog-data completeness).

    2. "email-triage": name "Email Triage on Telegram", tagline "The flagship", description "Triage 20–30 emails per day via Telegram. No new app to learn. Proven revenue driver.", heroHeadline "Email Triage on Telegram", heroSubheading "Twenty to thirty emails a day, sorted and routed to a Telegram bot you already know how to use.", setupPrice "$1,500", setupLabel "setup", monthlyPrice "$250/mo", monthlyLabel "monthly", hasLiveCheckout false, ctaLabel "Join Waitlist", ctaHref "/engage", scopeConstraint "Gmail's API verification governs how fast we can onboard each new inbox. We'll confirm your setup timeline before you commit — not promise instant multi-client rollout.", faqs: three items — Q "Do I need to learn a new app?" A "No. Triage runs through Telegram, an app you likely already have installed. There's no new dashboard to learn." / Q "How fast can this go live for my business?" A "Timeline depends on Gmail's API verification for your account. We'll confirm your specific setup timeline on the waitlist call before you commit to anything." / Q "What happens to emails it can't classify?" A "Anything outside its confidence threshold gets flagged and routed to you directly, not auto-answered."

    3. "writing-constitution": name "Writing Constitution + Content", tagline "The highest margin", description "Build a writing constitution (capture your voice), then run a content subscription on top. Highest margin per offering.", heroHeadline "Writing Constitution + Content", heroSubheading "The deliverable is the constitution — an 8-module document that captures your voice, not a promise that we'll write your posts for you.", setupPrice "$600", setupLabel "Constitution — one-off", monthlyPrice "$400/mo", monthlyLabel "Content subscription", hasLiveCheckout false, ctaLabel "Join Waitlist", ctaHref "/engage", scopeConstraint "This is not a 'we post for you' service. The constitution — an 8-module document capturing your voice — is the paid, differentiated deliverable. The content subscription runs on top of it once it exists.", faqs: three items — Q "What exactly is a writing constitution?" A "An 8-module document that captures how you write — tone, structure, do's and don'ts — so anyone (including AI) can produce content that sounds like you." / Q "Do I have to buy the content subscription too?" A "No. The constitution stands alone as a deliverable. The subscription is an optional next step once it exists." / Q "Is this the same as generic AI ghostwriting?" A "No. Most services skip the constitution and guess at your voice from a few examples. We build the document first, so every piece of content traces back to a defined standard."

    4. "rate-aware-invoice": name "Rate-Aware Invoice & Quote", tagline "The differentiator", description "Quote jobs at current market rates instead of guessing. Rate card + research + markup logic, source-cited.", heroHeadline "Rate-Aware Invoice & Quote", heroSubheading "Quote at current market rates instead of undercutting from ignorance — built on your rate card, not AI guesswork.", setupPrice "$1,000", setupLabel "setup", monthlyPrice "$150/mo", monthlyLabel "monthly", hasLiveCheckout false, ctaLabel "Join Waitlist", ctaHref "/engage", scopeConstraint "This isn't AI price research. You bring your own rate card and comparable jobs; the system structures the math, applies your markup logic, and cites every source it uses — flagging anything it isn't confident about instead of inventing a number.", faqs: three items — Q "Does the AI make up prices?" A "No. It never invents a number. It works from your rate card and comparable jobs you supply, applies your markup rules, and cites its sources." / Q "What if it isn't confident in a quote?" A "It flags low-confidence line items instead of guessing, so you know exactly where to double-check before sending a quote." / Q "Who is this built for?" A "Small businesses — construction and trades in particular — that currently quote from memory or gut feel and lose margin to it."

    5. "client-onboarding": name "Client Onboarding Agent", tagline "The expansion play", description "Provisions welcome packs, channels, and client folders in ~20 seconds. Best live demo. For agencies and consultancies.", heroHeadline "Client Onboarding Agent", heroSubheading "Watches Slack, Drive, and Zoom, then provisions a welcome pack, channel, and folder for a new client in about 20 seconds.", setupPrice "$2,000", setupLabel "one-time", monthlyPrice null, monthlyLabel null, hasLiveCheckout false, ctaLabel "Join Waitlist", ctaHref "/engage", scopeConstraint "Built for agencies and consultancies onboarding clients regularly, not the small-business self-serve buyer of the other four offerings. Widest integration surface of the five — expect the most setup coordination.", faqs: three items — Q "What does it actually provision?" A "A welcome pack, a dedicated channel, and a client folder — created automatically the moment a new client is confirmed, in about 20 seconds." / Q "Is this for solo operators or agencies?" A "Agencies and consultancies onboarding clients on a regular cadence. It's the highest-ticket offering of the five and the most complex to set up." / Q "What tools does it need access to?" A "Slack, Drive, and Zoom in your current setup. Because it touches more systems than the other offerings, onboarding takes coordination — we'll scope that with you on the waitlist call."

    Export three functions: getOfferingBySlug(slug: string): OfferingMetadata | null (looks up in offerings, returns null if not a key), getAllOfferingsSlugs(): OfferingSlug[] (Object.keys(offerings) cast to OfferingSlug[], preserving insertion order above), getWaitlistOfferingSlugs(): OfferingSlug[] (getAllOfferingsSlugs().filter((s) => !offerings[s].hasLiveCheckout)).

    Write tests/test-offerings-catalog.test.ts covering every assertion listed in `<behavior>` above, following the existing test style in tests/test-automate-checkout.test.ts (vitest describe/it, no mocking needed — pure data).
  </action>
  <acceptance_criteria>
    - `npm test -- tests/test-offerings-catalog.test.ts` exits 0 with all behavior-block assertions passing
    - src/lib/checkout.ts exports OfferingSlug, OfferingMetadata, OfferingFAQItem, offerings, getOfferingBySlug, getAllOfferingsSlugs, getWaitlistOfferingSlugs
    - `grep -c "AI price research" src/lib/checkout.ts` (after filtering comment lines with `grep -v '^\s*//'`) returns 0 — the forbidden framing never appears
    - Existing tests/test-automate-checkout.test.ts still passes unmodified (automateTiers/tiers exports untouched)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/test-offerings-catalog.test.ts tests/test-automate-checkout.test.ts</automated>
  </verify>
  <done>offerings record exists with all five entries, exact locked pricing, and copy-constraint tests pass and fail loudly if pricing or scope framing ever regresses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add catalog analytics event names</name>
  <files>src/lib/analytics.ts, tests/test-analytics.test.ts</files>
  <read_first>
    src/lib/analytics.ts (existing trackAnalyticsEvent eventName union and window.plausible dispatch pattern)
    tests/test-analytics.test.ts (existing test style to extend, not replace)
  </read_first>
  <behavior>
    - trackAnalyticsEvent("Offering Card Click", { offering_id: "instagram" }) calls window.plausible with event name "Offering Card Click" and props { offering_id: "instagram" }
    - trackAnalyticsEvent("Waitlist CTA Click", { offering_id: "email-triage" }) calls window.plausible with event name "Waitlist CTA Click" and props { offering_id: "email-triage" }
    - existing event names ("Land", "Demo Interaction", "Build Map Submit", "Checkout Opened", "Purchase", "Success Page Submit") still type-check and behave unchanged
  </behavior>
  <action>
    In src/lib/analytics.ts, extend the trackAnalyticsEvent eventName parameter's union type to add two new string literal members: "Offering Card Click" and "Waitlist CTA Click", appended to the existing union (do not remove or reorder existing members). No other logic changes — the function body already generically forwards eventName and props to window.plausible.

    Add two new `it()` blocks to tests/test-analytics.test.ts inside the existing `describe("trackAnalyticsEvent", ...)` block, following the exact pattern of the existing "calls window.plausible with the exact event name and props" test (assign window.plausible = vi.fn(), call trackAnalyticsEvent, assert toHaveBeenCalledWith the exact event name and `{ props: { offering_id: "..." } }`).
  </action>
  <acceptance_criteria>
    - `npm test -- tests/test-analytics.test.ts` exits 0, including the two new assertions
    - `npx tsc --noEmit` reports no new type errors introduced by the union extension
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/test-analytics.test.ts</automated>
  </verify>
  <done>trackAnalyticsEvent accepts "Offering Card Click" and "Waitlist CTA Click" and both are test-covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Developer-authored data → JSX render | Offering copy in checkout.ts is static, developer-written data (no user input at any point) that later plans render into JSX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-04 | Tampering / XSS | offerings record (name, description, scopeConstraint, faqs) | mitigate | All values are static string literals authored in this plan, never derived from request input; consuming components (plans 09.1-02/03/04/05) render via JSX text interpolation, which React auto-escapes — no dangerouslySetInnerHTML anywhere in this data path |
| T-09.1-SC | Tampering (supply chain) | npm installs | accept | No new packages installed this plan or phase (RESEARCH.md Package Legitimacy Audit: "No new packages in scope") |
</threat_model>

<verification>
Run `npm test -- tests/test-offerings-catalog.test.ts tests/test-analytics.test.ts` — both green. Run `npm test` (full suite) to confirm no regression in existing automate/checkout/analytics tests.
</verification>

<success_criteria>
- src/lib/checkout.ts exports a complete, typed, test-covered offerings catalog with exact CONTEXT.md pricing and non-overpromising copy for offerings #2 and #4
- src/lib/analytics.ts can track catalog-specific events
- Every downstream 09.1 plan can import `offerings`, `getOfferingBySlug`, `getAllOfferingsSlugs`, `getWaitlistOfferingSlugs` without further data authoring
</success_criteria>

<output>
Create `.planning/phases/09.1-automations-page-catalog-of-automations-with-detail-pages-pe/09.1-01-SUMMARY.md` when done
</output>
