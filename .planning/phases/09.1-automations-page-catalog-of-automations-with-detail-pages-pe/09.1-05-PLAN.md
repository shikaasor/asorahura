---
phase: 09.1
plan: 05
type: execute
wave: 3
depends_on: ["09.1-02"]
files_modified:
  - src/app/automate/page.tsx
  - src/app/automate/page.module.css
  - tests/test-automate-catalog-page.test.ts
autonomous: true
requirements: []  # No REQ-IDs exist for Phase 09.1 yet (see 09.1-01-PLAN.md note)

must_haves:
  truths:
    - "A visitor at /automate sees a catalog listing all five automation offerings as cards, not the old single-Instagram-product page"
    - "The catalog page fires the existing Land analytics event on arrival, same as Phase 7's /automate did"
    - "The catalog page does not misrepresent any offering — no refund/DFY-DWY-specific FAQ copy that only applies to Instagram appears on the shared catalog page"
  artifacts:
    - path: "src/app/automate/page.tsx"
      provides: "Catalog page: hero, five-card grid, catalog-level FAQ, closing section"
      contains: "CatalogGrid"
  key_links:
    - from: "src/app/automate/page.tsx"
      to: "src/components/automate/CatalogGrid.tsx"
      via: "import CatalogGrid from '@/components/automate/CatalogGrid'"
      pattern: "import CatalogGrid"
---

<objective>
Replace the Phase 7 single-offering `/automate/page.tsx` with the new catalog page: hero, the five-card `CatalogGrid` from plan 09.1-02, a catalog-appropriate FAQ (not the Instagram-specific one), and a closing section — per `09.1-UI-SPEC.md`'s Catalog Page spec.

Purpose: This is the ROADMAP phase's core deliverable — "a new catalog page lists all five automation offerings." Everything else this phase builds (data layer, cards, breadcrumb, detail pages) exists to be reachable from here.
Output: `/automate` renders the five-offering catalog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-automations-page-catalog-of-automations-with-detail-pages-pe/09.1-UI-SPEC.md
@src/components/automate/CatalogGrid.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite /automate/page.tsx as the catalog page</name>
  <files>src/app/automate/page.tsx, src/app/automate/page.module.css, tests/test-automate-catalog-page.test.ts</files>
  <read_first>
    src/app/automate/page.tsx (current file — this task replaces its content entirely; the file is being repointed from "offering #1 detail page" to "catalog page" per the CONTEXT.md Routing decision)
    src/app/automate/page.module.css (existing .closing/.closingContainer/.closingHeading/.closingBody classes — keep these class names and their rules, add new classes for hero/faq alongside them)
    src/app/automate/LandEventTracker.tsx (existing Suspense-wrapped tracker — reuse unchanged, same relative import "./LandEventTracker")
    src/components/automate/CatalogGrid.tsx (from plan 09.1-02)
    .planning/phases/09.1-automations-page-catalog-of-automations-with-detail-pages-pe/09.1-UI-SPEC.md (§Catalog Page: /automate — Section 2 Catalog Hero, Section 4 FAQ Section exact copy requirements)
  </read_first>
  <behavior>
    - the page's exported `metadata.title` is "Automations Catalog | Asor Ahura" and `metadata.description` is "Five ready-made automations for small businesses and creators. Start with one, scale to all five."
    - the page imports CatalogGrid from "@/components/automate/CatalogGrid"
    - the page does NOT import PricingSection, HeroSection (the Instagram-specific one from src/components/automate/HeroSection), TryItNowSection, ComparisonStrip, PhoneMockup, or FAQSection (all of those are Instagram-specific and now live only at /automate/instagram/page.tsx from plan 09.1-03)
  </behavior>
  <action>
    Rewrite src/app/automate/page.tsx: keep the `import { Suspense } from "react";`, `import type { Metadata } from "next";`, and `import { LandEventTracker } from "./LandEventTracker";` lines. Add `import CatalogGrid from "@/components/automate/CatalogGrid";`. Remove the six Instagram-specific component imports (HeroSection, TryItNowSection, ComparisonStrip, PricingSection, PhoneMockup, FAQSection) entirely — they now belong only to src/app/automate/instagram/page.tsx.

    Set `export const metadata: Metadata = { title: "Automations Catalog | Asor Ahura", description: "Five ready-made automations for small businesses and creators. Start with one, scale to all five." };`.

    Default export `AutomatePage` rendering: Suspense-wrapped LandEventTracker (unchanged from current file); a hero section (className styles.hero) containing an h1 (className styles.headline) with text "Automate Everything Automatable", a p (className styles.subheading) with text "Five ready-made automations for small businesses and creators", and a p (className styles.intro) with text "Choose the automation that fits your next growth opportunity. Start with one, scale to all five."; then `<CatalogGrid />`; then a catalog-specific FAQ section (className styles.faq) — a static (non-accordion, since this is short and doesn't need JS state) list of 3 question/answer pairs, each as an h3 (question) + p (answer): "How do I pick the right automation to start with?" / "Start with whichever offering matches the operational pain costing you the most time or money right now — Instagram Comment-to-DM has the lowest cost of entry if you want to test the model first."; "Can I get more than one automation?" / "Yes. Each offering is sold and delivered independently — buy or join the waitlist for as many as fit your business."; "What if the automation I need isn't listed here?" / "These five are what's built and proven today. Reach out via the waitlist form on any offering and describe what you need."; then reuse the existing closing section markup (className styles.closing / styles.closingContainer / styles.closingHeading / styles.closingBody) with heading "Ready to start?" and body "Pick the offering that fits your next opportunity. Start with one automation, then scale to others as your confidence grows."

    Update src/app/automate/page.module.css: keep the existing .closing/.closingContainer/.closingHeading/.closingBody rules unchanged. Add: .hero { padding: var(--spacing-6) var(--spacing-4); text-align: center; } .headline { font-size: var(--fontSize-7); font-weight: var(--fontWeight-bold); color: var(--ink-1); margin-bottom: var(--spacing-3); } .subheading { font-size: var(--fontSize-6); color: var(--ink-2); margin-bottom: var(--spacing-3); } .intro { font-size: var(--fontSize-2); color: var(--ink-2); max-width: 640px; margin: 0 auto; } .faq { padding: var(--spacing-6) var(--spacing-4); max-width: 700px; margin: 0 auto; display: flex; flex-direction: column; gap: var(--spacing-4); } .faq h3 { font-size: var(--fontSize-4); font-weight: var(--fontWeight-bold); color: var(--ink-1); margin-bottom: var(--spacing-1); } .faq p { font-size: var(--fontSize-2); color: var(--ink-2); line-height: var(--lineHeight-relaxed); }.

    Write tests/test-automate-catalog-page.test.ts: import `* as CatalogPage` from "../src/app/automate/page" and assert CatalogPage.metadata.title === "Automations Catalog | Asor Ahura" and CatalogPage.metadata.description === "Five ready-made automations for small businesses and creators. Start with one, scale to all five." Add a source assertion using node:fs readFileSync on src/app/automate/page.tsx asserting it contains "CatalogGrid" and does NOT contain "PricingSection" or "TryItNowSection" (confirms the Instagram-specific components were fully removed, not left as dead imports).
  </action>
  <acceptance_criteria>
    - `npm test -- tests/test-automate-catalog-page.test.ts` exits 0
    - `npx tsc --noEmit` reports zero errors in src/app/automate/page.tsx
    - Source assertion confirms no leftover Instagram-specific component imports remain in the catalog page file
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/test-automate-catalog-page.test.ts</automated>
  </verify>
  <done>/automate renders the five-offering catalog with correct metadata, land-event tracking preserved, and zero leaked Instagram-specific copy or components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Public marketing page | No auth, no user input processed on this page beyond client-side navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-04 | Tampering / XSS | Catalog hero/FAQ copy (static strings in this file) | mitigate | All copy is a literal string authored in this task, not derived from any request or data source — no injection surface |
| T-09.1-03 | Repudiation | LandEventTracker fires "Land" on catalog arrival | accept | Same as Phase 7 precedent — client-side marketing analytics, no PII, no trust decision depends on it |
</threat_model>

<verification>
Run `npm test -- tests/test-automate-catalog-page.test.ts` — green. Run `npm test` (full suite, after all wave-3 plans land) to confirm the whole phase's test surface is green together. Run `npx tsc --noEmit` — zero errors.
</verification>

<success_criteria>
- /automate is the five-offering catalog, not the old Instagram-only page
- No Instagram-specific copy or components remain on the catalog page
- Land event tracking continues to work exactly as it did in Phase 7
</success_criteria>

<output>
Create `.planning/phases/09.1-automations-page-catalog-of-automations-with-detail-pages-pe/09.1-05-SUMMARY.md` when done
</output>
