---
phase: 09.1
plan: 02
type: execute
wave: 2
depends_on: ["09.1-01"]
files_modified:
  - src/components/automate/Breadcrumb.tsx
  - src/components/automate/Breadcrumb.module.css
  - src/components/automate/OfferingCard.tsx
  - src/components/automate/OfferingCard.module.css
  - src/components/automate/CatalogGrid.tsx
  - src/components/automate/CatalogGrid.module.css
  - tests/test-automate-catalog-components.test.ts
autonomous: true
requirements: []  # No REQ-IDs exist for Phase 09.1 yet (see 09.1-01-PLAN.md note)

must_haves:
  truths:
    - "A visitor sees all five offerings as cards in a responsive grid, each showing name, tagline, description, exact locked price, and a CTA"
    - "Clicking an offering card's body navigates to that offering's own detail page; clicking the CTA button navigates to the correct destination (Instagram → its live page, others → /engage)"
    - "A detail page shows a breadcrumb reading 'Automations > [Offering Name]' that links back to the catalog"
  artifacts:
    - path: "src/components/automate/CatalogGrid.tsx"
      provides: "Responsive grid rendering one OfferingCard per entry in offerings"
      contains: "Object.values(offerings)"
    - path: "src/components/automate/OfferingCard.tsx"
      provides: "Single offering card: title/tagline link to detail page, separate CTA link to ctaHref"
      contains: "offering.ctaHref"
    - path: "src/components/automate/Breadcrumb.tsx"
      provides: "Automations > [Offering Name] navigation"
      contains: "Automations"
  key_links:
    - from: "src/components/automate/CatalogGrid.tsx"
      to: "src/lib/checkout.ts"
      via: "import { offerings } from '@/lib/checkout'"
      pattern: "from \"@/lib/checkout\""
    - from: "src/components/automate/OfferingCard.tsx"
      to: "src/lib/analytics.ts"
      via: "trackAnalyticsEvent(\"Offering Card Click\" | \"Waitlist CTA Click\", ...)"
      pattern: "trackAnalyticsEvent"
---

<objective>
Build the three new shared components this phase introduces (per 09.1-UI-SPEC.md "No New Components" list — these are the only three): `Breadcrumb`, `OfferingCard`, and `CatalogGrid`. These are consumed by the catalog page (09.1-05) and both detail-page routes (09.1-03, 09.1-04).

Purpose: A single, correctly-wired card and breadcrumb implementation that every offering (live-checkout or waitlist) renders through, so pricing/copy/analytics only need to be correct once.
Output: Three new components + CSS modules on the Phase 6 token system, importable by all remaining 09.1 plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-automations-page-catalog-of-automations-with-detail-pages-pe/09.1-UI-SPEC.md
@src/lib/checkout.ts
@src/lib/analytics.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Breadcrumb component</name>
  <files>src/components/automate/Breadcrumb.tsx, src/components/automate/Breadcrumb.module.css, tests/test-automate-catalog-components.test.ts</files>
  <read_first>
    src/components/automate/PricingSection.tsx (existing "use client" component pattern in this directory — file header, styles import convention)
    src/app/globals.css lines 1-46 (exact CSS custom property names: --ink-1, --ink-3, --spacing-3, --spacing-4, --fontSize-1, --fontWeight-normal — use these names verbatim, not any others)
    .planning/phases/09.1-automations-page-catalog-of-automations-with-detail-pages-pe/09.1-UI-SPEC.md (§Detail Pages → "1. Breadcrumb Navigation (NEW)" for markup, spacing, and color spec)
  </read_first>
  <behavior>
    - Breadcrumb is a function component accepting a single prop `offeringName: string`
    - Renders a `nav` with `aria-label="breadcrumb"` containing a Link to "/automate" with text "Automations", a separator, and the current offering name as plain (non-link) text
  </behavior>
  <action>
    Create src/components/automate/Breadcrumb.tsx as a "use client" component. Props interface BreadcrumbProps { offeringName: string }. Markup: nav element with aria-label="breadcrumb", containing next/link Link href="/automate" with text "Automations" and className styles.link, a span with " > " and className styles.separator, and a span with {offeringName} and className styles.current.

    Create src/components/automate/Breadcrumb.module.css using these exact token names (per globals.css): .breadcrumb { margin-bottom: var(--spacing-4); font-size: var(--fontSize-1); font-weight: var(--fontWeight-normal); line-height: var(--lineHeight-normal); color: var(--ink-3); }, .link { color: var(--ink-1); text-decoration: underline; transition: opacity 200ms ease; } .link:hover { opacity: 0.8; }, .separator { margin: 0 4px; }, .current { color: var(--ink-3); }.

    Start tests/test-automate-catalog-components.test.ts with a top-level import block for all three components built in this plan (Breadcrumb now, OfferingCard and CatalogGrid added in Task 2) — this file is shared across both tasks in this plan, do not create a second test file.

    Add a describe("Breadcrumb") block asserting: typeof Breadcrumb === "function" (module loads without throwing — validates all imports resolve cleanly in Node without a DOM, matching this project's existing node-environment vitest config which has no jsdom/React Testing Library dependency).
  </action>
  <acceptance_criteria>
    - `npm test -- tests/test-automate-catalog-components.test.ts` passes the Breadcrumb import assertion
    - `npx tsc --noEmit` reports zero errors in Breadcrumb.tsx
    - Breadcrumb.module.css contains no CSS custom property name outside the set verified in src/app/globals.css (no invented tokens like --color-ink-1 or --spacing-lg)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/test-automate-catalog-components.test.ts</automated>
  </verify>
  <done>Breadcrumb renders "Automations > [name]", links back to /automate, uses only existing design tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build OfferingCard and CatalogGrid components</name>
  <files>src/components/automate/OfferingCard.tsx, src/components/automate/OfferingCard.module.css, src/components/automate/CatalogGrid.tsx, src/components/automate/CatalogGrid.module.css, tests/test-automate-catalog-components.test.ts</files>
  <read_first>
    src/lib/checkout.ts (OfferingMetadata shape and offerings record — from plan 09.1-01, must already exist)
    src/lib/analytics.ts (trackAnalyticsEvent signature, "Offering Card Click" / "Waitlist CTA Click" — from plan 09.1-01)
    .planning/phases/09.1-automations-page-catalog-of-automations-with-detail-pages-pe/09.1-UI-SPEC.md (§Section 3: Offerings Grid — card structure, pricing block, CTA area, responsive grid breakpoints)
  </read_first>
  <behavior>
    - CatalogGrid renders exactly one OfferingCard per entry in `offerings` (5 total), in the order returned by getAllOfferingsSlugs()
    - OfferingCard's title/description area is a Link to `/automate/${offering.id}` (the detail page) — NOT nested inside the CTA link (HTML forbids nested anchors)
    - OfferingCard's CTA is a separate Link to `offering.ctaHref` with text `offering.ctaLabel`
    - Clicking the title/description Link fires trackAnalyticsEvent("Offering Card Click", { offering_id: offering.id })
    - Clicking the CTA Link fires trackAnalyticsEvent("Offering Card Click", { offering_id }) when offering.hasLiveCheckout is true, or trackAnalyticsEvent("Waitlist CTA Click", { offering_id }) when false
  </behavior>
  <action>
    Create src/components/automate/OfferingCard.tsx as a "use client" component. Props interface OfferingCardProps { offering: OfferingMetadata } (import OfferingMetadata type from "@/lib/checkout"). Structure: outer div className styles.card (NOT a Link — avoids nested-anchor invalidity). Inside: a next/link Link href={`/automate/${offering.id}`} className={styles.cardLink} onClick={() => trackAnalyticsEvent("Offering Card Click", { offering_id: offering.id })} wrapping an h3 with offering.name (className styles.title) and, if offering.tagline, a p with offering.tagline (className styles.tagline), then a p with offering.description (className styles.description). Below that, outside the cardLink Link, a pricing block div (className styles.pricing): if offering.setupPrice is not null render a p with offering.setupPrice (className styles.price) and a p with offering.setupLabel (className styles.label); if offering.monthlyPrice is not null render a second p pair the same way. Footer: a next/link Link href={offering.ctaHref} className={styles.cta} onClick handler that calls trackAnalyticsEvent with "Offering Card Click" when offering.hasLiveCheckout else "Waitlist CTA Click", both with { offering_id: offering.id }, containing text {offering.ctaLabel}.

    Create src/components/automate/OfferingCard.module.css using only tokens verified in globals.css: .card { background: var(--surface-2); border: 1px solid var(--border-2); border-radius: var(--radius-2); padding: var(--spacing-4); display: flex; flex-direction: column; gap: var(--spacing-3); } .card:hover { background: var(--surface-3); } .cardLink { text-decoration: none; color: inherit; display: flex; flex-direction: column; gap: var(--spacing-1); } .title { font-size: var(--fontSize-4); font-weight: var(--fontWeight-bold); color: var(--ink-1); } .tagline { font-size: var(--fontSize-1); color: var(--ink-3); } .description { font-size: var(--fontSize-2); color: var(--ink-2); line-height: var(--lineHeight-relaxed); } .pricing { display: flex; flex-direction: column; gap: var(--spacing-1); } .price { font-size: var(--fontSize-5); font-weight: var(--fontWeight-bold); color: var(--ink-1); } .label { font-size: var(--fontSize-1); color: var(--ink-2); } .cta { display: block; text-align: center; margin-top: auto; width: 100%; padding: var(--spacing-2) var(--spacing-3); background: var(--accent); color: var(--surface-1); border: none; border-radius: var(--radius-1); font-weight: var(--fontWeight-bold); text-decoration: none; min-height: 44px; line-height: 28px; transition: background 200ms ease; } .cta:hover { background: var(--accent-hover); }.

    Create src/components/automate/CatalogGrid.tsx as a "use client" component importing offerings from "@/lib/checkout" and OfferingCard from "./OfferingCard". Renders a section className styles.section > div className styles.container > div className styles.grid mapping Object.values(offerings) to <OfferingCard key={offering.id} offering={offering} />.

    Create src/components/automate/CatalogGrid.module.css: .section { padding: var(--spacing-6) var(--spacing-4); } .container { max-width: 1200px; margin: 0 auto; } .grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--spacing-4); } @media (max-width: 1024px) { .grid { grid-template-columns: repeat(2, 1fr); } } @media (max-width: 640px) { .grid { grid-template-columns: 1fr; gap: var(--spacing-3); padding: var(--spacing-4) var(--spacing-3); } }.

    Extend tests/test-automate-catalog-components.test.ts (from Task 1, same file — do not create a new file) with a describe("OfferingCard") block asserting typeof OfferingCard === "function", and a describe("CatalogGrid") block asserting typeof CatalogGrid === "function" plus a source assertion: read src/components/automate/OfferingCard.tsx with node:fs and assert it does NOT contain the substring "<Link href={offering.ctaHref}" nested inside the cardLink Link's children (a simple string-position check: the index of "cardLink" appears before the index of the CTA Link's opening, and the cardLink Link's closing tag `</Link>` appears before the second `<Link` — i.e., assert the file contains exactly two top-level `<Link` occurrences that are not nested, by counting `<Link` occurrences equals 2).
  </action>
  <acceptance_criteria>
    - `npm test -- tests/test-automate-catalog-components.test.ts` passes all three describe blocks
    - `npx tsc --noEmit` reports zero errors in OfferingCard.tsx and CatalogGrid.tsx
    - Source assertion confirms exactly 2 `<Link` occurrences in OfferingCard.tsx (no nested anchors)
    - CatalogGrid.tsx contains the substring `Object.values(offerings)`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/test-automate-catalog-components.test.ts</automated>
  </verify>
  <done>OfferingCard and CatalogGrid render all 5 offerings with correct per-offering CTA routing and analytics, with no invalid nested-anchor markup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| offerings data → JSX render | Card and breadcrumb components render offering.name/description/tagline/price fields authored in plan 09.1-01 |
| Client-side navigation | Card/CTA Links route to /automate/[offering-id] or /engage — both are existing or phase-owned routes, no external redirect targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-04 | Tampering / XSS | OfferingCard, Breadcrumb (render offering.name/description/tagline) | mitigate | All rendered text comes from the static offerings record (plan 09.1-01), interpolated via JSX (`{offering.name}`), never via dangerouslySetInnerHTML — React escapes all interpolated text automatically |
| T-09.1-05 | Repudiation | trackAnalyticsEvent calls in OfferingCard | accept | Client-side Plausible events are fire-and-forget marketing analytics with no PII and no server-side trust decision depending on them, consistent with Phase 7 precedent |
</threat_model>

<verification>
Run `npm test -- tests/test-automate-catalog-components.test.ts` — all describe blocks green. Run `npx tsc --noEmit` — zero new errors.
</verification>

<success_criteria>
- Three new components exist, import cleanly, and are typed against the plan 09.1-01 data contract
- Card markup has no invalid nested anchors
- Every offering's CTA routes to the correct destination per its hasLiveCheckout flag
</success_criteria>

<output>
Create `.planning/phases/09.1-automations-page-catalog-of-automations-with-detail-pages-pe/09.1-02-SUMMARY.md` when done
</output>
