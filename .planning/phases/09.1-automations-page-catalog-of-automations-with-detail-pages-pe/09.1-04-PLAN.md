---
phase: 09.1
plan: 04
type: execute
wave: 3
depends_on: ["09.1-02"]
files_modified:
  - src/app/automate/[slug]/page.tsx
  - src/app/automate/[slug]/page.module.css
  - tests/test-automate-slug-route.test.ts
autonomous: true
requirements: []  # No REQ-IDs exist for Phase 09.1 yet (see 09.1-01-PLAN.md note)

must_haves:
  truths:
    - "A visitor can reach a full detail page for each of the four non-Instagram offerings (email-triage, writing-constitution, rate-aware-invoice, client-onboarding) at /automate/[slug]"
    - "Each of those four pages shows real, offering-specific hero copy, pricing, the scope-constraint disclaimer, and FAQ — never placeholder text"
    - "None of the four pages render a Paddle checkout — they show a Join Waitlist CTA to /engage"
    - "Requesting an unknown slug returns a 404, not a crash or blank page"
  artifacts:
    - path: "src/app/automate/[slug]/page.tsx"
      provides: "Generic offering detail page template for the four waitlist offerings"
      contains: "getWaitlistOfferingSlugs"
  key_links:
    - from: "src/app/automate/[slug]/page.tsx"
      to: "src/lib/checkout.ts"
      via: "getOfferingBySlug(slug), generateStaticParams from getWaitlistOfferingSlugs()"
      pattern: "getOfferingBySlug"
    - from: "src/app/automate/[slug]/page.tsx"
      to: "src/components/automate/Breadcrumb.tsx"
      via: "Breadcrumb offeringName={offering.name}"
      pattern: "Breadcrumb offeringName"
---

<objective>
Build the dynamic `/automate/[slug]` route that serves the four offerings still deferred to a waitlist CTA (email-triage, writing-constitution, rate-aware-invoice, client-onboarding), statically generated at build time and whitelisted against `getWaitlistOfferingSlugs()` — never rendering Paddle checkout, per the locked "offerings 2–5 ship with waitlist/contact CTAs, NOT Paddle checkout" decision.

Purpose: Each of the four remaining offerings needs its own real, non-placeholder detail page per CONTEXT.md's "Copy depth" lock — this plan is where that copy (already authored in plan 09.1-01's data layer) becomes a page a visitor can actually reach.
Output: `/automate/email-triage`, `/automate/writing-constitution`, `/automate/rate-aware-invoice`, `/automate/client-onboarding` — four live, statically-generated detail pages sharing one template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-automations-page-catalog-of-automations-with-detail-pages-pe/09.1-CONTEXT.md
@.planning/phases/09.1-automations-page-catalog-of-automations-with-detail-pages-pe/09.1-UI-SPEC.md
@src/lib/checkout.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the generic offering detail page template</name>
  <files>src/app/automate/[slug]/page.tsx, src/app/automate/[slug]/page.module.css</files>
  <read_first>
    src/lib/checkout.ts (OfferingMetadata shape, getOfferingBySlug, getWaitlistOfferingSlugs — from plan 09.1-01)
    src/components/automate/Breadcrumb.tsx (from plan 09.1-02)
    src/components/automate/FAQSection.tsx (existing accordion pattern — useState openIndex, ChevronDown from lucide-react — replicate this interaction pattern inline in this page, do not import FAQSection itself since it is hardcoded to Instagram-specific Q&A)
    .planning/phases/09.1-automations-page-catalog-of-automations-with-detail-pages-pe/09.1-UI-SPEC.md (§Detail Pages → "Pricing Card for Waitlist Offerings" for the exact card structure and copy: "Ready to automate? We'll schedule a time to build this for you.")
  </read_first>
  <behavior>
    - generateStaticParams() returns exactly four entries, one per getWaitlistOfferingSlugs() member: { slug: "email-triage" }, { slug: "writing-constitution" }, { slug: "rate-aware-invoice" }, { slug: "client-onboarding" } — "instagram" is never among them
    - dynamicParams is set to false, so any slug not in that list of four 404s
    - generateMetadata({ params: { slug: "email-triage" } }) returns { title: "Email Triage on Telegram | Asor Ahura", description: offerings["email-triage"].description }
    - generateMetadata({ params: { slug: "not-a-real-slug" } }) returns {} (empty object, not an error)
    - The page component calls next/navigation's notFound() when getOfferingBySlug(slug) returns null (defense in depth even though dynamicParams=false already prevents unknown slugs from resolving)
    - The page never imports PaddleCheckout or PricingSection (those are Instagram-specific, live-checkout components)
  </behavior>
  <action>
    Create src/app/automate/[slug]/page.tsx as a server component file exporting: `export const dynamicParams = false;`; `export async function generateStaticParams()` returning `getWaitlistOfferingSlugs().map((slug) => ({ slug }))`; `export async function generateMetadata({ params }: { params: Promise<{ slug: string }> })` that awaits params, calls getOfferingBySlug(slug), and returns `{}` if null, else `{ title: \`${offering.name} | Asor Ahura\`, description: offering.description }` (Next.js 16 App Router passes params as a Promise — match the await pattern already used in this codebase's other dynamic route, src/app/blog/[slug]/page.tsx).

    Default export `OfferingPage` as an async server component receiving `{ params }: { params: Promise<{ slug: string }> }`, awaiting params, calling `const offering = getOfferingBySlug(slug)`, calling `notFound()` from "next/navigation" if offering is null.

    Render (all from src/lib/checkout.ts's offering object, all client-interactive pieces — the FAQ accordion and the CTA click handler — isolated into a small inline "use client" child component within this same file, e.g. `function OfferingDetailBody({ offering }: { offering: OfferingMetadata })`, since the outer page must stay a server component for generateStaticParams/generateMetadata to work correctly):

    1. Breadcrumb offeringName={offering.name} (import from "@/components/automate/Breadcrumb")
    2. Hero block: h1 with offering.heroHeadline, p with offering.heroSubheading
    3. Pricing/waitlist card: h2 "Pricing", a card div showing offering.setupPrice + offering.setupLabel (if not null), offering.monthlyPrice + offering.monthlyLabel (if not null), a p with "Ready to automate? We'll schedule a time to build this for you.", and if offering.scopeConstraint is not null a p styled as a disclaimer note containing that exact string, then a CTA Link href={offering.ctaHref} with onClick firing trackAnalyticsEvent("Waitlist CTA Click", { offering_id: offering.id }) (import trackAnalyticsEvent from "@/lib/analytics") containing text {offering.ctaLabel}
    4. FAQ block: h2 "FAQ", map offering.faqs to an accordion list using local useState<number | null> openIndex, replicating FAQSection.tsx's toggle-question/show-answer interaction pattern (ChevronDown icon from lucide-react, aria-expanded on the question button)
    5. Closing block: a Link back to "/automate" with text "See all 5 automations"

    Create src/app/automate/[slug]/page.module.css reusing the same token set as OfferingCard.module.css and Breadcrumb.module.css (--surface-1/2, --ink-1/2/3, --spacing-3/4/5/6, --fontSize-1 through --fontSize-7, --fontWeight-bold, --accent, --accent-hover, --border-1/2, --radius-1/2) for: page section padding, hero headline (fontSize-7)/subheading (fontSize-6), pricing card (surface-2 background, border-2, radius-2, spacing-4 padding), scope-constraint disclaimer paragraph (fontSize-1, ink-3, italic), CTA button (accent background, min-height 44px), FAQ list (matching FAQSection.module.css's question/answer/chevron classes).
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero errors in the new file
    - The file does NOT contain the substring "PaddleCheckout" or "PricingSection" (source assertion — confirms no accidental live-checkout leakage)
    - The file contains the exact string "getWaitlistOfferingSlugs"
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Four waitlist offerings each render a complete, correctly-copied detail page with no Paddle checkout present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test static generation, metadata, and 404 behavior</name>
  <files>tests/test-automate-slug-route.test.ts</files>
  <read_first>
    src/app/automate/[slug]/page.tsx (the file built in Task 1 — import generateStaticParams and generateMetadata directly, do not render the component)
  </read_first>
  <behavior>
    - importing the [slug]/page.tsx module in a plain Node/vitest context does not throw (validates all imports resolve without a DOM)
    - generateStaticParams() resolves to an array of exactly 4 objects matching { slug: "email-triage" | "writing-constitution" | "rate-aware-invoice" | "client-onboarding" }, and does not include { slug: "instagram" }
    - generateMetadata for each of the 4 known slugs returns a title ending in " | Asor Ahura" and a description matching that offering's exact description string from src/lib/checkout.ts
    - generateMetadata for an unknown slug returns an empty object
  </behavior>
  <action>
    Write tests/test-automate-slug-route.test.ts importing `* as SlugPage` from "../src/app/automate/[slug]/page" (vitest resolves TS path the same way existing tests import from "../src/lib/checkout"). Await SlugPage.generateStaticParams() and assert its length is 4 and its mapped slugs array (sorted) equals the sorted getWaitlistOfferingSlugs() output imported from "../src/lib/checkout" (avoids duplicating the literal list, keeps the test coupled to the single source of truth). For metadata, call `await SlugPage.generateMetadata({ params: Promise.resolve({ slug: "email-triage" }) })` and assert title/description against `getOfferingBySlug("email-triage")` from checkout.ts (again sourced, not hand-duplicated). Add one assertion for an unknown slug returning `{}`.
  </action>
  <acceptance_criteria>
    - `npm test -- tests/test-automate-slug-route.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/test-automate-slug-route.test.ts</automated>
  </verify>
  <done>Static generation, per-offering metadata, and the unknown-slug empty-metadata case are all automated-test-covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| URL path segment → data lookup | The `[slug]` route parameter is attacker-controllable (any string in the URL); it must never be used to construct a file path, query, or trusted identifier without validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-01 | Tampering (param tampering / slug injection) | src/app/automate/[slug]/page.tsx | mitigate | `dynamicParams = false` restricts build output to exactly the 4 whitelisted slugs from getWaitlistOfferingSlugs(); any other slug value is not statically generated and falls through to Next.js's built-in 404. Defense in depth: the page component also calls `notFound()` explicitly if `getOfferingBySlug(slug)` returns null, so even if dynamicParams is ever toggled to true in a future phase, unknown slugs still 404 rather than crash or render undefined data |
| T-09.1-04 | Tampering / XSS | offering.heroHeadline/heroSubheading/scopeConstraint/faqs rendered into JSX | mitigate | Same as plan 09.1-01/02 — all source data is static developer-authored content, rendered via JSX interpolation (React auto-escapes), no dangerouslySetInnerHTML |
</threat_model>

<verification>
Run `npm test -- tests/test-automate-slug-route.test.ts` — green. Run `npx tsc --noEmit` — zero errors.
</verification>

<success_criteria>
- All four waitlist offerings are reachable, statically generated, and metadata-correct
- No offering outside the whitelist is ever generated or served
- No Paddle checkout code path exists on this route
</success_criteria>

<output>
Create `.planning/phases/09.1-automations-page-catalog-of-automations-with-detail-pages-pe/09.1-04-SUMMARY.md` when done
</output>
