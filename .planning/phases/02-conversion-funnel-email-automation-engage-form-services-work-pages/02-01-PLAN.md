---
phase: 02-conversion-funnel
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/assessment.ts
  - src/lib/pdf.ts
  - src/lib/llm.ts
  - src/lib/email.ts
  - src/app/assessment/actions.ts
autonomous: true
requirements: [EMAIL-01, EMAIL-02, EMAIL-07, CONV-02]

must_haves:
  truths:
    - "Assessment completion triggers LLM email drafting without blocking the response"
    - "Cold leads (<40) receive education-angle email sequence; warm (40-70) case-study angle; hot (70+) urgency/checkout angle"
    - "Initial email arrives within 5 minutes of assessment completion with PDF attached"
    - "Follow-up emails scheduled at Day 3, 7, 14, 30 via Resend's scheduledAt"
    - "PDF is branded (Asor colors, professional) with score + category breakdown + personalized next step"
  artifacts:
    - path: "src/lib/llm.ts"
      provides: "draftEmailSequence() returning 5 LLM-personalized EmailDraft objects"
      exports: ["draftEmailSequence", "EmailDraft"]
    - path: "src/lib/assessment.ts"
      provides: "getSegment() helper returning cold|warm|hot from score"
      contains: "getSegment"
    - path: "src/lib/pdf.ts"
      provides: "generateAssessmentPDF() returning Buffer with branded PDFKit output"
      exports: ["generateAssessmentPDF"]
    - path: "src/lib/email.ts"
      provides: "sendAssessmentEmailSequence() firing initial+4 scheduled emails with PDF"
      exports: ["sendAssessmentEmailSequence"]
    - path: "src/app/assessment/actions.ts"
      provides: "submitAssessmentForEmail calls LLM+email fire-and-forget (void, non-blocking)"
      contains: "void"
  key_links:
    - from: "src/app/assessment/actions.ts"
      to: "src/lib/llm.ts"
      via: "fire-and-forget void call to draftEmailSequence"
      pattern: "void draftAndSend"
    - from: "src/lib/llm.ts"
      to: "generativelanguage.googleapis.com"
      via: "openai SDK with custom baseURL"
      pattern: "baseURL.*generativelanguage"
    - from: "src/lib/email.ts"
      to: "resend.emails.send"
      via: "individual sends with scheduledAt"
      pattern: "scheduledAt"
---

<objective>
Build the LLM-personalized email automation backend: Gemini LLM client via OpenAI SDK, upgraded branded PDF generator, score segmentation helper, and email delivery with per-segment sequences and Resend scheduling.

Purpose: Assessment completion must trigger a personalized email sequence (immediate PDF + 4 follow-ups) without ever blocking the user's results display. Each segment (cold/warm/hot) receives distinct content angles.

Output: src/lib/llm.ts, updated src/lib/assessment.ts, updated src/lib/pdf.ts, updated src/lib/email.ts, updated src/app/assessment/actions.ts
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-conversion-funnel-email-automation-engage-form-services-work-pages/02-RESEARCH.md
@src/lib/assessment.ts
@src/lib/email.ts
@src/app/assessment/actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: LLM client, segmentation helper, and upgraded PDF generator</name>
  <files>
    src/lib/llm.ts
    src/lib/assessment.ts
    src/lib/pdf.ts
  </files>
  <action>
**Install packages first:**
```
npm install openai pdfkit @types/pdfkit --legacy-peer-deps
```

**src/lib/llm.ts — create new file:**
- Import OpenAI from "openai"
- Create client with `apiKey: process.env.GEMINI_API_KEY` and `baseURL: "https://generativelanguage.googleapis.com/v1beta/openai/"`
- Export type `EmailDraft { subject: string; body: string; day: number }`
- Export type `EmailSequence { initial: EmailDraft; day3: EmailDraft; day7: EmailDraft; day14: EmailDraft; day30: EmailDraft }`
- Export async `draftEmailSequence(params: { firstName: string; email: string; score: number; segment: "cold" | "warm" | "hot"; tier: string; tierDescription: string; previewBullets: string[] }): Promise<EmailSequence>`
- Build a single LLM call with `response_format: { type: "json_object" }` and model `process.env.LLM_MODEL || "gemini-2.0-flash"`
- System prompt: "You are Asor Ahura, an AI systems consultant. Draft a personalized email sequence for a prospect. Return valid JSON with keys: initial, day3, day7, day14, day30. Each has: subject (string), body (string with \n for line breaks), day (number)."
- User prompt: build from params — include segment-specific angle instructions:
  - cold (<40): "Education angle — 'What businesses like yours are saving/earning with AI' — opportunity cost framing. Don't pitch services yet."
  - warm (40-70): "Case study angle — proof of what others with similar AI readiness scores achieved. Show social proof."
  - hot (70+): "Urgency angle — they're ready to build now. Primary CTA: go directly to https://asorahura.com/checkout?tier=strategy. Don't suggest free options."
- Include firstName, score, tier, tierDescription, previewBullets in the prompt context
- Initial email tone: "Personal from Asor, conversational 1-on-1, not a formal notification. Frame: 'Hey [Name], here's your AI Readiness Report. Based on your score of X, here's what I'd suggest...'"
- Wrap JSON parse in try/catch — if parse fails, return a static fallback EmailSequence with generic copy so email delivery never fails
- Add `export const maxDuration = 30` at top of file (Vercel function timeout)

**src/lib/assessment.ts — add getSegment() after existing exports:**
```typescript
export function getSegment(score: number): "cold" | "warm" | "hot" {
  if (score < 40) return "cold";
  if (score < 70) return "warm";
  return "hot";
}
```

**src/lib/pdf.ts — create new file (replaces any existing placeholder):**
- Import PDFDocument from "pdfkit"
- Export async `generateAssessmentPDF(params: { firstName: string; score: number; tier: string; tierDescription: string; segment: "cold" | "warm" | "hot"; previewBullets: string[]; answers: Record<number, string> }): Promise<Buffer>`
- Use PDFKit with `{ size: "A4", margin: 50 }` and wrap in Promise resolving on "end" event (NOT after doc.end() — PDFKit is async)
- Brand styling: background dark (#0a0a0a), accent gold (#C9A84C) for headings
- Sections to include:
  1. Header bar: "AI READINESS REPORT" + "asorahura.com" right-aligned
  2. Score block: large score number "/100" and tier name below
  3. Tier description paragraph
  4. "Your Top Opportunities" section with 3 bullet points from previewBullets
  5. Score interpretation table: show what the 4 tier ranges mean (Early Stage / Pre-Deployment / Deployment Ready / Advanced Optimization)
  6. Personalized next step based on segment:
     - cold: "Step 1: Take the free assessment (done). Step 2: Watch how similar businesses automated their top 3 manual tasks."
     - warm: "Book a Discovery Call — see how others at your readiness level achieved 40-60% reduction in manual effort."
     - hot: "You're ready to build. Book a Strategy Session to define your exact build scope: asorahura.com/checkout?tier=strategy"
  7. Footer: "Prepared by Asor Ahura | asorahura.com | hello@asorahura.com"
- Use `.fillColor()`, `.fontSize()`, `.font()` for styling. Use `doc.moveDown()` between sections.
- Collect chunks with `doc.on("data", ...)`, resolve Buffer in `doc.on("end", ...)`
- Call `doc.end()` after all content is written
  </action>
  <verify>npx tsc --noEmit 2>&amp;&amp; echo "TypeScript clean"</verify>
  <done>TypeScript passes. src/lib/llm.ts, src/lib/assessment.ts (with getSegment), and src/lib/pdf.ts exist with correct exports. No compilation errors.</done>
</task>

<task type="auto">
  <name>Task 2: Email delivery with scheduling and assessment action wiring</name>
  <files>
    src/lib/email.ts
    src/app/assessment/actions.ts
  </files>
  <action>
**src/lib/email.ts — rewrite to support full sequence with PDF and scheduling:**

Keep existing Resend import and client. Replace `sendAssessmentEmail` with two functions:

1. `sendAssessmentEmail` (keep signature, update implementation) — existing function stays for backwards compat but is now used as initial delivery only:
   - Add `pdf?: Buffer` parameter to params
   - If pdf Buffer provided, add `attachments: [{ filename: "AI-Readiness-Report.pdf", content: pdf }]`
   - Update subject: `Your AI Readiness Report — Score: ${params.score}/100`
   - Body: Use LLM-drafted initial email body if passed, else fallback React Email template
   - Remove `react:` field when using text body — use `html:` with simple `<pre>` wrapped body or `text:` field. Use `text:` for plain text body from LLM.

2. Export new function `sendAssessmentEmailSequence(params: { email: string; firstName: string; score: number; tier: string; tierDescription: string; previewBullets: string[]; answers: Record<number, string> }): Promise<void>`:
   - Import `draftEmailSequence` from "@/lib/llm"
   - Import `generateAssessmentPDF` from "@/lib/pdf"
   - Import `getSegment` from "@/lib/assessment"
   - Step 1: compute `segment = getSegment(score)`
   - Step 2: call `generateAssessmentPDF(...)` — branded PDF Buffer
   - Step 3: call `draftEmailSequence(...)` — LLM-personalized drafts
   - Step 4: send initial email immediately via `resend.emails.send({ ..., attachments: [{ filename: "AI-Readiness-Report.pdf", content: pdf }], text: sequence.initial.body, subject: sequence.initial.subject })`
   - Step 5: send 4 follow-ups with `scheduledAt`. DO NOT use batch endpoint — send each individually:
     - `resend.emails.send({ ..., text: sequence.day3.body, subject: sequence.day3.subject, scheduledAt: "in 3 days" })`
     - `resend.emails.send({ ..., text: sequence.day7.body, subject: sequence.day7.subject, scheduledAt: "in 7 days" })`
     - `resend.emails.send({ ..., text: sequence.day14.body, subject: sequence.day14.subject, scheduledAt: "in 14 days" })`
     - `resend.emails.send({ ..., text: sequence.day30.body, subject: sequence.day30.subject, scheduledAt: "in 30 days" })`
   - All 5 sends wrapped in try/catch individually — one failure doesn't break others
   - Return void — errors are logged, not thrown

**src/app/assessment/actions.ts — add fire-and-forget call:**
- Import `sendAssessmentEmailSequence` from "@/lib/email"
- After computing score/tier, add: `void sendAssessmentEmailSequence({ email, firstName, score, tier, tierDescription, previewBullets, answers })` — this MUST be fire-and-forget (never awaited)
- Keep the existing `sendAssessmentEmail` call removal or consolidate — the sequence function now handles everything including the initial email, so remove the old `sendAssessmentEmail` call
- Add `export const maxDuration = 30` at top of file (Next.js Server Action function timeout for Vercel)
- The return statement `{ success: true, score, tier }` executes immediately without waiting for the sequence
  </action>
  <verify>npx tsc --noEmit 2>&amp;&amp; echo "TypeScript clean"</verify>
  <done>TypeScript passes. Assessment action fires email sequence void (non-blocking). Email function sends PDF-attached initial email plus 4 scheduled follow-ups via Resend. Each segment (cold/warm/hot) gets distinct LLM-drafted content.</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — zero errors in the 5 modified/created files.
Run `npm run build` — build succeeds with no errors in email, llm, pdf, or assessment files.
Verify getSegment(35) === "cold", getSegment(55) === "warm", getSegment(75) === "hot".
</verification>

<success_criteria>
1. `src/lib/llm.ts` exists with draftEmailSequence export and Gemini-via-OpenAI-SDK client
2. `src/lib/assessment.ts` exports getSegment()
3. `src/lib/pdf.ts` exists with generateAssessmentPDF() returning Promise&lt;Buffer&gt;
4. `src/lib/email.ts` exports sendAssessmentEmailSequence() with 5 individual Resend sends (initial + day3/7/14/30)
5. `src/app/assessment/actions.ts` fires sequence void (non-blocking) — score returns immediately
6. TypeScript compiles clean across all 5 files
</success_criteria>

<output>
After completion, create `.planning/phases/02-conversion-funnel-email-automation-engage-form-services-work-pages/02-01-SUMMARY.md`
</output>
