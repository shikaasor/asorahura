---
phase: 03-lead-nurture-blog-launch-extended-email-sequences-segmentation
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/app/blog/page.tsx
  - src/app/blog/page.module.css
  - src/app/blog/[slug]/page.tsx
  - src/app/blog/[slug]/article.module.css
  - src/components/blog/BlogCTABlock.tsx
  - src/components/blog/CategoryFilter.tsx
autonomous: true
requirements:
  - BLOG-01
  - BLOG-02
  - BLOG-03
  - BLOG-04
  - BLOG-05
  - CONTENT-02
  - CONTENT-03

must_haves:
  truths:
    - "Visiting /blog renders a card grid (1 col mobile, 2 col tablet, 3 col desktop) showing all 4 case study posts"
    - "Each blog card shows: cover image, title, excerpt, reading time (e.g. '8 min read'), and a 'Case Study' category tag"
    - "Category filter pills appear above the grid and clicking 'Case Study' keeps all 4 cards visible"
    - "Visiting /blog/cervical-cancer-screening-tool renders the full MDX article with the post title, category, and reading time in the header"
    - "End of every blog post has a dark-box CTA block — case study posts show 'See how I can do this for your business' linking to /engage"
    - "No date is shown on any blog card"
  artifacts:
    - path: "src/app/blog/page.tsx"
      provides: "Blog listing page — Server Component"
      contains: "getBlogPosts"
    - path: "src/app/blog/[slug]/page.tsx"
      provides: "Individual article page — Server Component with MDX rendering"
      contains: "getBlogPostBySlug"
    - path: "src/components/blog/BlogCTABlock.tsx"
      provides: "Reusable end-of-post CTA block"
      exports: ["BlogCTABlock"]
    - path: "src/components/blog/CategoryFilter.tsx"
      provides: "Category filter pills — client component"
      exports: ["CategoryFilter"]
  key_links:
    - from: "src/app/blog/page.tsx"
      to: "src/lib/blog.ts"
      via: "getBlogPosts() import"
      pattern: "getBlogPosts"
    - from: "src/app/blog/[slug]/page.tsx"
      to: "src/lib/blog.ts"
      via: "getBlogPostBySlug(slug)"
      pattern: "getBlogPostBySlug"
    - from: "src/app/blog/[slug]/page.tsx"
      to: "src/components/blog/BlogCTABlock.tsx"
      via: "BlogCTABlock type prop from frontmatter"
      pattern: "BlogCTABlock"
    - from: "src/components/blog/BlogCTABlock.tsx"
      to: "/engage"
      via: "Link href for case-study type"
      pattern: "href.*engage"
    - from: "src/components/blog/BlogCTABlock.tsx"
      to: "/assessment"
      via: "Link href for educational type"
      pattern: "href.*assessment"
---

<objective>
Build the blog listing page (/blog), individual article page (/blog/[slug]), BlogCTABlock component, and CategoryFilter component. Wires to src/lib/blog.ts from Plan 01.

Purpose: Blog becomes the second funnel driver. Readers land on /blog, filter by category, read a case study, and hit the CTA block at the end routing to /engage or /assessment.
Output: Fully functional /blog and /blog/[slug] routes, reusable BlogCTABlock component.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-lead-nurture-blog-launch-extended-email-sequences-segmentation/03-CONTEXT.md
@.planning/phases/03-lead-nurture-blog-launch-extended-email-sequences-segmentation/03-RESEARCH.md
@.planning/phases/03-lead-nurture-blog-launch-extended-email-sequences-segmentation/03-01-SUMMARY.md
@src/lib/blog.ts
@src/app/services/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build BlogCTABlock and CategoryFilter components</name>
  <files>src/components/blog/BlogCTABlock.tsx, src/components/blog/CategoryFilter.tsx</files>
  <action>
Create src/components/blog/BlogCTABlock.tsx:

```typescript
import Link from 'next/link';

interface BlogCTABlockProps {
  type: 'case-study' | 'educational';
}

export default function BlogCTABlock({ type }: BlogCTABlockProps) {
  const isCaseStudy = type === 'case-study';
  return (
    <div style={{
      backgroundColor: '#0a0a0a',
      border: '1px solid #1f1f1f',
      borderRadius: '12px',
      padding: '2.5rem',
      marginTop: '4rem',
      textAlign: 'center',
    }}>
      <p style={{ color: '#a3a3a3', fontSize: '0.875rem', textTransform: 'uppercase', letterSpacing: '0.1em', marginBottom: '0.75rem' }}>
        {isCaseStudy ? 'Ready to build?' : 'Find out where you stand'}
      </p>
      <h3 style={{ color: '#ffffff', fontSize: '1.5rem', fontWeight: 700, marginBottom: '1rem' }}>
        {isCaseStudy
          ? 'See how I can do this for your business.'
          : 'Take the Free AI Readiness Assessment.'}
      </h3>
      <p style={{ color: '#a3a3a3', fontSize: '1rem', marginBottom: '2rem', maxWidth: '480px', margin: '0 auto 2rem' }}>
        {isCaseStudy
          ? 'The same architecture principles apply to your operations. Let\'s talk about what AI can automate for you.'
          : 'Find out your AI readiness score and get a personalised report in under 5 minutes.'}
      </p>
      <Link
        href={isCaseStudy ? '/engage' : '/assessment'}
        style={{
          display: 'inline-block',
          backgroundColor: '#ffffff',
          color: '#0a0a0a',
          padding: '0.875rem 2rem',
          borderRadius: '8px',
          fontWeight: 600,
          fontSize: '0.9375rem',
          textDecoration: 'none',
        }}
      >
        {isCaseStudy ? 'Work With Me' : 'Take the Assessment →'}
      </Link>
    </div>
  );
}
```

Create src/components/blog/CategoryFilter.tsx:

```typescript
'use client';

interface CategoryFilterProps {
  categories: string[];
  active: string;
  onChange: (category: string) => void;
}

export default function CategoryFilter({ categories, active, onChange }: CategoryFilterProps) {
  const all = ['All', ...categories];
  return (
    <div style={{ display: 'flex', gap: '0.5rem', flexWrap: 'wrap', marginBottom: '2.5rem' }}>
      {all.map((cat) => (
        <button
          key={cat}
          onClick={() => onChange(cat)}
          style={{
            padding: '0.375rem 1rem',
            borderRadius: '9999px',
            border: '1px solid',
            borderColor: active === cat ? '#ffffff' : '#2a2a2a',
            backgroundColor: active === cat ? '#ffffff' : 'transparent',
            color: active === cat ? '#0a0a0a' : '#a3a3a3',
            fontSize: '0.875rem',
            fontWeight: 500,
            cursor: 'pointer',
            transition: 'all 0.15s ease',
          }}
        >
          {cat}
        </button>
      ))}
    </div>
  );
}
```
  </action>
  <verify>Run `npx tsc --noEmit` — no errors in the two new component files. Confirm `src/components/blog/` directory exists with both files.</verify>
  <done>BlogCTABlock renders a dark CTA box with appropriate heading and Link. CategoryFilter renders pill buttons with 'All' prepended. Both compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Build /blog listing page with card grid and category filtering</name>
  <files>src/app/blog/page.tsx, src/app/blog/page.module.css</files>
  <action>
Create src/app/blog/page.tsx as a Server Component. Category state lives client-side in a wrapper. Pattern: Server Component fetches posts, passes to a thin client component that manages filter state.

```typescript
import { getBlogPosts } from '@/lib/blog';
import BlogListingClient from './BlogListingClient';

export default async function BlogPage() {
  const posts = await getBlogPosts();
  return <BlogListingClient posts={posts} />;
}
```

Create src/app/blog/BlogListingClient.tsx (client component for filter state):

```typescript
'use client';

import Link from 'next/link';
import Image from 'next/image';
import { useState } from 'react';
import CategoryFilter from '@/components/blog/CategoryFilter';
import type { BlogPost } from '@/lib/blog';
import styles from './page.module.css';

export default function BlogListingClient({ posts }: { posts: BlogPost[] }) {
  const categories = [...new Set(posts.map(p => p.category))];
  const [active, setActive] = useState('All');
  const filtered = active === 'All' ? posts : posts.filter(p => p.category === active);

  return (
    <main className={styles.main}>
      <section className={styles.hero}>
        <div className="container">
          <p className={styles.eyebrow}>Insights</p>
          <h1 className={styles.headline}>Case Studies & AI Insights.</h1>
          <p className={styles.subhead}>
            Real projects. Real architectures. Real outcomes. Engineering decisions behind production AI systems.
          </p>
        </div>
      </section>

      <section className={styles.listing}>
        <div className="container">
          <CategoryFilter categories={categories} active={active} onChange={setActive} />
          <div className={styles.grid}>
            {filtered.map((post) => (
              <Link key={post.slug} href={`/blog/${post.slug}`} className={styles.card}>
                <div className={styles.cardImage}>
                  <Image
                    src={post.coverImage}
                    alt={post.title}
                    fill
                    style={{ objectFit: 'cover' }}
                    sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"
                  />
                </div>
                <div className={styles.cardBody}>
                  <span className={styles.categoryTag}>{post.category}</span>
                  <h2 className={styles.cardTitle}>{post.title}</h2>
                  <p className={styles.cardExcerpt}>{post.excerpt}</p>
                  <p className={styles.readingTime}>{post.readingTime} min read</p>
                </div>
              </Link>
            ))}
          </div>
        </div>
      </section>

      <footer className={styles.footer}>
        <div className="container">
          <p>&copy; {new Date().getFullYear()} Asor Ahura. All rights reserved.</p>
        </div>
      </footer>
    </main>
  );
}
```

Create src/app/blog/page.module.css with dark aesthetic matching the site (reference src/app/services/page.tsx styles as the convention — dark background, white text, clean spacing):

```css
.main {
  background: #0a0a0a;
  min-height: 100vh;
  color: #ffffff;
}

.hero {
  padding: 5rem 0 3rem;
}

.eyebrow {
  font-size: 0.8125rem;
  font-weight: 600;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: #a3a3a3;
  margin-bottom: 1rem;
}

.headline {
  font-size: clamp(2rem, 5vw, 3.5rem);
  font-weight: 800;
  line-height: 1.1;
  color: #ffffff;
  margin-bottom: 1.25rem;
}

.subhead {
  font-size: 1.125rem;
  color: #a3a3a3;
  max-width: 600px;
  line-height: 1.7;
}

.listing {
  padding: 2rem 0 5rem;
}

.grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 2rem;
}

@media (min-width: 768px) {
  .grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (min-width: 1024px) {
  .grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

.card {
  display: flex;
  flex-direction: column;
  background: #111111;
  border: 1px solid #1f1f1f;
  border-radius: 12px;
  overflow: hidden;
  text-decoration: none;
  color: inherit;
  transition: border-color 0.2s ease, transform 0.2s ease;
}

.card:hover {
  border-color: #333333;
  transform: translateY(-2px);
}

.cardImage {
  position: relative;
  width: 100%;
  aspect-ratio: 16 / 9;
  background: #1a1a1a;
  overflow: hidden;
}

.cardBody {
  padding: 1.5rem;
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
  flex: 1;
}

.categoryTag {
  display: inline-block;
  font-size: 0.75rem;
  font-weight: 600;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: #a3a3a3;
  border: 1px solid #2a2a2a;
  border-radius: 9999px;
  padding: 0.25rem 0.75rem;
  width: fit-content;
}

.cardTitle {
  font-size: 1.0625rem;
  font-weight: 700;
  line-height: 1.4;
  color: #ffffff;
  margin: 0;
}

.cardExcerpt {
  font-size: 0.9rem;
  color: #a3a3a3;
  line-height: 1.6;
  margin: 0;
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.readingTime {
  font-size: 0.8125rem;
  color: #666666;
  margin: 0;
  margin-top: auto;
}

.footer {
  border-top: 1px solid #1a1a1a;
  padding: 2rem 0;
  text-align: center;
  color: #666666;
  font-size: 0.875rem;
}
```

Note: next/image with `fill` requires the parent (.cardImage) to have `position: relative`. The CSS above sets this via the `.cardImage` rule. The `next.config.mjs` already has `remotePatterns` configured — since coverImages are local paths (/site-images/*), no additional config is needed.
  </action>
  <verify>
Run `npm run dev` and visit http://localhost:3000/blog — the page renders with cards. Each card shows a cover image, title, excerpt, reading time, and category tag. Category filter pills are visible above the grid. Clicking 'Case Study' keeps all 4 cards.
  </verify>
  <done>
/blog renders all 4 case study posts in a responsive card grid. CategoryFilter pill buttons are functional. No date visible on any card. Cover images load from /site-images/*.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build /blog/[slug] article page with MDX rendering and CTA block</name>
  <files>src/app/blog/[slug]/page.tsx, src/app/blog/[slug]/article.module.css</files>
  <action>
Create src/app/blog/[slug]/page.tsx using next-mdx-remote/rsc compileMDX for server-side MDX rendering:

```typescript
import { notFound } from 'next/navigation';
import Image from 'next/image';
import Link from 'next/link';
import { ArrowLeft } from 'lucide-react';
import { compileMDX } from 'next-mdx-remote/rsc';
import { getBlogPostBySlug, getBlogPosts } from '@/lib/blog';
import BlogCTABlock from '@/components/blog/BlogCTABlock';
import styles from './article.module.css';

export async function generateStaticParams() {
  const posts = await getBlogPosts();
  return posts.map((p) => ({ slug: p.slug }));
}

export default async function BlogArticlePage({
  params,
}: {
  params: Promise<{ slug: string }>;
}) {
  const { slug } = await params;
  const post = await getBlogPostBySlug(slug);
  if (!post) notFound();

  const { content } = await compileMDX({ source: post.content });

  return (
    <main className={styles.main}>
      <div className="container">
        <div className={styles.layout}>
          <Link href="/blog" className={styles.backLink}>
            <ArrowLeft size={16} /> All Articles
          </Link>

          <header className={styles.header}>
            <div className={styles.meta}>
              <span className={styles.categoryTag}>{post.category}</span>
              <span className={styles.readingTime}>{post.readingTime} min read</span>
            </div>
            <h1 className={styles.title}>{post.title}</h1>
            <p className={styles.subtitle}>{post.excerpt}</p>
          </header>

          {post.coverImage && (
            <div className={styles.coverImageWrapper}>
              <Image
                src={post.coverImage}
                alt={post.title}
                fill
                style={{ objectFit: 'cover' }}
                priority
                sizes="(max-width: 1200px) 100vw, 900px"
              />
            </div>
          )}

          <article className={styles.body}>{content}</article>

          <BlogCTABlock
            type={post.category === 'Case Study' ? 'case-study' : 'educational'}
          />
        </div>
      </div>

      <footer className={styles.footer}>
        <div className="container">
          <p>&copy; {new Date().getFullYear()} Asor Ahura. All rights reserved.</p>
        </div>
      </footer>
    </main>
  );
}
```

Create src/app/blog/[slug]/article.module.css with a clean long-form reading layout (dark background, comfortable line length, readable typography):

```css
.main {
  background: #0a0a0a;
  min-height: 100vh;
  color: #ffffff;
  padding-bottom: 4rem;
}

.layout {
  max-width: 780px;
  margin: 0 auto;
  padding: 3rem 1.5rem;
}

.backLink {
  display: inline-flex;
  align-items: center;
  gap: 0.375rem;
  color: #a3a3a3;
  font-size: 0.875rem;
  text-decoration: none;
  margin-bottom: 2.5rem;
  transition: color 0.15s ease;
}

.backLink:hover {
  color: #ffffff;
}

.header {
  margin-bottom: 2rem;
}

.meta {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  margin-bottom: 1rem;
}

.categoryTag {
  font-size: 0.75rem;
  font-weight: 600;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: #a3a3a3;
  border: 1px solid #2a2a2a;
  border-radius: 9999px;
  padding: 0.25rem 0.75rem;
}

.readingTime {
  font-size: 0.8125rem;
  color: #666666;
}

.title {
  font-size: clamp(1.75rem, 4vw, 2.75rem);
  font-weight: 800;
  line-height: 1.15;
  color: #ffffff;
  margin-bottom: 1rem;
}

.subtitle {
  font-size: 1.0625rem;
  color: #a3a3a3;
  line-height: 1.7;
}

.coverImageWrapper {
  position: relative;
  width: 100%;
  aspect-ratio: 16 / 9;
  border-radius: 12px;
  overflow: hidden;
  background: #1a1a1a;
  margin: 2rem 0;
}

.body {
  line-height: 1.8;
  font-size: 1.0625rem;
  color: #d4d4d4;
}

.body h2 {
  font-size: 1.375rem;
  font-weight: 700;
  color: #ffffff;
  margin: 2.5rem 0 1rem;
  line-height: 1.3;
}

.body p {
  margin-bottom: 1.25rem;
  color: #d4d4d4;
}

.body strong {
  color: #ffffff;
}

.footer {
  border-top: 1px solid #1a1a1a;
  padding: 2rem 0;
  text-align: center;
  color: #666666;
  font-size: 0.875rem;
}
```

Note on image path with spaces: The chatbotly cover image path `/site-images/nueral network and multilingual flow.png` contains spaces. Next.js Image handles this correctly — the space gets URL-encoded automatically. If it fails, encode it in the MDX frontmatter as `/site-images/nueral%20network%20and%20multilingual%20flow.png`.
  </action>
  <verify>
Run `npm run dev` and visit http://localhost:3000/blog/cervical-cancer-screening-tool — article renders with title, category tag, reading time, cover image, MDX body, and dark CTA box at the end. CTA box shows 'Work With Me' linking to /engage (case study post type).
  </verify>
  <done>
/blog/[slug] renders MDX content from /content/blog/. Article header shows category + reading time. BlogCTABlock appears at end — case study posts link to /engage, educational posts link to /assessment. generateStaticParams exports all 4 slugs.
  </done>
</task>

</tasks>

<verification>
1. Visit http://localhost:3000/blog — all 4 cards render with images, titles, excerpts, reading times, category tags; no dates visible
2. Click 'Case Study' filter pill — all 4 cards remain; no cards are hidden
3. Visit http://localhost:3000/blog/cervical-cancer-screening-tool — article renders, dark CTA block present at bottom
4. Verify CTA block: case study post → 'Work With Me' link to /engage
5. Visit http://localhost:3000/articles — redirects to /blog (307)
6. Visit http://localhost:3000/articles/lloyds-list-ocr-pipeline — redirects to /blog/lloyds-list-ocr-pipeline
7. `npx tsc --noEmit` — zero errors
</verification>

<success_criteria>
- /blog listing page renders responsive 3-column card grid with category filtering
- /blog/[slug] renders MDX articles with BlogCTABlock at end
- BlogCTABlock routes case-study → /engage, educational → /assessment
- No date visible on any card
- Reading time visible on cards and article header
- Category filter dynamically derived from post frontmatter
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-lead-nurture-blog-launch-extended-email-sequences-segmentation/03-02-SUMMARY.md`
</output>
