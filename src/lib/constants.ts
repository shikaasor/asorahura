export const CALENDLY_URL = "https://calendly.com/asorahura";

/** The organization's one public address: every sender line, every contact
 *  link, and anything printed into a deliverable resolves here. It was spread
 *  across six files as three different addresses before. */
export const SUPPORT_EMAIL = "support@asorahura.com";

/** Sender line for anything Resend sends on our behalf. */
export const SUPPORT_FROM = `Asor Ahura <${SUPPORT_EMAIL}>`;
