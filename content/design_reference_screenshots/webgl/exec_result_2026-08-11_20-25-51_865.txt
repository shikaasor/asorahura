{"summary":[{"attrs":["position","uv"],"customHead":[";\n#ifdef USE_UV1\n\tattribute vec2 uv1;\n#endif\n#ifdef USE_UV2\n\tattribute vec2 uv2;\n#endif\n#ifdef USE_UV3\n\tattribute vec2 uv3;\n#endif\n#ifdef USE_TANGENT\n\tattribute vec4 tangent;\n#endif\n#if defined( USE_COLOR_ALPHA )\n\tattribute vec4 color;\n#elif defined( USE_COLOR )\n\tattribute vec3 color;\n#endif\n#ifdef USE_SKINNING\n\tattribute vec4 skinIndex;\n\tattribute vec4 skinWeight;\n#endif\n\nvarying vec2 vUv;\n\nvoid main() {\n    vUv = uv;\n    gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);\n}","pixelate(vUv, PIXEL_SIZE);\n\n    vec3 color = vec3(0.0);\n    float trail = 0.0;\n\n    for (int i = 0; i \u003c TRAIL_LENGTH; i++) {\n        float size = uVelocity * 0.025 * (1.0 - float(i) / float(TRAIL_LENGTH));\n        float strength = uTrailStrength[i] * (1.0 - float(i) / float(TRAIL_LENGTH));\n        trail += circle(uv, uTrail[i], size) * strength;\n    }\n\n    trail = clamp(trail, 0.0, 1.0);\n    color = mix(vec3(0.0), vec3(1.0, 1.0, 1.0), trail);\n\n    gl_FragColor = vec4(color, trail * uVelocity);\n}"],"displacement":true,"i":0,"pointSize":false,"shaderLengths":[1641,3035],"uniforms":["modelViewMatrix","projectionMatrix","uTrail[0]","uTrailStrength[0]","uVelocity"]},{"attrs":["position","normal"],"customHead":["if\n#ifdef USE_TANGENT\n\tattribute vec4 tangent;\n#endif\n#if defined( USE_COLOR_ALPHA )\n\tattribute vec4 color;\n#elif defined( USE_COLOR )\n\tattribute vec3 color;\n#endif\n#ifdef USE_SKINNING\n\tattribute vec4 skinIndex;\n\tattribute vec4 skinWeight;\n#endif\n\nvarying vec3 vNormal;\nvarying vec3 vPosition;\n\nvoid main() {\n    vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);\n    vPosition = mvPosition.xyz;\n    vNormal = normalize(normalMatrix * normal);\n    gl_Position = projectionMatrix * mvPosition;\n}"," reveal ---\n    float edge = 0.05;\n    float revealMask = dot(normal, vec3(0.0, 1.0, 0.0)); // Y-facing normals reveal first\n    revealMask = revealMask * 0.5 + 0.5; // remap to 0..1\n\n    float visible = step(uReveal, revealMask);\n    float edgeBand = smoothstep(uReveal - edge, uReveal, revealMask)\n                   - smoothstep(uReveal, uReveal + edge, revealMask);\n\n    vec3 edgeColor = vec3(.5, 0.8, 0.2);\n    color = mix(color, edgeColor, edgeBand);\n\n    gl_FragColor = vec4(color, visible);\n}"],"displacement":true,"i":1,"pointSize":false,"shaderLengths":[1790,3136],"uniforms":["modelViewMatrix","projectionMatrix","normalMatrix","uRemapColor","uLightDir","uFlatShading","uBrightness","uNormalStrength","uReveal"]},{"attrs":["position","normal","skinIndex","skinWeight"],"customHead":["vPosition;\n#endif\n#ifdef USE_INSTANCING\n\tmvPosition = instanceMatrix * mvPosition;\n#endif\nmvPosition = modelViewMatrix * mvPosition;\ngl_Position = projectionMatrix * mvPosition;\n#ifdef USE_LOGDEPTHBUF\n\tvFragDepth = 1.0 + gl_Position.w;\n\tvIsPerspective = float( isPerspectiveMatrix( projectionMatrix ) );\n#endif\n#if 0 \u003e 0\n\tvClipPosition = - mvPosition.xyz;\n#endif\n#if defined( FLAT_SHADED ) || defined( USE_BUMPMAP ) || defined( USE_NORMALMAP_TANGENTSPACE )\n\tvViewPosition = - mvPosition.xyz;\n#endif\n}","SIDED\n\t\tnormal = normal * faceDirection;\n\t#endif\n\tnormal = normalize( normalMatrix * normal );\n#elif defined( USE_NORMALMAP_TANGENTSPACE )\n\tvec3 mapN = texture2D( normalMap, vNormalMapUv ).xyz * 2.0 - 1.0;\n\tmapN.xy *= normalScale;\n\tnormal = normalize( tbn * mapN );\n#elif defined( USE_BUMPMAP )\n\tnormal = perturbNormalArb( - vViewPosition, normal, dHdxy_fwd(), faceDirection );\n#endif\n\tgl_FragColor = vec4( packNormalToRGB( normal ), diffuseColor.a );\n\t#ifdef OPAQUE\n\t\tgl_FragColor.a = 1.0;\n\t#endif\n}"],"displacement":true,"i":2,"pointSize":false,"shaderLengths":[16262,10893],"uniforms":["modelViewMatrix","projectionMatrix","normalMatrix","bindMatrix","bindMatrixInverse","opacity","boneTexture"]},{"attrs":["position","uv"],"customHead":["\n#ifdef USE_UV1\n\tattribute vec2 uv1;\n#endif\n#ifdef USE_UV2\n\tattribute vec2 uv2;\n#endif\n#ifdef USE_UV3\n\tattribute vec2 uv3;\n#endif\n#ifdef USE_TANGENT\n\tattribute vec4 tangent;\n#endif\n#if defined( USE_COLOR_ALPHA )\n\tattribute vec4 color;\n#elif defined( USE_COLOR )\n\tattribute vec3 color;\n#endif\n#ifdef USE_SKINNING\n\tattribute vec4 skinIndex;\n\tattribute vec4 skinWeight;\n#endif\n\nvarying vec2 vUv;\n\nvoid main() {\n    vUv = uv;\n    gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);\n}\n"," (uBackground == vec3(0.0)) ? color.a : 1.0;\n    alpha *= waterfallAlpha;\n    alpha *= uOpacity;\n\n    vec3 finalColor = blendNormal(uBackground, color.rgb, color.a);\n\n    // -------------------------\n    // ACCENT COLOR — edges only\n    // -------------------------\n    float trailMask = mouseTrail.r * ascii.r * edgeMask;\n    finalColor = mix(finalColor, uAccentColor, trailMask);\n\n    finalColor += vec3(uBrightness);\n\n    gl_FragColor = vec4(finalColor, alpha);\n    // gl_FragColor = mouseTrail;\n}"],"displacement":true,"i":3,"pointSize":false,"shaderLengths":[1642,9471],"uniforms":["modelViewMatrix","projectionMatrix","uNoise","uMedia","uFillPixels","uOverwriteColor","uGreyscale","uInvert","uMatrix","uResolution","uOffset","uAspect","uGranularity","uCharactersLimit","uOutProgress","uOpacity","uSmear","uBrightness","uScale","uColor","uAccentColor","uBackground","uTime","tDiffuse","tMouseTrail","uCharactersTexture"]}],"uniquePrograms":4}